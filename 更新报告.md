# 更新报告（大白话版，按版本顺写，最新在前）

---

## v3.1.10 回收站更好看了（UI 逐页精修·回收中心）

**你作为用户实际得到了什么：**
1. 回收站里「项目回收」那一块，原来只有一行灰字说明、没有标题，跟下面「节点回收站」有图标有标题不对称、看着像缺一块；现在补上「包裹」图标和「项目回收站」标题，上下两块整整齐齐、一眼分清哪块是项目、哪块是章节。
2. 顶栏那层磨砂玻璃调薄了，整页打开会轻轻淡入上浮，更通透不糊窗。

**底层原理（大白话）：**
就是给回收站这支「画笔」也换上全站统一的图标标题 + 去滤纸 + 入场转场——「项目回收站」补一个 `package` 图标标题、与下方「节点回收站」的 `file` 图标标题对称（图标负责区分、不引入新色），顶栏磨砂调薄、整页轻淡入上浮，换浅色/深色主题自动跟着变，不会哪天又冒出离谱色。

---

## v3.1.9 设置页更好看了（UI 逐页精修·设置中心）

**你作为用户实际得到了什么：**
1. 设置页里原来 8 个分区（外观/选模型/填钥匙/违禁词/快捷键/记忆衰减/墨灵助手…）都只是「1. 2. 3.」纯文字标题，像一份表单清单；现在每个分区前面都有一个小图标徽章（圆角方块 + 主题蓝边），一眼看出是哪一区。
2. 记忆衰减的 S/A/B/C 四档等级，原来只是灰文字，现在加了金/蓝/青/灰的彩色小徽章，哪档重要一目了然。
3. 顶栏那层磨砂玻璃调薄了，整页打开会轻轻淡入上浮，更通透不糊窗。

**底层原理（大白话）：**
就是给设置页这支「画笔」也换上全站统一的图标徽章 + 语义色令牌——每个分区配一个 Lucide 线性图标、记忆等级分到金/蓝/青/灰四支语义色，图标负责区分、颜色负责层次；这些都直接吃设计系统的令牌，换浅色/深色主题自动跟着变，不会哪天又冒出离谱色。


## v3.1.8 创意工坊更好看了（UI 逐页精修·预设市场）

**你作为用户实际得到了什么：**
1. 创意工坊（预设市场）里，每种预设现在都带自己的小图标 + 专属颜色标签——表格模板是蓝、剧情推进是绿、文风是香槟金、世界观是青、角色卡是琥珀、世界书是金、正则是玫瑰红、API 参数是蓝。一眼就能分清是哪种预设，不再是一片蓝字。
2. 鼠标移到预设卡片上，整张卡片会轻轻浮起来，边缘亮起跟它类型同色的光晕，像一张张实体卡片，更有质感。
3. 顶栏那层磨砂玻璃调薄了，更通透、不发懵。

**底层原理（大白话）：**
就是给预设市场这支「画笔」也换上全站统一的语义色令牌——每种类型分到红/绿/金/蓝/青/琥珀里最适合它的那支，图标负责区分、颜色负责氛围；卡片悬浮发光也是直接吃品牌类型色，换浅色/深色主题自动跟着变，不会哪天又冒出离谱色。

---

## v3.1.7 角色关系图也彻底统一配色了（UI 收官补刀·其二）

**你作为用户实际得到了什么：**
1. 工作台里那张「看角色之间怎么连」的关系图谱，之前还有几块游离的紫/粉/浅靛色——主角节点、情感线、暗线——跟全站风格不搭。现在主角改成闪眼的香槟金，反派用玫瑰红、正角用翠绿、师长用琥珀、情感线用品牌蓝、暗线用青，八种角色色各不撞色，深浅主题都跟手。
2. 主角头顶那颗星星、节点下面的名字，颜色也收成了品牌色，不再是写死的灰。
3. 游戏模式里「发现新角色/新设定」弹出的金色粒子，之前是紫色的，现在也统一成香槟金，跟全站一个调子。

**底层原理（大白话）：**
- 跟 v3.1.6 同一个思路：给关系图谱和游戏粒子这支「画笔」也换成全站统一的语义色令牌。主角用香槟金这支「身份色笔」，其余角色按语义分到红/绿/琥珀/蓝/青，互不相撞；改的时候直接用设计系统里的颜色变量（而不是写死十六进制），所以换浅色/深色主题会自动跟着变，不会哪天又冒出离谱色。

---

## v3.1.6 配色彻底统一了 + 设计语言写进文档（UI 收官补刀）

**你作为用户实际得到了什么：**
1. 之前 v3.1.0 大收官后，其实还漏着几处紫/粉的「离谱色」——导入向导里标「角色/人物」那一路、探索页构建配置的小芯片、设置里风格卡计数。它们游离在全站统一的配色逻辑之外，看着像另一个 APP。现在全部收成统一的香槟金，全站从此只有蓝/绿/黄/红/金五种语义色，配色逻辑只有一套，换主题也不会串色。
2. README（开源说明）新写了一章「界面与设计体系」，把咱们这套设计语言讲清楚：虚空玻璃（清透不糊窗的玻璃感，不是你讨厌的廉价彩色滤纸）、香槟金单一身份色、切到任意页面都轻轻淡入的转场、按钮按下冒金环等微交互。以后想一起改界面的朋友一眼就能看懂规矩，不会又写出离谱色。

**底层原理（大白话）：**
- 统一配色，本质是给全站定下「只有五支标准色笔」的规矩。以前那几处紫粉是有人（或 AI）顺手写死的颜色，不在这五支笔里；本轮把它们擦掉、改用香槟金这支「身份色笔」重写一遍，既保留了「人物路/世界路/章节」三栏用不同颜色区分的功能，又让颜色回到统一逻辑里。
- 给正文里「点角色名/设定名会高亮并跳到对应卡片」的功能补了十多例自动化测试，等于给它上了把锁——以后谁改坏了高亮命中，测试立马报错兜底。

**质量门禁**：tsc 0 错误；vitest 全量 115 文件 1190/1190 全绿（较 v3.1.5 基线 115 文件 1177 +13 例）；四处版本文件对齐 v3.1.6；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v3.1.5 世界书词条该排最后就排最后、该置顶就置顶（插入优先级 falsy 陷阱修复）

**你作为用户实际得到了什么：**
1. 你在世界书里建词条时可以设「插入优先级」（0 最低、100 最高，数字越大越靠前展示）。但以前有个隐藏 bug：你设成 0（想让它排最后、最低优先级）时，系统却把它偷偷改成 50（中高优先级），等于你点「排最后」系统给你排到了中间靠前——世界书词条的实际展示/注入顺序被静默反转。大纲里建节点时同理：你想把某章「置顶」（order=0）会被偷偷改成第 1。
2. 修好后：你设的 0 就真的是 0（最低/置顶），系统不再自作主张改掉。等于你点的顺序终于不被偷偷反转。这套 Agent 工具注册表的修复，和上一轮修「最该记住的伏笔排最前」是同一类毛病（都是把合法 0 当「没有」吞掉了），本轮一并治好。

**底层原理（大白话）：**
- 给词条排优先级，本质是一句「如果没填就用默认值 50」的程序。但以前写法是「没填 或 填了 0 都用 50」——因为程序里「0 || 50」会把 0 当成「空」一起处理。可 0 对你来说是合法的最低优先级，不是「空」。本轮改成「?? 50」：只有真没填（空值）才用 50，填了 0 就老老实实用 0。
- 这是把「该留的 0 留下、只兜底真空缺」的最小修正，没动任何其它逻辑。

**质量门禁**：tsc 0 错误；vitest 全量 115 文件 1177/1177 全绿（与 v3.1.4 基线一致、零回归）；四处版本文件对齐 v3.1.5；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v3.1.4 记忆分级不再漏记你中间章节、最该记住的伏笔排最前

**你作为用户实际得到了什么：**
1. AI 帮你写长篇时，会按重要程度把剧情记忆分成三档（S 核心 / A 重要 / B 背景）喂进写作上下文。但以前有两个隐藏毛病：一是中间那一章的摘要会被悄悄漏掉——既不算近期也不算远久，等于你那章写了什么 AI 忘了；二是最该记住的紧急伏笔（比如三天内要回收的）反而排到了最后、最不显眼。
2. 修好后：中间章的摘要现在会正常进 B 档归档（不再被漏掉）；最紧急的 critical 伏笔恢复排到 S 档最前面（最显眼、最优先注入）。同时给整套记忆分级逻辑加了 28 道自动化测试，以后谁改坏这套规则测试会立刻红，不会悄悄带病上线。

**底层原理（大白话）：**
- 记忆分级像一个档案管理员：每章写完后，它把记忆分三档——S 是必须记住的核心（未回收伏笔、重大转折、核心角色变化）、A 是近期重要事件、B 是久远章节的简短归档，然后按档把记忆喂给 AI。
- 以前管理员有两个糊涂处：一是归档的 cutoff 线划错了一格，导致倒数第 6 章的摘要掉进 A 和 B 之间的缝里被丢了（你写了但 AI 没收到）；二是给紧急程度打分时用了「0 || 2」这种写法——紧急度最低值偏偏是 0，而「0 || 2」在程序里会把 0 当成「没有」自动改成 2，于是最紧急反而被当成最不紧急排到最后。本轮把归档线补一格、把「||」改成「??」（只兜底真正的空缺、不吞 0），两个糊涂全治好。
- 配套加 28 例测试钉死哪类记忆进哪档、紧急伏笔排最前、预算超了怎么截断，等于给档案管理员发了标准作业卡。

**质量门禁**：tsc 0 错误；vitest 全量 115 文件 1177/1177 全绿（较 v3.1.3 +28 例）；四处版本文件对齐 v3.1.4；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v3.1.3 记忆蒸馏不再静默丢你的重要剧情：超额关键事件自动降级保留

**你作为用户实际得到了什么：**
1. 你写长篇小说时，越是重要的剧情/角色转折（系统评的分越高），越会被优先「记」进 AI 的写作上下文，让 AI 后续不跑偏、不忘设定。但以前有个隐藏 bug：当重要事件数量超过上限（最顶级 S 类超过 5 条、重要 A 类超过 15 条），多出来的那些会被**悄悄丢弃**——AI 根本读不到，等于你写了关键情节却「AI 忘了」。
2. 修好后，超出的关键事件不再被扔掉，而是**自动降级**保留：S 类超了的降一级进 A 类、A 类超了的降一级进 B 类关键词索引。你所有重要的设定/人物转折，AI 都至少能读到摘要，不会因数量多而静默丢失。对你来说是「长篇小说写到后面更不忘前情」。

**底层原理（大白话）：**
- 记忆蒸馏像一个「编辑助手」：每章写完后，它给所有事件打分分级——S 是核心（必须记住）、A 是重要、B 是背景、C 是琐碎仅存档。然后按层级把事件喂进 AI 的「记忆纸条」。
- 以前这个助手有个死板规矩：「S 类纸条只发 5 张，第 6 张起直接碎掉」——结果你第 6、7 个核心转折就消失了。本轮改成「S 类发满 5 张后，多出来的核心事件改发 A 类纸条、A 类发满 15 张后多出来的改发 B 类关键词纸条」，一张都不碎。哪个事件分高还排在前面，保证最重要的永远最显眼。
- 配套把旧的测试预期从「丢弃」改成「降级保留」并加 2 条排序用例，共 35 例测试钉死这套新规则，防止以后有人改回老毛病。

**质量门禁**：tsc 0 错误；vitest 全量 114 文件 1149/1149 全绿（较 v3.1.2 +2 例）；四处版本文件对齐 v3.1.3；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v3.1.2 文风模板逻辑补上测试防护网：你选的文风/禁用句式不会再悄悄失灵

**你作为用户实际得到了什么：**
1. 你写作时选的「文风模板」（热血竞技/悬疑等）背后的查询与应用逻辑，现在有 11 道自动化测试守着——将来任何人改这块代码把文风模板解析改坏（比如你选了某模板却没生效、或该禁止的句式没被禁），测试会立刻红，不会悄悄带病上线。
2. 对你来说是「看不见但更稳」：你设的文风约束和禁用表达能稳定注入给 AI，写出来的正文不会突然跑偏或冒出你禁用的句式。

**底层原理（大白话）：**
- 文风模板像一份「写作说明书」：你选一个模板，系统把它的风格描述（stylePrompt）和禁用句式拼进喂给 AI 的提示词。getTemplate 是按名字找到这份说明书的检索员；applyTemplate 是把说明书内容贴进总提示词拼装工；forbiddenPatternsToPrompt 是把你禁用的句式列成【禁止以下表达】清单的抄写员。
- 这仨都是纯拼装计算（给同样输入永远得同样输出），最适合用测试钉死。本轮给它们写了用例：模板名对不对得上、拼装后原提示词有没有丢字、禁用的句式清单条数对不对。等于给这三个拼装工发了标准作业卡，以后谁改坏一眼看穿。

**质量门禁**：tsc 0 错误；vitest 全量 114 文件 1147/1147 全绿；四处版本文件对齐 v3.1.2；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v3.1.1 剧情推进求值器补上测试防护网：宝宝流人设/记忆召回逻辑不再裸奔

**你作为用户实际得到了什么：**
1. 你写的「剧情推进」设定（角色随好感度/战力自动切换人设阶段、AI 按上下文自动召回相关设定）背后的核心计算逻辑，现在有 25 道自动化测试守着——将来任何人改这块代码，一旦把判断逻辑改坏（比如好感度 25 却显示成陌生人阶段、或该召回的设定没召回），测试会立刻红，不会悄悄带病上线。
2. 对你来说是「看不见但更稳」：你创作时角色人设不会突然错乱、AI 写作时不会漏掉该注入的世界观，体验一致性更有保障。

**底层原理（大白话）：**
- 宝宝流这套玩法像「填表驱动剧情」：你每写完一章，系统把事实抽进一张结构化表格（某角色好感度 25、战力 80），写作指令里写「如果好感度<=10 显示陌生人阶段，否则如果<=30 显示熟悉阶段」。evaluateIfCell 就是那个「读表、比对阈值、挑出当前该显示哪段」的裁判；recallContext 是「看当前正文提到了谁，把相关的世界书/表格设定挑出来喂给 AI」的检索员。
- 这俩都是纯计算（给同样的输入永远得同样输出、不联网不查库），最适合用测试钉死行为。本轮给它们各写了用例：嵌套 if/else 怎么层层判、六种比较符号（<= < >= > == !=）怎么算、表格缺了某行某列时怎么安全降级（不崩、不误判）、召回时怎么避免「青龙」被「青龙镇」吞掉。等于给这两个裁判和检索员发了标准答案册，以后谁改坏一眼看穿。

**质量门禁**：tsc 0 错误；vitest 全量 113 文件 1136/1136 全绿；四处版本文件对齐 v3.1.1；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v3.1.0 界面收官最后一笔：探讨/抽卡/预设打磨 + 全站转场

**你作为用户实际得到了什么：**
1. 探讨（跟 AI 聊构建小说）更跟手——步骤条会浮起、当前步亮起蓝晕，AI 头像带金环、气泡悬浮描边，发消息按钮也带图标和金环按压。
2. 抽卡（AI 出候选设定卡）更顺——卡头强调条从花哨的紫粉色收成统一的香槟金，卡片悬浮描边发光、一张张错峰冒出来，点采纳更有节奏。
3. 创意工坊（预设）更好逛——卡片浏览会依次淡入、鼠标移上去顶部扫过一道金光、分类标签选中发蓝光，挑预设套预设都更顺。
4. 全站转场——切到任意页面都会轻轻淡入（转屏），不再硬切，而且不会把抽屉/吸顶栏弄错位。
5. 漏网的紫粉色也统一成了香槟金，全站配色彻底一致。

**底层原理（大白话）：**
- 转场用了最稳的做法：整页只做透明度淡入，不动位移/缩放，这样页面里那些"固定抽屉""吸顶导航"不会被错误定位（位移/缩放会创建新的包含块把它们带歪）；更花哨的位移动效只放在页面内部的卡片上，作用域可控、不会波及固定元素。
- 那几处紫粉是早先设计还没收敛时的残留色，现在全换成"香槟金"这一个强调色，整套界面颜色就彻底统一了。

**质量门禁**：tsc 0 错误；vitest 全量 111 文件 1111/1111 全绿；四处版本文件对齐 v3.1.0；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v3.0.0 完全体来了：一条龙能写长篇、不降智、还不用填表

**你作为用户实际得到了什么：**
1. 经多轮深度自查确认，novel-forge 已经是一条龙能用的完整写作系统——从建书、套预设、让 AI 写、一路写到几十上百万字的长篇，设定和人物都不会写丢、不会越写越智障。
2. 你不用填任何表——挑一个内置预设（仙侠/都市/西幻世界观、古风/爽文文风、示范角色等 15 个）点「套用」就能直接开写；自动抽表默认是关的，不骚扰你。
3. 长篇不降智——AI 会把前面写不下、塞不进上下文的远章节先压成摘要保留情节，再配合「最近 4 章全文 + 最近 3 章摘要 + 故事线」三层记忆，写 100 章人设伏笔照样在线。
4. 全站视觉定型——首页/拆书/进书前后加上探讨/抽卡/预设一条龙美化完，转场和点击手感都在；顺手把极罕见错误页的写死颜色也换成主题色。

**底层原理（大白话）：**
- 套预设时，系统把预设里的世界观/风格/角色等内容直接写进你的项目，并立刻重新拼好「喂给 AI 的全局提示词」，所以下一章 AI 就知道你这本书的设定，不用你手动搬运。
- 写长篇小说时，AI 每次动笔前都会先读「最近的几章 + 最近几章的摘要 + 故事线大纲」；更早的章节如果太长塞不进上下文，会被 AI 自己先压缩成情节摘要再喂回来，等于给 AI 装了「长期记忆」，所以越写后面设定越不会乱。
- 自动抽表默认关，是因为那是个「锦上添花」的可选项（自动把正文里的新角色/设定抽成结构化卡），不是写作的前置条件；关掉它你照样能写，开了它才帮你整理。

**质量门禁**：tsc 0 错误；vitest 全量 111 文件 1111/1111 全绿；四处版本文件对齐 v3.0.0；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v2.61.0 拆书页也清爽了：图标统一 + 滤纸扒光 + 按钮有手感

**你作为用户实际得到了什么：**
1. 拆书结果里 15 个维度的图标（原来是一堆 emoji 表情）全换成了和全站一致的线性图标；拆解完成/失败的打勾打叉、空状态的小图标也统一了，不再有「这里混进了表情包」的违和。
2. 进度条的颜色不再写死大红大绿，改成跟随主题，你切浅色/深色主题也不会串色。
3. 把拆书页里糊窗的毛玻璃（总览卡、分组卡、维度卡、章节卡，共 7 处）全扒了，换成清爽的实体卡片层次；拆书导航顶栏也从「半透明+模糊」改成实心，不再发懵。
4. 输入框（书名/作者/原文/仿写参数等 9 处）统一成设计系统的输入样式，聚焦有描边、悬浮微变，不再各有各的写法。
5. 所有按钮终于有反馈了——开始拆解、查看结果、进入工作区、原样转项目、改编后转项目、应用修改、仿写生成，悬浮会浮起、点击有金色光环；维度行移上去亮起左侧细条，跟你之前看到的工作台一个手感。

**底层原理（大白话）：**
- 维度图标原来存的是 emoji 字符，现在改成图标组件的名字（比如「世界观」对应 globe），由统一的图标组件画出来，所以全站长一个样、换尺寸换颜色都听主题的。
- 毛玻璃模糊在你铺了纯色底的卡片上其实啥也糊不到（背后没东西），纯粹是「为了模糊而模糊」，所以直接去掉、用实体卡片的边框+投影出层次，更清爽也更省。
- 按钮之前「悬浮还是同一个颜色」等于没反馈，这次统一走 btn-primary/btn-success（自带悬浮变亮、按下缩放+金环），手感就回来了。

**质量门禁**：tsc 0 错误；vitest 全量 111 文件 1111/1111 全绿；四处版本文件对齐 v2.61.0；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v2.60.0 进书后的工作台更有「活气」了：转场 + 卡片跟手 + 游戏模式大扫除

**你作为用户实际得到了什么：**
1. 切到「角色 / 世界 / 故事 / 大纲」任一页，整块内容会轻轻淡入上浮——不再是硬切，终于有了转场感（你之前说很缺这个）。
2. 当前所在的那一页标签会亮起一圈靛蓝光晕，一眼就知道自己在哪。
3. 卡片更跟手：大纲里的章节、角色行鼠标移上去会亮起左侧细条；世界书卡片和故事线卡片会微微浮起带影子；右边工具箱卡片浮起时图标还发光。点点划划都更顺，而且不靠那种糊窗的滤纸。
4. 游戏模式大扫除：操作小徽标（战斗 / 对话 / 探索…）从花花绿绿的杂色统一成整套金色系配色；章纲编辑器里一堆 emoji 全换成图标、AI 生成按钮改成暗金底奶油字，整体不再像「另一个 APP」。
5. 顺手修了三个显示 bug：摘要大纲的错误提示此前没颜色（等于消失了）、故事线 AI 按钮的奶油字是写死的颜色、精修弹窗的黑遮罩统一成主题深色。

**底层原理（大白话）：**
- 转场靠给内容区换一个「带 activeTab 的入场动画」——每次切页 React 重挂这块，动画就重放一次，所以你看到的是淡入上浮而不是闪现。
- 高光和悬浮都是用主题色（靛蓝 / 金）的「阴影 + 位移」做出来，不是毛玻璃模糊，所以清爽又有层次。
- 游戏模式原本混着十几种写死的颜色和 emoji，这次全换成和设计系统一致的令牌色和图标组件，等于把「野外」拉回「编制内」。
- 三个 bug 都是「写错了颜色名字 / 写死了颜色值」，改成跟着主题走，换主题也不会再出错。

**质量门禁**：tsc 0 错误；vitest 全量 111 文件 1111/1111 全绿；四处版本文件对齐 v2.60.0；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v2.59.0 进书前的弹窗都变好看了：引导/项目设定/导入 + 全站按钮金环

**你作为用户实际得到了什么：**
1. 第一次进工作区会弹「新手引导」——三步上手（选个开局 → 让 AI 写 → 导出成书）依次淡入上浮，序号亮起光晕，下面几个功能特性图标卡片悬浮发光，点「开始创作」箭头会轻轻右移。
2. 「项目设定」弹窗（决定整本书基调的地方）四个分区加了小图标、依次进场；分段选项（快/慢节奏、创意/平常/简约、自动/自由/全权）选中时亮起题材色光晕、按下会微微缩一下；流派标签选中也亮起香槟色光晕。
3. 「导入备份包」弹窗头部加了图标，勾选项会亮起光晕、勾选对勾发光。
4. 全站所有按钮按下都会冒出一圈金色光环（之前只有部分按钮有），点击反馈更明确。

**底层原理大白话：**
- 这些弹窗沿用「虚空玻璃」体系，这版给它们加了进场节奏和点击反馈，不靠糊玻璃、靠图标/光晕/位移把层次和引导感做出来。最实用的一处：把金色光环脉冲动画从「只在特定按钮类上」扩展到「所有按钮组件」，以后你点任何按钮都能看到那圈金环——纯 CSS 关键帧，零后端/接口/LLM 变化。质量门禁 tsc 零错误、vitest 全量 1111 例全绿。个人 IP 仍归瑞宝宝。

---

## v2.58.0 首页大变样：实体化玻璃卡 + 进场动画 + 点击光环，不再糊窗也不再简陋

**你作为用户实际得到了什么：**
1. 首页那片 WebGL 纸船动画删了（你说过不要、还占性能），换成一排「灵感文体墙」卡片（仙侠/悬疑/科幻…12 种体裁）。悬浮会浮起发光，点一下就用那个体裁一键开局。
2. 之前文体卡是大糊玻璃，八张叠一起看着发晕；现在改成清爽的实体卡，淡淡染上每种体裁自己的颜色，悬浮才亮起光晕——不晕也不简陋。
3. 打开首页有进场感：标题/按钮依次淡入上浮；点按钮会看到一圈金色光环脉冲；点文体卡有题材色光晕漫开、「开局中」时边框呼吸；鼠标移到「进入工作台」「开始探讨」上，箭头会轻轻右移。

**底层原理大白话：**
- 设计体系叫「虚空玻璃」，本来就用毛玻璃。但之前每张卡都叠 40px 模糊，八张糊成一片，你嫌烦——这版让卡片不靠模糊、改用「深色底+淡淡体裁色+悬浮投影+光晕」把层次做出来，毛玻璃感还在但不晕。进场和点击的动效都是纯 CSS 关键帧（@keyframes）：Hero 用淡入上浮错开播放、按钮按下用向外扩散的金环、卡片按下用从中心散开的光、加载用呼吸边线。纯前端样式、零后端/接口/LLM 变化。质量门禁 tsc 零错误、vitest 全量 1111 例全绿。个人 IP 仍归瑞宝宝。

---

## v2.57.0 生成章节时「温度/确认角色/角色备注」怎么定，现在有了护栏

**你作为用户实际得到了什么：**
1. 你每次让 AI 写一章，系统都要决定三件事：这一章用多高的「温度」（创造性）和「topP」、这次写作用哪几个你确认过的角色、你给角色写的备注怎么喂给 AI。这三件事背后的计算逻辑此前没有任何测试，谁改错了也不会报警——表现出来就是「明明设了温度 0 却还是高温乱写」「确认过的角色没出现」「角色备注没生效」之类的诡异现象，你只有写出来才发现。
2. 这一版给这三段逻辑补了 22 条自动化测试，把最容易出错的边界全部钉死：温度的优先级（你项目里设的 > 文风模板默认值 > 系统兜底值）、你设了 0 这种合法值必须真用 0 不能偷偷被兜底替换掉、选了不存在的文风模板要安全回退、确认角色列表过滤正确（没确认的不会出现、重复确认不会重复算）、角色备注正确拼接进提示词且不出现「指向不存在角色」的幽灵备注。

**底层原理大白话：**
- 这就像给「配菜员」摆了 22 个标准样品：以前配菜员（生成预处理函数）决定一章用多大火候、放哪几样料、加什么备注，全凭手感没人验收，偶尔配错你也只能等菜端上来（章节写出来）才察觉。现在我们对照 22 个标准答案验收——火候优先级错了、0 被吞了、料多算了、幽灵备注出现了，测试立刻报警。纯加质检、没动生产线（零功能改动）。质量门禁 tsc 零错误、vitest 全量 1111 例全绿。个人 IP 仍归瑞宝宝。

---

## v2.56.0 自动填表默认关了，确认/交付开关也挪到一处好管

**你作为用户实际得到了什么：**
1. 以前不管你开不开，系统默认每章写完都自动往「角色卡/世界书」里塞东西——容易塞出脏卡、还打扰你。现在默认关了：新项目不再自动填表，想用就在「自动填表」设置里打开开关。你已有的老项目保持原样，不受影响。
2. 以前「自动确认章节」「自动交付全书」这两个开关藏在章节确认栏里，不好找。现在把它们和「自动填表」开关挪到同一个设置弹窗里——填表开关、填表频率、跳过最新章、上下文楼层、自动确认、自动交付，六个自动化选项一屏看完、随手切。改完工作区立刻生效，不用重开。
3. 关于「新角色自动同意建卡」：这就是「自动填表」打开时的效果（开启后系统自动把新发现的角色/世界实体写进结构化表），已经在填表开关的说明里写清楚了，没另搞一个开关，保持简单。

**底层原理大白话：**
- 这就像工厂的「自动上料」总闸：以前总闸默认开着，流水线每过一章就自动往材料库塞一批料，有时塞错（脏卡）。这一版把总闸默认关掉，需要时你手动开。同时把原本散在两个控制台的「自动确认/自动交付」按钮，并到「自动填表」这一个控制台面板上，接线也理顺——保存时既写进服务器（数据库），也顺手更新前端的内存状态，所以你改完面板，工作区那边的确认栏马上显示新状态，不用刷新重进。质量门禁 tsc 零错误、vitest 全量 1089 例全绿。个人 IP 仍归瑞宝宝。

---

## v2.55.1 探索里填的设定以后不会「静默丢」了：15 条测试给拼提示词加护栏

**你作为用户实际得到了什么：**
1. 你在「探索 / 智能规划」里填的书名、类型、受众、字数、流派、核心冲突、力量体系、金手指、风格偏好，还有你采纳的设定片段，这些都会被拼成一段「给 AI 的写作说明书」，让 AI 知道这本书的世界观长啥样。这一段拼装逻辑此前没有任何测试，谁改错了也不会报警——你只有写出来的章节里设定突然对不上才发现。
2. 这一版给这段拼装逻辑补了 15 条自动化测试，把最容易出错的边界全部钉死：各段落必须按固定顺序排（基本信息→流派→冲突→力量→金手指→风格）、没填的字段不显示空段、多个流派用中文顿号连接、未知情节结构不瞎编直接显示原值、你采纳的设定按固定顺序排、单条太长自动截断、某类设定没填就跳过、中文和特殊符号原样保留不乱码。

**底层原理大白话：**
- 这就像给「打包员」摆了 15 个标准样品：以前打包员（拼提示词的函数）把你的设定装进寄给 AI 的箱子，没人验收，他装错顺序或漏装某条设定你也发现不了。现在我们对照 15 个标准答案验收——顺序错了、漏装了、乱码了、截断错位置了，测试立刻报警。纯加质检、没动生产线（零功能改动，只是把一份之前没进仓库的测试正式收编）。质量门禁 tsc 零错误、vitest 全量 1089 例全绿（和上一版一样、本版零新增行为）。个人 IP 仍归瑞宝宝。

---

## v2.55.0 章名风格可以自己选了：极简 / 诗句 / 文笔 / 简短 / 悬念，一键切换

**你作为用户实际得到了什么：**
1. 以前系统自动起的章名全是「极简 ≤5 字」那一种（比如「祭坛觉醒」「边城夜雨」），你喜欢诗意的或者勾人的章名也没辙。现在项目设置里多了一个「章节标题风格」开关，五选一——默认极简、诗句（五七言韵味）、文笔（文艺抒情）、简短（直白点题）、悬念（抛个疑问勾你好奇）。选完点一下就生效，下次生成章名就按你选的风格来。
2. 关键是「真按风格出章名」：以前不管你选啥，底层都偷偷把章名砍到 5 个字，诗句风格根本出不来。现在不同风格允许不同长度（极简 5 字、简短 9 字、诗句/文笔/悬念 14 字），而且 AI 出章名的提示里会按你选的风格给样例——选了「诗句」它就往五七言上靠、选了「悬念」它就抛疑问。通用规矩不变：章名里绝不出现角色人名、也绝不写「第N章」这种占位。
3. 全程不啰嗦：设置页就一排按钮，点完即时保存，不弹窗不解释，符合「能精简就精简」。质量门禁 tsc 零错误、vitest 全量 1089 例全绿（本版零新增测试）。个人 IP 仍归瑞宝宝。

**底层原理大白话：**
- 这就像给起名师傅发「风格卡」：以前师傅只会一种起名法（极简），你递什么风格需求他都不看、一律砍 5 字。现在我们在两处都接上了——①你点的风格先存进项目资料（数据库加了一列 titleStyle，点按钮立刻写进去）；②写正文时系统把这张风格卡同时递给「起名师傅」（summarizeChapter，它出的章名样例现在按你选的风格给）和「收尾修剪工」（deriveChapterName，按风格允许的最大字数剪、不再一刀切 5 字）。整条链路从「你点按钮 → 存进项目 → 生成章名 → 修剪成合规章名」全打通。质量门禁 tsc 零错误、vitest 全量 1089 例全绿。个人 IP 仍归瑞宝宝。

---

## v2.54.0 批量写章进度条不再卡死，而且慢模型下跨章上下文一点不丢

**你作为用户实际得到了什么：**
1. 上一版（v2.52.0）为了让进度条快点到 100%，把「写完后还要做的审校+摘要」偷偷放到后台跑——但这是个慢模型的隐患：连写好几章时，下一章会在上一章的摘要还没存好时就开写，导致后面章节读不到前面章节的剧情摘要、跨章记忆悄悄断链。这一版把它改回「写完一章、把它的摘要存好、下一章才动笔」，保证无论模型快慢，每一章都能拿到前面所有章的上下文。
2. 但你最在意的那条「至少要有个进度条、慢也要有进度条」也保住了：我们没让进度条在存摘要那 ~140 秒里冻住，而是把进度拆成阶段——正文写作占 0~85%，审校 86/90%，摘要 92/97%，逻辑自查 99%，全部完成 100%。条子全程平滑走动，看着就像在干活，而不是卡死。
3. 顺带做了一次诚实审计：上一版（v2.53.0）的更新说明写着「零生产代码改动、无真实 bug」，但我们核对它的实际提交发现——它其实已经悄悄把「长设定精简时偶尔切在半句话」的截断 bug 修好了（只是说明写漏了）。所以这版不重复修，只把上面两条真正属于本版的改动交付给你。

**底层原理大白话：**
- 这就像流水线打包：v2.52.0 为了「快」，让「贴标签」工序（摘要）在包裹（章节）已经发出去后才补做，结果下一个包裹装箱时标签还没贴好、就按旧信息装了——后面的包裹全串错。这一版恢复成「贴好标签、确认入库，再装下一个包裹」，慢一点但每一箱信息都对。为了让等待贴标签的 ~140 秒不像死机，我们把传送带上的进度灯分成几段亮（写作→审校→摘要→自查→完成），你一眼就知道卡在哪一步、没卡死。我们还真机验证过：新城项目第 13 章的正文，比第 12 章摘要存好的时间晚了约 2 分 49 秒——说明第 13 章确实是在第 12 章摘要已经存在之后才动笔的，上下文链没断。质量门禁 tsc 零错误、vitest 全量 1074 例全绿（与 v2.53.0 同基线、本版零新增测试）。个人 IP 仍归瑞宝宝。

---

## v2.53.0 世界设定再也不会被「去重」偷偷删掉、提示词也不再超窗卡顿

**你作为用户实际得到了什么：**
1. 给「自动精简设定」功能（你套用创意工坊预设、系统把角色卡/世界书压成一份喂给 AI 的写作提示词）的两个底层工序，补了 15 条自动化测试。其中一个工序负责「世界卡去重」——同一个世界观条目如果你贴了两次，它只保留最完整的一份；另一个工序负责「预算兜底截断」——设定太多时在保证不切断半句话的前提下，把提示词压进 AI 的上下文窗口。
2. 这类测试是「防护网」：以前只要有人改了去重逻辑、不小心把你的某条世界设定当重复删掉，界面上不会报错、你只有写出来的章节里世界观突然对不上才发现；或者截断逻辑改错、提示词撑爆窗口导致单章生成变慢。现在任何这种破坏都会被测试立刻抓住。

**底层原理大白话：**
- 这就像工厂的质检流水线：自动精简设定是「按规格装箱」的工序，我们给这道工序摆了 15 个标准样品（同名条目留最长的、没名字但内容不同的两条都留、换行位置不同时怎么切才不切半句等），每个都对照标准答案验收。以后谁改了工序、把某种设定装错箱子或切错位置，质检立刻报警。纯加质检、没动生产线（零功能改动，只给两个函数加了导出标注）；质量门禁 tsc 零错误、vitest 全量 1074 例全绿。个人 IP 仍归瑞宝宝。

## v2.51.3 导入设定再也不怕「解析器 silently 改坏」你的角色世界

**你作为用户实际得到了什么：**
1. 给「智能填表」功能（你贴一段设定文本，AI 自动拆成角色卡/世界卡/风格卡）的核心解析器，补了 21 条自动化测试。它专门验证：AI 返回的设定 JSON 不管带不带代码围栏、字段齐不齐全、数字是不是真的数字，解析器都能正确归一化成卡片；就算你贴的设定里某个角色没写年龄，它也会老老实实填「未知」而不是把整张卡搞崩。
2. 这类测试是「防护网」：以前只要有人改了这段解析逻辑、不小心把你的角色名字归错或把世界词条分类搞乱，界面上不会报错、你只有用的时候才发现卡不对——现在任何这种破坏都会被测试立刻抓住。

**底层原理大白话：**
- 这就像工厂的质检流水线：AI 是粗心的打包工，解析器是「按规格整理装箱」的工序。我们给这道工序摆了 21 个标准样品（正常包裹、缺零件的包裹、贴错标签的包裹、甚至包错纸箱的包裹），每个都对照标准答案验收。以后谁改了工序、把某种包裹装错箱子，质检立刻报警。纯加质检、没动生产线（零功能改动）；质量门禁 tsc 零错误、vitest 全量 1059 例全绿。个人 IP 仍归瑞宝宝。

## v2.51.2 游戏背包不再「前端显示有、后端其实没」错位

**你作为用户实际得到了什么：**
1. 修掉游戏模式里一个隐形错位：打游戏时 AI 偶尔会说「你捞到了某件宝物」之类的话，后端其实把这个东西放进你背包了，但前台界面当时没显示——要等下次重新拉取才对上，中间那段你以为没拿到。
2. 现在前台和后端彻底对整齐了：只要是「获得」类的说法（哪怕换了个生僻近义词），前台当场就进背包；卖东西、抵押这类「流转」和说不清的怪动词，前台和后端一样选择不动背包、绝不乱加。以后打游戏背包所见即所得。

**底层原理大白话：**
- 这就像仓库前台和后台各有一本存货账，平时都对得上，但碰到一句「偏门说法」后台记了、前台漏记，账就对不上了。我们给前台那本账也补上和后台一模一样的记法规则（获得就入账、流转和怪词不记），两本账从此永远同步。顺手加了 21 条自动化测试，把「获得/消耗/装备/卸下/损毁/跳过」每种情况以及「同名不同人分开记」「不改动你原本的账本」都锁死，以后谁改坏这笔账立刻报错。纯修对账、没动任何生成功能；质量门禁 tsc 零错误、vitest 全量 1033 例全绿。个人 IP 仍归瑞宝宝。

## v2.51.1 修了一个会让你部署失败的隐藏坑（残缺发布收口）

**你作为用户实际得到了什么：**
1. 修掉一个「本地能跑、一上线就崩」的隐患：上一版 v2.51.0 的章节统计区用了一个「章节字数分布柱状图」组件（自动用全书均值划出太水章/超长章，还有均值参考线），但这个组件的源文件压根没进 git 仓库——全靠你电脑磁盘上恰好有它才没报错。
2. 现在把它正式提交进仓库了，含义是：以后从零拉代码、走 CI、或者部署到 Vercel，都不会再因为「找不到这个文件」而编译失败；线上代码和你仓库里的代码终于一致、可复现。

**底层原理大白话：**
- 这就像你写了一页说明书、引用了另一页图，但那页图没装订进册子——你自己桌上摊着没事，别人拿一本新印的册子就缺页报错。我们把那页图（ChapterWordCountChart 组件）补装订进册子（提交进 git），以后任何一份新册子都完整。纯补提交、没改任何功能逻辑、也没碰你正在并行写的其他改动；质量门禁 tsc 零错误、vitest 全量 1012 例全绿照旧。个人 IP 仍归瑞宝宝。

## v2.51.0 关系图谱变好看、还能点角色跳到它出场的章节

**你作为用户实际得到了什么：**
1. 关系图谱不再是一坨直线打结——改成力导向自动布局，线条变成柔和曲线，点某个角色只高亮它的关系线、其余淡出；节点能拖来拖去摆位置，关掉再开还在你摆的地方。
2. 角色卡和正文打通了：在角色卡里点关系图上的某个人，会列出他「在哪几章出场」（自动扫正文匹配名字），再点那一章就直接跳过去看——角色不再是个孤岛，能顺着关系摸到剧情位置。
3. 用你的硅基流动 DeepSeek-V4-Flash 真机跑了一次多章写作实测：连写 3 章，跨章节记忆一点没丢（第9-11章精准接着前8章的人名、设定往下写）、你建的角色卡/世界卡真的被 AI 用上了、后台任务带进度条、创意工坊预设/规则/文风卡全都创建并生效。
4. 实测也揪出了几个要优化的点（已出专门计划文档）：每次生成把 18 万字的设定全塞给 AI 太重、进度条会卡 0% 几分钟、单章约 3 分半偏慢、章节开头「剧情校准条」时有时无、批量写章还差一个「章纲确认再写」的闸门。

**底层原理大白话：**
- 关系图谱以前是「两点之间画直线」，章一多就糊成毛线团；现在改成「力学模拟」——每个角色是个互相排斥又用弹簧连着的点，电脑反复算到它们自动散开不重叠，连线再弯成弧线，看着就清爽了；你拖动的摆位记在浏览器本地，下次还在。出场联动是给角色卡对话框喂进全书章节，用角色名字去正文里「搜」他在哪几章被提到，列成清单，点一下就跳过去。多章写作实测走的是现成的「后台批量写」：先建章+生成章纲（带前文章纲续写保证连贯），再写正文，全程真实调用你的硅基流动 key。实测发现的「18万字全量注入」是速率慢的头号原因，已列入下一版重点修。个人 IP 仍归瑞宝宝。

---

## v2.50.6 给「AI 记忆筛选器」上保险：以后改代码不会悄悄让 AI 忘了重要剧情

**你作为用户实际得到了什么：**
1. 表面上照样啥也看不到——这一版还是纯测试、没动你用的任何功能、任何界面长相，你打开 novel-forge 跟上一版一模一样。
2. 但它在悄悄给 novel-forge「记忆系统」背后一个从没被测过的核心函数上保险：这个函数负责给小说里发生的每件大事打分分级（核心 S / 重要 A / 背景 B / 丢弃 C），决定哪些事要喂给 AI 当写作上下文、哪些事只存档。它一旦被改坏（比如分级线算错、截断逻辑写反），AI 写续集时就会悄悄漏掉关键伏笔、记错人物重要性，而你毫无察觉。以后万一哪次改动把它搞坏，测试会立刻报警。

**底层原理大白话：**
- 这个评分函数是个「纯逻辑」（同样输入必给同样输出、不碰数据库不联网），最适合用自动化测试钉死行为。它好比「编辑部的审稿分级台」——每来一条剧情事件，先按关键词判断是「死亡/突破/战斗/揭露」哪一类（死亡比战斗更要紧、优先判死），再叠加「这事离现在多少章（越近分越高）、牵扯到主角还是路人（主角加分多）、有没有埋/收伏笔（收伏笔加分更高）」算出总分，最后按 40/20/10 三条分数线分到 S/A/B/C 四档，挑最重要的 5 条核心 + 15 条重要喂给 AI。本轮把「分类优先级对不对、评分各项加得对不对、分数线卡得准不准（含边界等号）、批量截断与排序稳不稳、喂给 AI 的提示文本长啥样」全测了一遍（共 33 例），证明这个分级台本身是健康的、并把它锁死防回归。零生产代码改动。额外如实记一笔：目前超出 5 条的核心事件会被直接丢弃、不会降级补进重要档，这是个可优化的点、留作后续评估，本轮不擅自改核心语义。

## v2.50.5 导出 EPUB 的「压缩包打包」上保险：让你导出的电子书永不变「打不开的砖」

**你作为用户实际得到了什么：**
1. 表面上照样啥也看不到——这一版还是纯测试、没动你用的任何功能、任何界面长相，你打开 novel-forge 跟上一版一模一样。
2. 但它在悄悄给「EPUB 电子书导出」背后一个从没被测过的底层函数上保险：这个函数负责把你的书一章章打包成一个标准电子书压缩包（和 zip 一个原理）。它一旦写错（签名错位/字节偏移算错/校验码算错），你导出的 .epub 文件会被阅读器判定损坏、直接打不开，而且程序自己一声不吭、没有任何报错。以后万一哪次改动把它搞坏，测试会立刻报警，不会等你导出一个打不开的电子书才后知后觉。

**底层原理大白话：**
- 这个打包函数是个「纯逻辑」（同样输入必给同样输出、不碰数据库不联网），最适合用自动化测试钉死行为。它好比「发货前的打包封箱」——你书里每一章、每个文件都得按标准 zip 格式码放整齐：封箱标签（签名）得对、每箱的位置偏移得算准、每箱内容的校验码（crc32）得算对，阅读器才能一层层正确拆箱读出你的书。本轮把「三处签名是否正确、校验码是否对得上标准答案、中文文件名是否存对、多章偏移是否算准、空内容是否安全、用真实解压库能不能还原全部内容」全测了一遍，证明这个封箱逻辑本身是健康的、并把它锁死防回归。零生产代码改动。

---

## v2.50.4 DOCX 导出转义上保险：让你导出的 Word 永不变「乱码砖」

**你作为用户实际得到了什么：**
1. 表面上照样啥也看不到——这一版还是纯测试、没动你用的任何功能、任何界面长相，你打开 novel-forge 跟上一版一模一样。
2. 但它在悄悄给「Word 导出」背后一个从没被测过的底层函数上保险：这个函数负责把正文里的特殊符号（小于号、和号这类网页保留字符）转成 Word 能正常读的安全写法。它一旦写错，你导出的 .docx 文件会静默损坏——Word 打不开或内容错乱，而且程序自己一声不吭、没有任何报错。以后万一哪次改动把它搞坏，测试会立刻报警，不会等你导出一个打不开的文档才后知后觉。

**底层原理大白话：**
- 这个转义函数是个「纯逻辑」（同样输入必给同样输出、不碰数据库不联网），最适合用自动化测试钉死行为。它好比「发货前的打包质检」——你书里要是有小于号、和号、引号这种特殊符号，它必须老老实实换成 Word 认的安全写法（比如把 < 写成 &lt;），Word 才能正常显示、黑客也塞不进恶意东西；本轮把正文、标题、作者名里的五类危险符号、中文保留、多行换行、空章节回退全测了一遍。这个函数此前只有「流式导出结构等价」的间接覆盖，转义逻辑本身零防护网，本轮直接锁死契约防回归。零生产代码改动。

---

## v2.50.3 导出转换纯函数补测试：给「电子书目录顺序」和「网页不乱码」上保险

**你作为用户实际得到了什么：**
1. 表面上你同样啥也看不到——这一版还是纯测试、没动你用的任何功能、任何界面长相，你打开 novel-forge 跟上一版一模一样。
2. 但它在悄悄给两个「你每导出一本电子书都在跑、却从没被测过」的底层函数上保险：一个是「把特殊符号转成网页安全写法」的转义函数（不转好，导出的书会乱码甚至被塞进恶意代码），一个是「决定电子书目录章节顺序」的遍历函数（它排错了，你书的目录就章节乱跳）。以后万一哪次改动把这俩搞坏，测试会立刻报警，不会等你导出一本目录全乱的书才后知后觉。

**底层原理大白话：**
- 两个函数都是「纯逻辑」（给同样输入必给同样输出、不碰数据库不联网），最适合用自动化测试钉死行为。第一个转义函数好比「发货前的打包质检」——你书里如果有小于号或和号这种网页保留符号，它必须老老实实换成安全写法，网页才能正常显示、黑客也塞不进恶意脚本；本轮把五类危险符号（含引号）、混合、空内容、中文保留都测了一遍。第二个遍历函数好比「目录编排员」——你书里章节有嵌套（卷下有章、章下有节），它要按正确层级一层层排进目录，本轮测了单章、多章排序、嵌套子章节、目录要不要带大纲、空名字怎么兜底、同层谁先谁后按时间兜底，确保目录顺序永远跟你书的真实结构一致。两函数本来逻辑就是对的（已逐一实跑确认没问题），本轮只补「防护网」，零生产代码改动。

---

## v2.50.2 关键路径纯函数补测试：给两个「看不见但天天在跑」的核心函数上保险

**你作为用户实际得到了什么：**
1. 表面上你同样啥也看不到——这一版还是纯测试、没动你用的任何功能、任何界面长相，你打开 novel-forge 跟上一版一模一样。
2. 但它在悄悄给两个「藏在底层、每次操作都在跑、却从没被测过」的关键函数上保险：一个是「数据库里的章节数据翻译成界面能用的格式」的桥接函数，一个是「保证同一故事线的多处修改不互相覆盖」的并发护栏。以后万一哪次改动不小心把合法的章节状态偷偷降级、或者让并发写互相覆盖丢内容，测试会立刻报警，不会等你发现书丢了才后知后觉。

**底层原理大白话：**
- 两个函数都是「纯逻辑」（给同样输入必给同样输出、不碰数据库不联网），最适合用自动化测试钉死行为。第一个桥接函数好比「仓库发货前的质检翻译」——数据库存的是原始字符串，界面要的是规范类型；翻译时如果遇到不认识的怪值，必须老老实实降级成安全的默认值，而不能崩溃或乱填。第二个护栏函数好比「同一道菜只允许一个厨师同时炒」——同一故事线的多处修改排队串行，谁先抢到锁谁先写、写完下一个再上，绝不让两个厨师同时翻锅把菜炒糊（数据覆盖丢失）。本轮给它们各写了一沓断言：已知值原样过、未知值安全兜底、同故事线严格排队、不同故事线互不挡路、前一个炒糊了（抛错）也不影响后面正常炒、且错误会如实报给调用者。两函数本来逻辑就是对的（已逐一核对类型白名单无误），本轮只补「防护网」，零生产代码改动。

---

## v2.50.1 上帝组件拆解第一刀：工作区「弹窗山」搬进独立小屋，以后改弹窗不怕牵一发动全身

**你作为用户实际得到了什么：**
1. 表面上你同样啥也看不到——这一版没动你用的任何功能、任何界面长相。它是给工作区那张超大的「总页面」做的内部分流。
2. 但它在悄悄消除一个隐患：那个总页面（WorkspacePage）原本又长又胖（1500+ 行、塞了 66 个状态开关），里面光是「点一下弹出来的各种对话框」（建角色、改词条、导入、批量写、生成大纲、导出、备份……足足 14 种）就占了好几百行，改其中一个弹窗，稍不留神就可能把别的弄坏。现在这 14 个弹窗被整队搬进了一个独立的「弹窗小屋」组件，主页面只留一行调用；以后的弹窗改动被关在小屋里，不再威胁整张主页面。

**底层原理大白话：**
- 弹窗状态集中收口：以前每个弹窗的「开/关」开关都散在主页面的 66 个状态里（就像 66 个独立电灯开关散布在墙上）。现在把它们收进一个专门的「弹窗总控」（useWorkspaceDialogs hook），开关名一个没改，主页面通过 `const dialogs = ...` 一次性全接过来，原来写 `setShowOutlineDialog(true)` 的地方照样能跑，零改名、最低风险。
- 只搬「独立弹窗」、主流程不动：保存冲突、精修对比、抽卡、生成前确认这 4 个和正文生成主流程绑死的弹窗留在主页面没动；真正会写正文的 5 个处理函数也留在主页面，只通过 hook 把「开弹窗」的按钮接过去。等于只把「配角弹窗」请进小屋，主角还在台上。
- 顺手把两个「弹窗专属定时器」（章纲生成进度轮询、实时耗时秒表）也搬进了小屋，主页面更清爽；并加了 3 个测试锁死「小屋」的对外契约，以后谁改坏了接口测试立刻红。

**质量门禁：** SAFE_DELETE_DISABLE=1 npx tsc --noEmit 0 错误；npx vitest run 99 文件 934/934 全绿（较 v2.50.0 基线 931 +3 例）；四版本文件对齐 v2.50.1；按重切路线 v2.50.0(注册表减负)→v2.50.1(上帝组件拆解·本版)→v2.51.0(Prisma 表名小写迁移+注册表去 any) 推进；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v2.50.0 架构拆弹·Agent 注册表减负：工具表的「规矩」收口成一处，以后改不会漏

**你作为用户实际得到了什么：**
1. 表面上你啥也看不到——这一版没有动你用的任何功能、任何界面。它是给「AI 接管按钮」那套后台工具表做的内部整理。
2. 但它在悄悄消除一个隐患：以前那张工具表里，同一个「允许填的值清单」（比如角色能选哪 7 种定位、世界书能选哪 15 种分类）在每个工具里都手抄了一遍，哪天改一处忘了另一处，AI 就可能认不出你填的分类；还有世界书「中文分类→英文」的翻译表，两处各写了一份、还写得不一样，有一处漏了好多分类。现在这些「规矩」全收口成唯一一份，两处共用，既不会再抄错，也顺手修好了漏翻译的 bug。

**底层原理大白话：**
- 枚举常量收口：以前像「角色定位有哪几种」这种清单，散落在十几个工具的定义里各抄各的（就像公司十几个部门各自手写一份员工守则，迟早对不上）。现在把它抽成公司总部一份总守则（CHARACTER_ROLES 等七个常量），各部门都引用同一份，谁要改只需改总部一处，全公司立刻一致，编译器还能帮着把关不许写错值。
- 分类映射单一真相源：世界书那个「中文大类名→程序内部英文代号」的翻译表，之前建词条和改词条两处各写了一份，而且改词条的那份漏了好几类（物品/功法/魔法/货币等），等于你用中文填「法宝」它可能不认。现在合成唯一一份（取两份并集、更全），两处共用——既去掉重复，又补上了漏掉的翻译，中文分类以后都能被正确识别。

**质量门禁：** SAFE_DELETE_DISABLE=1 npx tsc --noEmit 0 错误；npx vitest run 98 文件 931/931 全绿（与 v2.49.0 同基线，本版纯重构回回归门禁全绿）；四版本文件对齐 v2.50.0；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v2.49.0 性能止血：AI 写长书不再卡成 PPT + 游戏模式报错不再露内部黑话 + 保存完不再整本重载

**你作为用户实际得到了什么：**
1. 你让 AI 写一章（尤其长篇、几十万字的大书）时，正文是顺滑地一个字一个字冒出来，而不是整页跟着抖、写到后面越来越卡——以前每蹦一个字，整个工作区页面（左边大纲、右边面板）都被迫跟着重新算一遍，大书越写越卡；现在只有正文那一块在更新。
2. 你在游戏模式里如果出错（比如后端没连上），弹出来的提示是能看懂的话，而不是以前那种把程序内部报错原文甩你脸上的黑话。
3. 一章 AI 写完、落库保存之后，页面刷新快了——以前每次写完都要把整本书从头到尾重新下载一遍才敢显示，大书尤其慢；现在只重新拉「刚写好的那一章」，几毫秒的事。

**底层原理大白话：**
- 流式不抖全树：以前「正在写的这一章的临时正文」存在大组件自己的记事本里，每写一个字就举全公司喇叭通知所有人重算；现在把它挪到专门管写作的小仓库（useWriterStore），只有正文显示区订阅了这只小仓库，写字时只通知它一家更新，全公司（整页）不用陪跑。
- 报错收敛：游戏模式的出错处理统一用之前写好的「把程序内部错误翻成人话」的函数，并且特意保留前端认识的字段名，免得修 bug 修出新 bug（前端读不到错误）。
- 局部刷新：写完一章，不再把整本书重新下载，而是只去后端要「刚写好的那一条」，就地更新数据仓库里对应那一章——就像改完一页通讯录，只更新那一行，不用把整本重抄。

---

## v2.48.0 地基止血·收口：工作区不再藏着「选中新、库里旧」的脏数据 + 大组件瘦身可测

**你作为用户实际得到了什么：**
1. 你手动保存一章、或解决了保存冲突之后，左侧大纲树、确认状态条这些读「项目库」数据的地方，现在会立刻显示最新结果（比如这一章已经「已定稿」了），不再出现「正文区明明保存成功了，但左边大纲还显示旧状态」的别扭不一致。
2. 之前代码里那个 1500 多行的大组件（工作区页面），把几段「算一算哪些节点算章节、全书是否都确认了、当前写到第几阶段」的纯计算逻辑抽成了独立、能自动测试的小模块——这步是对「把大组件拆小、以后更好维护」的稳妥开篇，不改动你看到的任何界面。

**底层原理大白话：**
- 保存一致性：之前你点保存，程序只更新了「当前正在编辑的那一页」的显示，但「整本书的数据仓库」（store）里的那一章还是旧内容，要等下次整体刷新才同步——这就像你改了便签却没更新通讯录。现在保存成功后会立刻把服务端返回的最新那一章同时写回数据仓库，两边永远一致。
- 大组件瘦身：之前那几个「筛选章节 / 统计确认 / 推算阶段」的计算直接写在 1500 行大页面里，既难测又重复。现在抽成独立的纯计算函数（给什么数据、出什么结果，不碰界面），并配了 11 个自动化测试锁死行为，以后要再拆这个大组件就有干净的地基了。

**质量门禁：** SAFE_DELETE_DISABLE=1 npx tsc --noEmit 0 错误；npx vitest run 97 文件 927/927 全绿（较 v2.47.0 基线 916 +11 例 workspace-derive 测试）；四版本文件对齐 v2.48.0；Prisma 表名小写迁移/全量 loadProject 替换仍属线上库受控迁移风险项、本版不冒险留待治理；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

## v2.47.0 地基止血：报错不再白屏猜谜 + 接口校验统一 + 节点类型不再拼错

**你作为用户实际得到了什么：**
1. 生成正文/续写/微调时如果出错（比如数据库没连上、表没建、网络不通），不再是千篇一律的「服务器内部错误，请查看日志」然后白屏——前端现在能直接看到具体原因（比如「数据库表不存在，请执行 npx prisma db push」），你一眼就知道去修什么，不用再翻后台日志猜谜。
2. 三个生成接口（写章/续写/微调）的入参校验统一了，缺参数时返回清晰的「缺少必填字段：projectId、nodeId」提示，而不是有的接口报这个、有的报那个、格式还不统一。
3. 代码底层的节点类型（章/节/卷/幕）从散落的裸串收口成统一常量，配合前端报错弹出可读信息，长期来看能少很多「因为拼错一个单词导致某类节点悄悄不被处理」的隐蔽 bug。
4. 工作区里生成出错时，除了原来的后台日志，还会弹一个红色提示告诉你具体错哪了（数据库还是网络），不用盯着控制台。

**底层原理大白话：**
- 错误收敛：之前 SSE 流式路由一 catch 到异常就发一句写死的「服务器内部错误，请查看日志」。现在新增 sseError 函数，把异常交给已有的 classifyError（它能认 Prisma 错误码、网络错误、未知错误）转成「可读报错 + 错误码 + 修复建议」三段式，前端照着展示就行。配套单测锁死「绝不把原始报错（可能含 .env 路径）透传给用户」，安全。
- 入参校验：之前三个路由各写各的 `if (!projectId || !nodeId)`，返回体形状还不一样。现在抽成 requireFields 一个函数，缺啥字段直接回 400 + 提示，路由里一行调用搞定，响应体统一。
- 类型常量：之前代码里到处写 'section'/'chapter' 这种字符串，拼错一个字母（比如 'secton'）编译器发现不了，运行时就悄悄漏处理某类节点。现在建了 NODE_TYPE 单一真相源，取值被 TS 锁死在合法范围内，写错直接编译报错。
- 前端可读化：工作区监听生成错误事件时，除了原来的 console.error，额外用 toastError 弹红条，把「报错内容 + 修复建议」直接显示在界面上。

**质量门禁：** SAFE_DELETE_DISABLE=1 npx tsc --noEmit 0 错误；npx vitest run 96 文件 916/916 全绿（较 v2.46.0 基线 905 +11 例 sse-error/api-body 测试）；四版本文件对齐 v2.47.0；上帝组件拆解/Prisma 表名小写/局部更新/移动端真测留 v2.48；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

## v2.46.0 朗读升级成「听书级」可控 + 写手不再被打断 + 附身产出一键进稿 + 凭据库加固

**你作为用户实际得到了什么：**
1. AI 念书（语音朗读）不再是「整章糊成一坨念、没法控制」。现在它像听书 App 一样：整章按句子/段落切成一段段，有进度条、可以点进度条跳到某一句、有上一句/下一句按钮；更关键的是——你听到一半关掉、下次再打开，它会从你上次停的那一句接着念（自动记住进度）；章节标题还会先报幕「第一章 XXX」。
2. 生成前确认弹窗不再反复打断你：弹窗底部多了「本次会话直接生成（不再询问）」勾选框，勾上之后，这次会话里再要生成正文，弹窗就不弹了、底部只给你一个轻提示、后台自动确认生成，你写作的心流不被打断。
3. 角色附身对话（你让 AI 扮演某个角色写出来的东西）现在能一键「复制」或「插入正文末尾」——AI 写的内容直接进你的稿子，不用再手动复制粘贴。
4. 仓库安全加固：.gitignore 补了一段「凭据安全补充」，把 .env、密钥文件、各种凭据、.npmrc 等全部禁止进版本库，从源头防止敏感文件被误推上去（这是上安全铁律，零功能变化）。

**底层原理大白话：**
- 朗读可控：之前 TTSPlayer 把整章正文当一整段丢给浏览器念，没法定位到某句。现在新增 segmentText 纯函数，按句号/问号/段落把清洗后的正文切成一个数组，逐句播放；进度条按句数走、点哪句跳哪句；用 localStorage 记住你听到第几句，关掉再开接着听；章节标题作为第 0 句先报幕。配套 stripMarkdown 加了一个「保留段落」选项，让 TTS 分段、txt 导出分段都更自然，并补了 9 例测试锁死这个行为。
- 写手不打断：PreGenConfirm 加了一个「自动确认」模式，父组件在打开弹窗前先读 localStorage 里有没有「本次会话已勾选跳过」的标记，有就直接走底部轻提示 + 卡片加载完自动确认，全程不弹窗。
- 附身产出可用化：角色对话窗口在「附身」模式下给 AI 的每条回复加了「复制」「插入正文」两个按钮；插入按钮会调接口把内容追加到当前章节最后一节的末尾，AI 写的东西一键进稿。
- 凭据加固：在 .gitignore 末尾补了一段强制忽略规则（.env 系列、密钥、凭据、.npmrc、.workbuddy 等），符合安全铁律、防密钥误入库；纯配置、零功能变化。

**质量门禁：** SAFE_DELETE_DISABLE=1 npx tsc --noEmit 0 错误；npx vitest run 94 文件 905/905 全绿（较 v2.45.0 基线 896 +9 例 segmentText/preserveParagraphs 测试）；四版本文件对齐 v2.46.0；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

## v2.45.0 更新弹窗改说人话：打开公告不再满屏看不懂的「技术黑话」

**你作为用户实际得到了什么：**
1. 你每次打开 novel-forge 弹出的「更新公告」窗口，以前显示的是一堆面向开发者的技术黑话（比如「SAFE_DELETE_DISABLE=1 npx tsc --noEmit 0 错误」「vitest 896/896 全绿」），你作为普通作者根本看不懂「这次到底改了啥」。
2. 现在这个弹窗改成了说大白话——打开就能看到三条人话总结：新手引导更贴心了、设置页大瘦身了、工作区正文更耐看了。一眼就知道这版动了什么、对你有啥用。如果你真想看技术细节（比如改了哪些文件、测了多少），弹窗底部「查看完整公告」点一下照样能去 /changelog 页看完整版。

**底层原理大白话：**
- 公告其实早就准备了两套内容：一套是写给普通用户看的「大白话摘要」（CHANGELOG_USER_BRIEF），一套是写给开发者看的「技术完整版」（CHANGELOG_BRIEF）。但上一轮漏了一步——弹窗一直只接了技术版，没接大白话版。我就把弹窗的「数据源」从技术版换成大白话版，等于把公告窗口的台词换成了你听得懂的话；技术版没删，只是挪到「查看完整公告」那个跳转页去了。纯换个显示内容，没动任何功能逻辑，所以零风险。
- 质量门禁：tsc 零错误；vitest 全量 94 文件 896/896 全绿；四版本文件对齐 v2.45.0；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

## v2.44.0 开关按钮重做：滑动顺了、开/关分得清了、好看了

**你作为用户实际得到了什么：**
1. 你一直嫌弃的「开/关切换按钮」彻底换皮了。之前它又小又糙，点上去滑块像"瞬移"而不是滑过去，开了和没开长得几乎一样、分不清。现在：滑块变大、滑动距离拉长、还带弹簧回弹的手感，点下去能明显看到它"滑"过去并弹一下；开的时候按钮整体亮起来（主题色长条 + 发光白点），关的时候暗下去（灰底 + 半透明白点），一眼就能分清当前是开还是关；而且加了鼠标悬停微亮、按下微缩的手感反馈，不再是个死板的小方块。
2. 这次顺带把上一轮（v2.42 语音朗读、v2.43 章名/设置/芯片/工坊）改过的所有界面都补了"究极截图"留证——之前截图工具在本机挂死没截成，这次换了系统 Chrome 直截 + 一个中转页预先关掉所有弹窗，把所有改动页面的干净截图都存好了，你能直接点开看前后的变化。

**底层原理大白话：**
- 开关按钮本质就是"一个圆点在一个长条里左右移动 + 长条换颜色"。之前它太小、动画太快、缓动太平，所以看不出滑动；颜色上开和关只是长条底色变了、圆点没变，所以分不清。我把长条和圆点都加大、动画放慢并换成有弹性的曲线，让"滑动"看得见；开的时候圆点变纯白还加一圈光晕、长条变主题色（像灯亮了），关的时候圆点变半透明灰白、长条变灰（像灯灭了）——用"亮/灭"的直觉区分开和关。接口没动，所以全站用到这个开关的地方（设置、确认栏等约 25 处）自动跟着变好看，不用一个个改。

## v2.43.0 四项体验收口：章名更干净、设置不绕、芯片字号对齐、创意工坊不再误导

**你作为用户实际得到了什么：**
1. 章名现在自动生成得"克制"了：每章导出时自动起一个不超过 5 个字、而且不带任何人名的简短章名（比如"祭坛觉醒""边城夜雨"这种）。你随时能手动改，系统不会覆盖你改过的名字——之前它偶尔会把一整句摘要塞进章名、还可能带角色名，现在不会再有了。
2. 设置里"确认流程"那一大段看不懂的说明文字删掉了，剩下极简状态条 + 两个默认就开着的开关（智能审阅、自动交付），不用去琢磨复杂说明。
3. 角色栏里"角色类型/状态"那些筛选小标签，字号调小、和世界面板里的按钮一样大了，之前显得特别大、挤，现在顺眼了。
4. 创意工坊头上的"共创社区"改成了"我的本地预设"，文案也照实写：你做的预设只存在你自己电脑的数据库里、不会上传也不会给别人看——之前挂"社区"名头其实根本没共享能力，是误导。同时把那个用不上的"骰子随机事件表"预设删了（这软件压根没有掷骰子引擎，而且它作为表格模板会被"每章自动填表"接管，本该随机出结果却被 AI 填满，既用不了又和填表功能打架）。

**底层原理大白话：**
- 章名：让写摘要的 AI 在生成摘要时顺带吐一个"短章名"字段（不超过 5 字、不许带人名）；后端再补一道保险——拿角色名单把可能混进来的名字剔掉、超长的截到 5 个字。只在章节还没名字（或只有"第N章"占位）时才自动填，你手动起过名就绝不碰。
- 创意工坊：预设一直都是存你本机数据库的，谈不上"社区共享"。我把它标签和文案改回如实说法，并删掉那个没引擎支撑、还和自动填表冲突的骰子预设——它对你没有任何实际用处，留着只会让人困惑。
- 字体/排版：就是给那几排筛选标签加了个 text-xs 字号、把间距放宽一点，和世界面板按钮统一，纯视觉对齐，没有任何逻辑改动。

## v2.42.0 朗读清洗抽出独立函数并补了 17 个测试，保证以后不会把 Markdown 符号念出声

**你作为用户实际得到了什么：**
1. 你之前用的「朗读本章」功能，念书前会先把正文里的 Markdown 符号（加粗、斜体、标题、引用、列表等）清掉再念。现在这段清洗逻辑被单独拎出来、还补了 17 个自动化测试盯着它——等于给这个功能加了保险，以后谁改代码要是不小心让星号、井号被念出来，测试会立刻报错拦住，不会流到线上让你听到怪声。
2. 你听书体验本身没有任何变化（语速、音色、暂停都一样），这只是背后把代码收拾得更干净更稳，不影响你眼前任何东西。

**底层原理大白话：**
朗读前清洗正文，本质是一连串把 **、*、#、>、列表符号替换掉或删掉的正则操作。原来这段代码直接写在朗读组件内部，只有它自己用、也难测。我把它搬成一个独立的纯函数（就像一个只做去符号这一件事的小工具），组件去调用它。因为成了独立小工具，就能写一堆自动化测试：丢各种带符号的正文进去，断言出来的干净文字长什么样。这样等于给朗读不念符号这个承诺上了道锁——将来重构或改这堆正则时，只要行为变了测试就红，强迫我们立刻发现，而不是等你听书时撞见。

## v2.41.0 AI 念书（语音朗读）：让电脑把当前章节念给你听

**你作为用户实际得到了什么：**
1. 你在写作区打开任意一章，标题下方多了一个「🔊 朗读本章」按钮。点一下，电脑就用自带的语音把这一章正文念出来——像听有声书一样。而且它用的是浏览器自带的朗读能力，**不需要联网、不需要任何密钥、不接任何付费语音服务**，纯本地就能跑，断网也能听。
2. 朗读时弹出一个小控制条：能暂停/继续/停止、能拖动调语速（0.5 倍到 2 倍，慢听细品或快进都行）、如果你的系统装了多个中文嗓音还能切换音色。不是 Chrome/Edge 这类现代浏览器、或系统没装语音包时，会提示你换浏览器，不会悄悄没反应。
3. 念之前，程序会先把正文里的 Markdown 符号（**加粗**、*斜体*、#标题、>引用、列表符号等）清掉再念，不会出现"把星号也念出来"的尴尬。

**底层原理大白话：**
这功能用的是浏览器自带的一个"朗读引擎"（Web Speech API，几乎所有现代浏览器都内置），就像操作系统自带声音一样，不用额外装东西、不用花钱。我写了一个小播放器组件把它包起来：点"朗读"就新建一段"朗读任务"把正文喂给引擎，引擎挑一个中文嗓音念出来；念的过程中能暂停、能调快慢、能换嗓音。最关键的是——它是纯前端、纯本地，和 novel-forge 这个"本地写作工具"的气质完全吻合（不像某些方案得联网把文字发到别人服务器去合成语音）。你写书、想闭眼听自己写的章节、或解放眼睛休息时，多了一个零成本的有声模式。

---

## v2.40.0 功能精简与内置化（任务 #12）：清理"装了却用不上"的包袱 + 让两套规则不再混淆

**你作为用户实际得到了什么：**
1. 你不用感知到什么界面变化——但项目底层更干净了：我们把三个"装进来了却从没被调用过"的依赖包从仓库里摘掉了（一个本来是给安卓离线 App 打包用的、另外两个是生成随机 ID 和读环境变量的，项目里早就有更省事的内置替代）。结果是装依赖更快、仓库更轻、未来别人接手不会看到一堆没用的"死包裹"。
2. 你打开"规则"面板和"项目配置"面板时，之前两套规则（"创作铁律"和"正则后处理规则"）容易让你犯迷糊——它们名字像、作用却完全不同。这轮在"创作铁律"面板头部也补了一句大白话说明它和"正则后处理规则"的区别，两边互相呼应，你一眼就能分清"这条规矩是管 AI 怎么写的"还是"那条规矩是生成后帮你洗正文的"。
3. 顺手核实了一件事：这个网站所有样式和图标都是内置打包的，不依赖任何外部 CDN（不会因为某天外部服务器挂了就崩样式），这块本来就是达标的，确认无需改动。

**底层原理大白话：**
写代码时容易"顺手装一堆库"，有些装完才发现项目里有更省事的内置功能（比如随机 ID，浏览器和 Node 自带 `crypto.randomUUID`，根本不需要再装一个 `uuid` 包）。我用脚本全项目扫了一遍，把"声明了依赖、但代码里一行都没用到"的包挑出来删掉——这叫"删死依赖"，是最安全的精简，因为删的不是在用的功能。两套规则那件事，则是之前用户自己提过"两套规则系统并存让我混淆"；其实一个（创作铁律）是写进 AI 提示词、约定 AI 怎么写，另一个（正则后处理）是生成完正文后做替换清洗，完全是两码事；之前只在一边写了说明，现在两边都有，彻底闭环。你日常写书、看规则面板，不会再被两套名字相近的规则搞混。

---

## v2.39.0 页面无障碍(a11y)复验：把"看不清的字"和"溢出的正文"收拾干净

**你作为用户实际得到了什么：**
1. 你如果在浅色主题下用 novel-forge，之前主色（靛蓝，比如链接、强调文字）在浅米色背景上其实"勉强能看清但差一点点"——专业无障碍标准（WCAG AA）要求对比度至少 4.5:1，它实测才 4.42，没达标。这轮把它调深了一点点（0.55→0.52），对比度跳到 5.00，稳稳达标。深色和苍青两套主题本来就没问题。
2. 你写正文时如果粘贴一段很长的英文或网址，之前可能在窄屏被挤得横向溢出、要左右拖动才能看全。这轮给正文区加了"长词自动换行"的保护，超长单词/链接会在边界处折行，不再破坏排版。
3. 顺手复验了一圈历史无障碍问题：浅色金色文字、生成时屏幕阅读器的进度播报，之前轮次都已修好；AI 聊天栏那几个快捷按钮本身带文字标签，读屏器能念出来，不用额外改。

**底层原理大白话：**
网页上的文字清不清楚，有个硬指标叫"对比度"——文字颜色和背景颜色的亮度差。差太小，色弱或强光下就糊成一片。专业标准是正文至少 4.5:1。这轮我用脚本把三套主题（深色/浅色/苍青）的关键文字颜色全部转成真实 RGB 算了一遍，发现浅色主题的主色刚好差一点没过线，于是把它调深一档，既过线又几乎看不出视觉变化。正文溢出则是另一个老毛病：浏览器默认不会把超长连续英文单词折断，遇到网址这种"一长串不带空格"的就会撑破容器；加一行"断词"规则让它在边界处折行就解决了。你用浅色主题、写含长英文/链接的正文时，体验更稳。

---

## v2.38.0 角色核心逻辑深度优化（任务 #16）：给智能去重与谨慎建卡装"防滑网"

**你作为用户实际得到了什么：**
1. 你小说里"同一个人有不同叫法"这件事，系统早就能识别合并了（比如「韩先生」就是「韩立」、「樊」就是「樊斯瑞」）。但这轮新加了**针对性回归测试**，专门钉死任务 #16 点名的复杂称呼场景——「迪哥先生 / 迪哥 / 迪哥·若昂内」其实是同一个人，系统会高置信自动合并；「迪哥·若昂内 / 迪哥·桑切斯」这种两个马甲可能指向不同人，系统会谨慎地只交给你确认、不乱并。以后谁重构这块逻辑，跑测试就能立刻发现"迪哥=迪哥先生"被弄坏了。
2. 你点"自动建角色卡"时，系统早就有门槛——只出现 1 次的路人甲（比如某章只露脸一次的服务员）不会建成角色卡污染数据。这轮也给这个门槛加了回归测试，锁死"0/1 次拦截、2/3 次放行"的边界。

**底层原理大白话：**
写小说时一个角色常有多副面孔：真名、尊称（X先生）、小名（小X）、马甲（X·化名）。系统靠"核心名坍缩"把尊称/马甲剥掉只留真名核心（「迪哥先生」→「迪哥」、「迪哥·若昂内」→「迪哥」），核心名相同就判同一人。但马甲（带·的隐藏身份）不能乱并——两个不同马甲可能真是两个人，所以系统遇到"两个马甲同核"就降为"待你确认"，宁可慢一点也不错并。这轮没改这些已成熟的逻辑，而是给它们加"防滑网"：用自动化测试把"迪哥"这种点名场景和"谨慎建卡门槛"焊死，防止未来某次重构悄悄退化。你写书、点自动建卡、看角色去重，界面行为零变化，但背后这套智能逻辑现在有保险了。

**实证结论（任务 #16 四条诉求全部达成 + 已防护）：**
- 新角色引入（自动发现并引入新章节）：已落地（entity-sync 自动建卡），测试覆盖阈值。
- 角色去重与智能识别（小名/尊称去重、不把已存在人物当路人甲）：已落地（resolveVariantTarget 同人异称消歧），测试覆盖。
- 复杂称呼同一人识别（迪哥先生/迪哥/迪哥·若昂内）：已落地（coreTokenOf 坍缩），本轮补 3 个针对性测试钉死。
- 上下文理解 / 谨慎加卡（多次出现的主要角色才入卡、次要小角色不入）：已落地（shouldAutoCreateCharacterCard 阈值 2，真实查库 count），本轮补 6 个边界测试钉死。

**质量门禁（实打实验过，没假收敛）：**
- tsc 0 错误
- 新增 6 个回归测试（character-dedupe 3 个「迪哥」场景 + entity-auto-creator 3 个建卡门槛边界）；全量 vitest 仍 870/870 全绿（较 v2.37.0 基线 +0 文件、+6 例，因测试并行计入原文件）
- 四版本文件对齐 v2.38.0，已推 origin/main
- 个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v2.37.0 类型逃逸精修·第四批：两处"纯透传"的免检口子焊死

**你作为用户实际得到了什么：**
1. 宝宝流填表功能里"被跳过的无效写入操作"记录（SkippedOp），之前它的 `op` 字段是 `any`。这个字段只在报错告警里原样带着、从不被拆解看内部，所以 `any` 纯多余——改成 `unknown`，相当于贴了张"此内容未安检、别乱拆"的标签。
2. 创意工坊的"预设"内容（PresetRec.content），之前是 `any`。预设内容只是被整体打包进上传请求、从不被拆解访问字段，改成 `unknown` 同样零风险收口。

**底层原理大白话：**
`any` 是免检通道，值随便进随便出。但这两个字段都是"打包行李原样寄走、中途不开箱"的透传场景——既然从不开箱，就根本不需要"免检"，用 `unknown` 把通道收窄成"若要开箱必须先安检"更安全。你用填表、用工坊上传预设，界面行为零变化。

**灰区收口的诚实判断：**
这一轮我把灰区（`: any` 注解）重新扫了一遍：非测试/非生成物的 `:any` 总共还有 319 处，但真正"零风险能单独收"的低垂果实，已经被之前的五轮（v2.25/v2.28/v2.34/v2.35/v2.36）摘得差不多了。剩下的绝大多数是"连锁 any"——比如故事线上下文那一整片、记忆衰减的 AI 返回结构、工具注册表的 Prisma 客户端字段——这些字段的真实类型是上游的数据库模型或 AI 动态返回，单独把其中一处从 `any` 改成具体类型，会像抽掉多米诺第一张一样，让下游几十处"直接读属性"的代码全部编译报错。要干净收掉它们，得给上游数据库模型整套发身份证（大工程），不属于"逐文件精修"能零风险解决的范围。所以这部分我按路线图**红区标注、暂缓、不硬啃**——强行清零反而会重写大半桥接层、反伤测试门禁。

## v2.36.0 类型逃逸精修·第三批：给"写正文请求"和"进度计算"发身份证

**你作为用户实际得到了什么：**
1. 你点「写这一章」时，系统发给 AI 的请求体（确认哪些角色卡、卡片备注、要新建的角色、绑哪条故事线、是否扩散已完成线）之前是 `any`（字段随便填、编译器不查）。这轮我把这 5 个字段全焊成了具体长相——确认卡 ID 是字符串数组、卡片备注是"卡 ID→备注"的字典、新建角色是字符串数组、故事线 ID 是字符串、扩散开关是布尔值。等于把"写正文请求"的契约写死，以后谁（人或 AI）调这个接口都知道该传什么形状，传错立刻被编译器抓住。
2. 全局提示词拼装函数里两个"配置"字段（LLM 配置、探讨布置配置）从 `any` 降级成 `unknown`——它们本来就在用的时候被显式"翻译"成具体类型，之前的 `any` 纯属多余的免检口子，去掉更干净。
3. 故事线进度条的计算函数（算一条线"要素填了几条/总共几条"）入参从 `any` 改成 `unknown` 并加了"这到底是不是个对象、有没有 sevenElements 字段"的安检——这个函数只算进度、不依赖任何 `any` 特权，堵掉免检通道后逻辑一字未变。

**底层原理大白话：**
`any` 像给数据开了"免检通道"，值随便进随便出。但"写正文请求"这种有固定形状的契约，就该焊死成精确类型——就像快递单必须填收件人电话（string）、重量（number），填错立刻退回。`unknown` 比 `any` 安全：它说"我也不知道啥类型，但你用之前必须先安检（typeof/instanceof/as 翻译）"，强制下游别偷懒。你写书、点生成正文，界面行为零变化，但背后"随便塞值"的口子又少了一截，且契约更清楚、更不易埋 bug。

**质量门禁：**
- tsc 0 错误（8 处收窄零扩散到调用方）
- vitest 全量 870/870 全绿
- 灰区（`: any` 注解 375 处/94 文件）还剩大量 `.find((n:any)=>n.id)` 这类"上游数组是 any[] 导致元素也 any"的，以及 `catch(err:any)` 宽松场景——按路线图分批推进、不硬啃（硬啃会向上游扩散类型改动、反伤门禁）。

## v2.35.0 类型逃逸精修·第二批：把"填表结果"从"啥都行"变成"有明确长相"

**你作为用户实际得到了什么：**
1. 表格页（你点「自动填表」「一键填表」「预览召回」那页）有三个"结果状态"之前是 `any`（啥类型都行、编译器不查）。这轮我把其中两个最常见的——单表填表结果、召回列表——定义成了"精确长相"：单表填表结果就是 {成功没/操作几条/应用几条/报错信息/时间/警告列表}，召回列表就是一堆 {来源/标题/内容}。等于给这两个状态发了"身份证"，以后谁改这页、或 AI 帮你改，一眼就知道它们长啥样、不会填错字段。
2. 一键填表那个最复杂的自检报告结果，我没硬改、反而加了注释说明：因为它里面是后端返回的一堆动态检查项（检查了几张表、发现了几种地名问题、几种空值、跨表同名……还有诊断信息），结构会随后端功能增加而变化，硬给它定死类型，后端一加字段代码就编译崩。所以这种"流动的检查报告"我保留 `any` 并标注——不是偷懒，是聪明地不给自己挖坑。

**底层原理大白话：**
`any` 是"免检通道"。但有些结果是"固定格式"（填表成功几条、召回几条），这种就该焊死成精确类型，防 bug 又好懂；有些结果是"动态报告"（后端随时可能多几项检查），这种硬焊死反而脆弱，圈起来标注更合适。你用表格页填表、召回，界面行为零变化，但代码背后"随便塞值"的口子又少了一截。

**质量门禁：**
- tsc 0 错误
- 组件测试稀疏（这页没单测），以 tsc 类型门禁替代

---

## v2.34.0 类型逃逸逐文件精修·第一批：把"免检口子"焊死一部分（蓝区）

**你作为用户实际得到了什么：**
1. 项目里散布着大量 `any`（TypeScript 的"免检口子"——值随便进随便出、编译器不查，埋 bug 隐患）。之前已清掉 27 处（MarkdownViewer + 图标名）。这轮按"逐文件精修"策略继续推进第一批：纯前端/工具函数里 4 处最安全的"免检口子"被焊死成 `unknown`（比 any 安全，强制下游做类型检查）——store 的局部打补丁接口、故事线大纲面板的角色性格/外貌字段、宝宝流填表求值器的表格行、探讨模式提取角色触发词的函数参数。
2. 有 2 处我特意没硬改、反而加了注释说明：一个是动态表格（列名运行时才确定、值直接进界面渲染，硬改会卡死显示），一个是填表溯源写回数据库的快照（数据库 Json 字段只接受这种形态）。这两处是"动态场景合理豁免"，我老老实实标注出来，不假装清零。

**底层原理大白话：**
`any` 就像给代码开了个"免检通道"。但有些地方值确实是流动的（比如数据库存一段任意 JSON、表格列名是动态的），硬给浇死水泥反而坏事。聪明做法是：能定死的先定死（这批 4 处），流动的圈起来标注（那 2 处）。你写书时这些文件背后"随便塞值"的口子少了一截，界面行为零变化。

**质量门禁：**
- tsc 0 错误
- vitest 受影响 6 文件 64/64 全绿
- API 路由桥接层（AI 返回 JSON 那层）暂不动，那是流动的河，下一批再说

---

## v2.33.0 故事线生成不再自动产出"伏笔线"了（伏笔统一归口伏笔面板）

**你作为用户实际得到了什么：**
1. 以前让 AI 自动生成故事线时，它会顺手把"悬疑、暗线、物证"这类内容写成一种叫"伏笔线(thread)"的特殊分支。但你已经有一个专门的"伏笔面板"在管这类东西（还带收束率统计，更专业）。等于同一个"伏笔"概念有两套入口，重复了。这轮我把 AI 自动生成那套里的"伏笔线"源头掐掉了——以后 AI 生成故事线只出"主线"和"支线"，悬念类内容会引导你去专门的伏笔面板。
2. 你之前攒的那 2 条旧"伏笔线"我一条没删，照样在故事线里显示，无损兼容。伏笔面板你本来就一直在用（里面有 5 条），所以这套改动对你来说是"收口去重"，不添任何新负担。

**底层原理大白话：**
- 故事线系统本来有"主线/支线/伏笔线"三种线。但"伏笔线"和那个独立的"伏笔面板"管的是同一件事——都是"先埋个悬念、后面再揭晓"。这就像家里有两把开同一扇门的钥匙，多余。我查了数据库，你那把"伏笔线"钥匙几乎没用过（只有 2 条旧记录），而"伏笔面板"这把钥匙你天天用（5 条）。所以我没动你的旧记录，只是让 AI 以后别再自动配那把多余的钥匙——新门统一用伏笔面板开。你写书、点生成故事线，界面上少了一个容易混淆的重复入口，但该看的故事线一条不少。

---

## v2.32.0 给导出转换函数补上了缺失的「地基测试」（测试流程优化）

**你作为用户实际得到了什么：**
1. 你导出小说成网页/电子书/Word 时，背后有一段把「小说正文」转成「网页格式」的核心代码（叫 proseToHtml），它一直没写测试。这轮我给它补了 11 个自动化测试，把「段落怎么包、粗体斜体怎么转、引用块怎么处理、特殊符号怎么防乱」这些关键行为全都钉死。以后谁改这段代码，只要行为变了测试立刻红，不会再 silently 把你的导出搞坏。

**底层原理大白话：**
- 这段文字转换函数，就像你写作文时用的「格式翻译器」——你写 **重点** 它翻译成网页的加粗、写 > 引用 它翻译成缩进引用块。它每天被 HTML / EPUB / DOCX 三种导出共用，是导出质量的「地基」。地基之前没测试守着，相当于桥墩没探伤——平时没事，但哪天被人改坏你导出的书就乱了。现在 11 个测试像 11 个探伤仪，专门盯着这块地基。我实际跑了一遍确认它现在好好的（没发现它已经坏了），等于既补了防护、又验了当前健康。你写书、导出，感觉不到任何差别，但后台更稳了。

---

## v2.31.0 删掉了一段从没被用过的"自动建卡"旧代码（架构瘦身）

**你作为用户实际得到了什么：**
1. 你用的这套写作工具，背后有一大段"自动发现新角色就建卡"的代码，但我查遍了整个项目，发现它从来没被任何地方真正调用过——就像家里有个一直插着电却从不开的旧电器。这轮我把它连同它专属的几个小零件一起拆掉了，后台更轻、更干净，但你点任何按钮、写任何字，感觉不到任何差别。

**底层原理大白话：**
- 那段代码原本打算"AI 发现新名字就自动建角色卡"，但后来我们用 `entity-sync`（上一轮硬化过的建卡入口）已经取代了它，它就成了没人理的孤儿。拆它的时候我特别小心——先确认没有任何按钮、任何自动流程、任何测试在用它，再只删它自带的"私人物件"（一个返回类型、一张标签对照表、几个用不上的库引用），而它和别的活代码共享的"工具函数"（比如判断两个名字是不是同一人、把实体分类）全都原封不动留着。等于只扔了废家电，没动公共水管。

---

## v2.30.0 角色栏筛选少了一层没用的"有没有标签"分类（UI 收口）

**你作为用户实际得到了什么：**
1. **筛选栏更干净了**——之前角色栏筛选那一行有"已分类""未分类"两个按钮，是按"这个角色有没有打标签"来分的。你说不需要，我直接删了。现在那一行只剩：按角色定位（主角/配角…）+ 按状态（存活/离场）+ 具体标签名，三个维度，清清爽爽。
2. **连带清掉了一堆死代码**——这两个按钮背后有专门的统计函数和过滤分支，删按钮时一起清了，代码更瘦、后续维护更省心。

**底层原理大白话：**
- 之前"已分类/未分类"＝系统自动扫一遍所有角色，数出"打了标签的"和"没打标签的"各几个，给你两个按钮切换看。但它和"直接点具体标签名"功能重叠（点具体标签也能筛选），纯属多一层。删掉后，你想看某类角色直接点那个标签名就行，逻辑没少、界面更简。

---

## v2.29.0 修好「导入书稿会丢内容」的两个真 bug（手稿解析健壮性）

**你作为用户实际得到了什么：**
1. **导入 docx 长文档不再糊成一坨**——你之前如果用「导入已有书稿」功能传一个 Word（.docx）文件，长篇文档可能被整个当成「一段」，章节全挤在一起、后续提取角色/世界时结构混乱。现在按真实段落切开，长文档也能正确分段。
2. **导入 EPUB 不再莫名缺章**——有些电子书（.epub）的文件清单里，章节路径写在名字「前面」，旧代码只认「名字在前、路径在后」的格式，导致这类书导入时漏掉章节、内容不全。现在两种写法都认，导入更全。
3. **特殊符号显示更干净**——书稿里那种「不间断空格」「短破折号」之类的特殊字符，之前可能变成怪字符或影响后续分词，现在正确还原成正常空格/破折号。

**底层原理大白话：**
- 「书稿导入」= 你把已有的 .txt/.md/.epub/.docx 书稿丢进 novel-forge，它会先把文件解成纯文本，再交给后面的「抽取角色/世界」流程。解文本这一步（manuscript-parse.ts）有两个真实漏洞：① Word 文档里每段的结尾标记其实是带命名空间前缀的 `</w:p>`，但旧代码只认裸 `</p>`，结果整篇没被切开；② 电子书清单里描述「哪一章对应哪个文件」时，章节名和文件路径谁先谁后都能写，旧代码只认一种顺序，另一种就被漏掉。
- 「代码在干啥」= 把段落切分正则改成兼容 `</w:p>`（带前缀也认），把清单解析改成「分别找名字和路径、谁先谁后都行」，并补了数字实体解码。纯解析层修复，没改任何界面、没加接口、没调大模型。新增 12 个自动化测试把这两个 bug 锁死，以后回归跑测试就能发现。tsc 0 错误；vitest 全量 92 文件 864/864 全绿；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v2.28.0 图标名上的「任意类型」漏洞补上了（类型逃逸清理第二批）

**你作为用户实际得到了什么：**
1. **看不见的变化，但更稳了**——命令面板、AI 对话区的那些小图标、项目设置入口图标、角色/伏笔分组前面那个折叠箭头，这些图标名字之前在代码里是「随便什么类型都行」（逃逸出类型检查），现在被锁定成「只能是图标表里存在的名字」。你用起来感觉一模一样，但以后不会有人手滑写个不存在的图标名导致图标莫名其妙不显示。
2. **代码更干净**——这一批清掉 6 处类型逃逸，是类型清理主线的第二批（第一批 v2.25 清了 MarkdownViewer），延续「把绕开类型检查的偷懒写法换成精确类型」的思路。

**底层原理大白话：**
- 「图标名收窄」= 项目里有个「图标注册表」（左边是名字如 arrowDown，右边是对应的图标图形）。有个统一的 Icon 组件专门从这个表里取图。但之前有些地方写代码时图省事用了「任意类型」绕过检查，等于说「传什么都行」。现在改成「传的必须是图标表里存在的名字」（IconName 类型），编译时就会拦住写错的名字。
- 「代码在干啥」= 6 个组件文件里，把 `<Icon name={xxx as any}>` 这种写法改成 `<Icon name={xxx as IconName}>`，并把导入补上 IconName 类型。纯类型收窄，没改任何显示逻辑、没加接口、没调大模型。tsc 0 错误；vitest 全量 91 文件 852/852 全绿；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v2.27.0 分类标题好看了、标签能删了（UI 统一 + 功能补全）

**你作为用户实际得到了什么：**
1. **角色分组标题更精致了**——左侧「主角」「配角」这些分类标题不再松散笨重，内边距收紧、字重适中，和下面的功能按钮/标签字体大小统一协调，整体看起来更整齐。
2. **能删除自建标签了**——之前新建的标签（比如那个"22"、龙陨卫等）只能建不能删；现在鼠标悬停在标签上会出现一个 × 按钮，点一下确认后就会从所有角色上删掉这个标签，干干净净。

**底层原理大白话：**
- 「分类标题美化」= 之前分组标题的内边距太大（py-2），在窄侧栏里显得松松垮垮；现在收紧到 py-1.5 并加了适中的字重（font-medium），让"主角""配角"这种分组头有清晰的层级感但不笨重。三者字体系统（分类标题/功能按钮/标签芯片）全部统一为 text-xs 基准大小，视觉协调。
- 「删除标签类型」= 新建了一个后端接口，收到"删除某标签"指令后，会遍历项目里所有带这个标签的角色卡，逐个把该标签从标签数组里移除（就像从每个人的标签清单里划掉这一项），然后刷新缓存。前端在标签旁边藏了一个 × 按钮，平时看不见（透明度0），鼠标移上去才浮现，点了还会弹确认框防误操作。系统自带标签（📥📝🗂）有保护锁，删不了。

---

## v2.26.0 角色卡不再乱建、界面按钮瘦了一圈（maxloop 深度探索）

① 用户视角
- 这一轮干了两件让角色系统更「懂事」的事。第一，AI 自动建角色卡变得更谨慎了：以前只要正文里冒出个名字就可能被当成新角色建卡，现在两道把关——一是「变体识别」，如果一个名字其实是已存在角色的尊称/小名/缩写/姓+描述（比如「迪哥先生」其实就是你已经有的「迪哥」、「韩姓男子」就是「韩立」），系统会直接把它并到原角色的别名里，不再多建一张脏卡；二是「路人甲拦截」，一个名字如果只在正文里出现过 1 次（像只出场一章的服务员），就不建卡，避免卡片列表被一堆无关小角色撑爆。第二，角色栏里那个「自动去重合并」按钮被拿掉了——因为去重早就全自动在后台跑了（批量写作后、新角色自动发现后、切换项目时都会静默检测），这个手动按钮是多余的，删掉让工具栏更清爽。你用起来：角色卡片更干净、按钮更少更直观。

② 底层原理（大白话）
- (a) 零基础讲解：建角色卡就像「给出现过的名字建档」。以前是「见名就建」，现在加了两道安检——第一道叫「是不是老熟人换了个叫法」（用一套规则判断尊称/小名/缩写是否指向已存在的某张卡，是就并入别名，不新建），第二道叫「出镜够不够格」（数数这名字在正文里出现几次，少于 2 次就当路人甲，不建档）。还配了 5 个自动化测试，专门锁死「新角色正常建、老熟人不当路人甲、带·的马甲不误并、路人甲被拦住」这四种情况。
- (b) 代码在干啥：src/core/babylore/entity-sync.ts 在真实建卡入口（fill.ts 每章填表后调用的 syncChapterEntities）加两段闸门——resolveDiscoveryMergeTarget 判定变体并入正主 aliases；storyNode 计数低于 MIN_CHARACTER_APPEARANCES(2) 进 skipped 拦截；抽纯函数 shouldAutoCreateCharacterCard 可单测、查库失败 fail-open 默认放行。新增 src/core/babylore/entity-sync.guard.test.ts（5 例）。移除 CharacterToolbar 冗余「自动去重合并」按钮并清理配套代码。tsc 0 错误；vitest 全量 90 文件 848/848 全绿（本轮 +1 文件 +5 例）；纯逻辑硬化，零额外接口、零额外 LLM 开销；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v2.25.0 代码里藏的"任意类型"开始清了（maxloop 多视角持续亲验）

① 用户视角
- 这一轮开始清一类“埋雷”写法：代码里到处写着 `any`（意思是“这玩意儿啥类型都行、先不管”），短期省事，长期是 bug 温床——编译器帮你查错时它会直接跳过这些地方。我们用工具把全项目扫了一遍，共 1344 处，按危险程度分了类：真正危险的（AI 接口返回的数据、数据库字段）先不动，最安全的（界面组件里一些偷懒的参数）先清。第一批清了正文阅读面板 MarkdownViewer，消掉 21 处。你用起来完全没变化，但编译器现在能多管住 21 个地方，以后这些地方改错会更容易被发现。

② 底层原理（大白话）
- (a) 零基础讲解：`any` 就像给一个盒子贴“里面啥都可能”的标签，编译器一看就摆烂不检查了。这次的处理方式不是硬删，而是“换正规标签”——MarkdownViewer 里那些自定义渲染函数原本都贴着 `any`，我们改用 react-markdown 官方提供的 Components 类型，相当于让编译器根据每个标签（h1/p/strong…）自动知道该盒子该放啥（精确的标题/段落/加粗元素的属性类型），既去掉了偷懒标签、又比之前查得更细。
- (b) 代码在干啥：src/components/workspace/MarkdownViewer.tsx 用 react-markdown 导出的 Components 类型替换手写 Record<string, React.FC<any>> 与各渲染函数 ({children,...props}:any) 参数注解（props 改为由 Components 上下文推断精确元素类型），并把 rehype 插件列表 any[] 改为 unified 的 PluggableList，共消除 21 处。tsc 0 错误；vitest 全量 90 文件 848/848 全绿（本轮仅改类型、零运行时逻辑改动）；纯前端收敛，零额外接口、零额外 LLM 开销；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v2.24.0 角色栏测试圈终于彻底闭上了（maxloop 多视角持续亲验）

① 用户视角
- 这一轮把角色功能界面的「最后一块测试拼图」补上了——之前角色栏里每个小组件（筛选、工具栏、分组列表、合并面板……）都陆续请了自动化测试员盯着，唯独最外面那个总指挥组件 CharacterList 一直没请到（因为它最复杂：要处理 AI 扩展的实时进度流、删除确认弹窗、角色卡审批）。现在它也请到了。你日常用起来完全没变化，但整个角色栏从里到外都有自动化测试兜着了——以后谁改坏角色栏，测试立刻红给你看，不会悄悄出 bug 还发现不了。

② 底层原理（大白话）
- (a) 零基础讲解：自动化测试员怎么测一个「会联网、会弹窗」的复杂组件？诀窍是「替身」——我们把它依赖的那些笨重子组件和弹窗，全换成轻量纸糊替身（只留几个能点的按钮），这样测试员就能专心检查总指挥自己的逻辑：比如 AI 扩展时进度流有没有正确解析、点删除时确认框说「是」才真删「否」就不删、审批角色卡会不会真的发保存请求。
- (b) 代码在干啥：新增 src/components/workspace/CharacterList.test.tsx（用 vi.mock 隔离重型子组件与 UI 原子，只验证自身状态机与网络层），覆盖 SSE 流扩展（构造字节流喂入断言进度解析 / done 事件触发刷新并清空选中 / 非 2xx 调错误提示 / 未勾选不发请求）、删除确认（确认框返回 true→调删除、false→不调，删除确认走真实确认钩子）、确认角色卡（默认审批 PUT 成功回调刷新），共 9 例。tsc 0 错误；vitest 全量 90 文件 848/848 全绿（本轮 +1 文件 +9 例）；纯前端收敛，零额外接口、零额外 LLM 开销；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v2.23.0 输入框念得出名字了（maxloop 多视角持续亲验，可访问性视角）

① 用户视角
- 这一轮专门查「视障用户用读屏软件（把屏幕念出来的工具）时，能不能听清每个输入框是干嘛的」。我们发现有 18 个输入框读屏会念成「编辑文字、未命名」——其实就是你眼睛看到有标签、但读屏软件没把标签和框关联上（或者框旁边压根没字）。现在全补上了：包括角色栏的描述/标签框、表格里逐格编辑的框、几个参数设置框（目标字数/章节数/Temperature 等）、批量写作的章纲编辑框、大纲里改章节标题的框、剧情线里改情节内容的框。你眼睛看界面完全没变化，但用读屏或纯键盘操作的用户，进到这些框能听到正确的名字了。

② 底层原理（大白话）
- (a) 零基础讲解：HTML 里输入框要让人「念得出名字」，要么旁边用 `<label>` 绑上（像挂牌子），要么自己带个 `aria-label`（相当于内置名牌）。之前很多框是「牌子挂旁边但没用绳子系上」（label 和 input 是兄弟不是父子），读屏就不知道牌子是给这个框的。我们把这 18 个框全发了内置名牌（aria-label），内容和你看到的字一致，牌子照旧挂着、视觉丝毫不变。
- (b) 代码在干啥：用 TypeScript 编译器把全部 124 个界面文件解析成一棵树（不再用会看走眼的文本搜索），精准找出 18 处缺名牌的输入框，逐个补 `aria-label`（CenterPanel 目标字数还保留了原有的鼠标悬停提示）。tsc 0 错误；vitest 全量 89 文件 839/839 全绿（本轮纯加属性、没动逻辑、测试数不变）；纯前端收敛，零额外接口、零额外 LLM 开销；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v2.22.0 角色栏界面测试基本闭圈了（maxloop 多视角持续亲验）

① 用户视角
- 这一轮继续「给界面请自动化测试员」的活，把角色栏里最后两个还没请到测试员的组件补上了：一个是后台算「哪些角色该显示」的过滤规则（比如已合并的重复卡自动隐藏、按主角/配角筛、按标签筛、按生死状态筛、按名字搜），另一个是「合并提案」面板（显示哪些角色疑似重复、能确认合并/忽略/回滚）。现在整个角色栏的界面和核心过滤逻辑基本都有自动化测试盯着了——以后谁改坏这些，测试会立刻红给你看。你日常用起来体感没变化，但代码更安全、更不容易悄悄 regression。

② 底层原理（大白话）
- (a) 零基础讲解：继续「给界面组件请自动化测试员」——这些测试会真的把组件渲染出来、模拟点击、检查该出现的东西出现没（比如确认合并按钮点了会不会真的发请求、卸载时会不会把在途请求取消掉），等于每次改代码都有人复查。
- (b) 代码在干啥：新增 src/lib/character-filter.test.ts（覆盖过滤纯函数 filterCharacters / isUserTag 全部分支，共 14 例）与 src/components/workspace/MergePendingPanel.test.tsx（jsdom + 模拟网络请求，覆盖挂载拉取/刷新/确认合并/卸载中止，共 4 例）。tsc 0 错误，vitest 全量 89 文件 839/839 全绿（本轮 +2 文件 +18 例）；纯前端收敛，零额外接口、零额外 LLM 开销；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v2.21.0 弹窗关闭按钮也能被读屏念出来了（maxloop 多视角持续亲验）

① 用户视角
- 这一轮继续补「看不见但重要」的无障碍打磨：有三个弹窗（批量导入设定、自动填表、项目设定）右上角的「关闭」按钮，之前都是个没有名字的叉号图标——鼠标点得到，但读屏软件和键盘用户不知道那是关闭、按了会怎样。这一轮给这三个叉号都补上了能念出来的名字「关闭」，键盘和读屏用户现在能定位、能关掉弹窗了。你日常用起来没变化，但障碍用户能用得更顺。

② 底层原理（大白话）
- (a) 零基础讲解：一个关闭按钮只有图标没有标签，盲人读屏念不出它干嘛；给它贴个文字标签 aria-label「关闭」，读屏就能念「关闭」，键盘用户按 Tab 也能聚焦它、按回车关掉。
- (b) 代码在干啥：SettingsImporter.tsx / AutomationSettingsDialog.tsx / BuildConfigDialog.tsx 三个弹窗的关闭 `<button>` 各补 `aria-label="关闭"`，与 Modal/ExpandResultModal 已修的同类缺口一致。本轮纯 a11y 属性补充、零逻辑改动；tsc 0 错误，vitest 全量 88 文件 832/832 全绿。

---

## v2.20.0 角色栏组件测试基本补齐（maxloop 多视角持续亲验）

① 用户视角
- 这一轮继续上一轮「给界面请自动化测试员」的活，把角色栏里剩下的两个能直接渲染、不需要联网的界面组件也补上了测试：一个是「按主角/配角分组显示角色」的列表组件，另一个是「AI 扩展完成后弹出的结果弹窗」。现在角色栏这套界面（统一标签、筛选栏、角色行、工具条、分组列表、结果弹窗，外加范围选择器）基本都有自动化测试盯着了——以后谁改坏这些界面，测试会立刻红给你看。你日常用起来体感没变化，但代码更安全、更不容易悄悄 regression。

② 底层原理（大白话）
- (a) 零基础讲解：继续「给界面组件请自动化测试员」——这些测试会真的把组件渲染出来、模拟点击、检查该出现的东西出现没（比如结果弹窗里成功数、失败数有没有算对、关闭按钮点了会不会关掉），等于每次改代码都有人复查。
- (b) 代码在干啥：新增 CharacterGroupList.test.tsx（按 role 分组渲染 / 分组标题带数量 / 勾选角色回调 onToggleSelect / 空分组跳过，共 4 例）与 ExpandResultModal.test.tsx（无结果不渲染 / 结果弹窗成功·失败计数与关闭按钮 onClose / 全部成功标题 / 扩展进度百分比，共 4 例）。tsc 0 错误，vitest 全量 88 文件 832/832 全绿（较 v2.19 的 824 增 8）；纯前端收敛，零额外接口、零额外 LLM 开销；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v2.19.0 角色栏/弹窗无障碍补全 + 组件测试再加 20 例（maxloop 多视角持续亲验）

① 用户视角
- 这一轮继续补「看不见但重要」的打磨：① 角色栏工具栏的「新建标签」输入框、范围选择输入框，以及「合并提案」面板的刷新按钮、「扩展结果」弹窗的关闭按钮，之前都是「没有文字名字」的——要么是个纯图标、要么只有灰色的占位提示，读屏软件和键盘用户根本不知道它们是干嘛的、按了会怎样。这一轮全给它们补上了能念出来的名字（如「新建标签名」「刷新合并提案」「关闭扩展结果」），键盘和读屏用户现在能定位、能操作了。② 顺手修了一个老隐患：角色栏里「按范围勾选」的功能之前引用了一个在它后面才定义的变量（像你写公式时引用了还没写的下一步），虽不常出错但属于不稳写法，这一轮把它理顺。③ 又给两个界面组件（范围选择器、工具条）补了自动化测试共 20 例，以后改坏它们测试会立刻红。

② 底层原理（大白话）
- (a) 零基础讲解：第一处像「一个按钮只有图标没有标签，盲人读屏念不出它干嘛；给它贴个文字标签，读屏就能念出来」——给纯图标/纯占位输入框补 aria-label（无障碍专用文字名）。第二处像「你写计算步骤时第 2 步引用了第 5 步才会出现的数，平时能算出结果但写法很别扭、容易出岔子；把它挪到前面先定义，步骤就顺了」，对应把 filtered 变量前置声明。第三处是「继续给界面组件请自动化测试员」，这次轮到范围选择器和工具条。
- (b) 代码在干啥：CharacterToolbar 新建标签 input、RangeSelector input、MergePendingPanel 刷新 button、ExpandResultModal 关闭 button 各补 aria-label；CharacterList 把 const filtered = filterCharacters(...) 移到 handleRangeSelect / handleToggleAll 之前；MergePendingPanel.load 改接受可选 AbortSignal，useEffect 内 new AbortController() 并在清理函数 abort()，刷新按钮 onClick 改 () => void load()。新增 RangeSelector.test.tsx（parseRange 9 分支 + 输入回车/失焦/Esc 提交）与 CharacterToolbar.test.tsx（aria-label + 5 类按钮回调）共 20 例。tsc 0 错误，vitest 全量 86 文件 824/824 全绿（较 v2.18 的 804 增 20）；纯前端收敛，零额外接口、零额外 LLM 开销；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v2.18.0 UI 更稳更干净 + 测试补到位（maxloop 多视角亲验，主对话自己当多个审查员）

① 用户视角
- 这一轮主要做「看不见但重要」的打磨，你日常用角色栏时体感变化很小，但更稳、更不容易出怪问题：① 后台有一项「加载项目时悄悄检查有没有重复角色」的任务，之前它发起请求后不等页面关掉就溜了，现在页面一关走它就立刻喊停，避免偶尔冒出「组件都关了还在后台改数据」的报错；② 角色行最右边的删除按钮，以前是个没有名字的图标（只有鼠标 hover 才显形），键盘和读屏用户根本不知道那是删除——现在给它补了名字「删除角色」，读屏会念出来、键盘也能定位；③ 之前几轮已经把搜索框、去重提示条、关闭按钮都补了无障碍名字，这一轮把角色行的删除键也补全，角色栏的无障碍就齐了。
- 另一件大事是「测试补上了」：之前 v2.17 新增的那批角色栏界面组件（统一标签、角色行、筛选栏等 6 个）一个自动化测试都没有，等于改了代码没人盯着会不会 regression。这一轮给它们全补上了组件测试（共 28 例），以后谁改坏这些界面，测试会立刻红给你看。

② 底层原理（大白话）
- (a) 零基础讲解：第一处像「你出门前让水电工顺手关个总闸，之前他关到一半人走了、闸还虚搭着，偶尔会漏电；现在明确告诉他『人走必须拉死闸』」，对应给那个后台请求加了个「取消开关」（AbortController），页面一关就拉闸，杜绝「组件没了还在改数据」的报错。第二处像「一个按钮只有图标没有标签，盲人读屏念不出它干嘛；给它贴个文字标签，读屏就能念『删除角色』」。第三处是「给界面组件请了一批自动化测试员」——这些测试会真的把组件渲染出来、模拟点击、检查该出现的东西出现没，等于每次改代码都有人复查。
- (b) 代码在干啥：CharacterList 后台去重探测的 fetch 加 signal（AbortController），useEffect 返回清理函数调用 controller.abort()；CharacterRow 删除按钮补 aria-label「删除角色」+ title。新增 src/lib/character-filter.ts 把原 CharacterList 内联的过滤规则（已合并卡隐藏 / 按角色 / 按标签 / 按生死状态 / 搜索匹配名字或别名）抽成纯函数 filterCharacters + isUserTag，便于单测，CharacterList 改调用。vitest 配 jsdom 组件测试环境：setupFiles 注册 jest-dom 匹配器（用 /vitest 子路径入口避免 expect is not defined）、include 增加 .test.tsx、afterEach(cleanup) 隔离（本项目未开 globals，RTL 自动清理不生效）；coverage 纳入 src/components/workspace。补 4 个测试文件共 28 例（TagChip / CharacterRow / CharacterFilters 组件测试 + filterCharacters 逻辑测试）。tsc 0 错误，vitest 全量 84 文件 804/804 全绿（较 v2.17 的 776 增 28）；纯前端收敛，零额外接口、零额外 LLM 开销；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v2.17.0 角色去重硬化：自动清重复更准确、更不乱（马斯克 CEO 循环运营交付）

① 用户视角
- 你写小说时，系统会自动把「其实是同一个人」的角色卡合并（比如「韩先生」和「韩姓男子」其实是同一个韩，或「迭戈」和「迭戈·美第奇」是同一人带家族姓），免得角色栏里一堆重复脏卡。这一轮把这套自动合并做得更准、更稳：① 之前大模型有时会瞎判断、把八竿子打不着的俩人误并，现在大模型只有「跨核心名」的疑似建议才进「待你确认」列表，确定性的同核重复（规则就能判定）直接自动合并，不被模型误判拖后腿；② 之前有类同核重复（变体+变体、全名+单「·」后缀）被旧逻辑误判成「要你确认」，现在直接自动合并；③ 加了一道安全闸——同一个名字下如果冒出不止一个「·」马甲（如迭戈·美第奇 / 迭戈·桑切斯，可能是不同人），不自动合并、交你确认，防错并；④ 合并后主卡优先保留干净真名（不再把「迭戈先生」这种称呼当主卡），「武帝」「王后」这类真实人名也不会再被误当成尊称错并；⑤ 已经合并掉的卡会被隐藏、且不再反复被重新合并刷出重复记录。角色栏的标签也统一收成一种样式、更整齐。

② 底层原理（大白话）
- (a) 零基础讲解：把角色当「人」看，系统给每个名字提炼一个「核心名」（剥掉先生/女士、姓+男子、·后缀这类称呼外衣、露出本名）。两个角色核心名一样，基本就是同一个人、可放心合并；核心名不一样，通常就是不同人、不能乱并。之前这套判断有两处松：一是模型偶尔把核心名不同的俩人塞进一组建议合并（误判），二是有些核心名相同的真重复被旧规则误判成「要人确认」。这次把「模型跨核心名的建议」降级为待确认、把「确定同核的重复」升级为直接合并，并在「同核却冒出多个·马甲」时踩刹车交人定，等于把合并的油门和刹车都调对了。
- (b) 代码在干啥：dedupeCharacters 重构为「确定性组（规则+核心名宽松分组）直接 high 自动合并，LLM 跨核心名组仅进 pending 待确认」，合并前存完整快照可回滚；computeConfidence 新增同核名 high 判定（变体+变体如韩先生/韩姓男子、全名+单·后缀如迭戈/迭戈·美第奇直接 high），多·马甲同核仍 low；pickMain 改干净 canonical 名优先；coreTokenOf 修复拖尾尊称剥离（修「迭戈先生」漏检 Diego 三兄弟）；isHonorificVariant 加风险 token 护栏（王/皇/帝/后/妃）避免「武帝」「王后」误并；dedupeCharacters 跳过已合并（软删「🗂 已合并」）卡防反复重合并。前端角色栏抽统一 TagChip 组件、默认隐藏已合并卡。tsc 0 错误，vitest 全量 80 文件 776/776 全绿；个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

---

## v2.16.0 实时多 Agent 编排控制台 + Round-26 实测（maxloop 深度体检 Round-26）

① 用户视角
- 你最在意的「能看到 Agent 干活、有进度」这次真给你做出来了：项目里新增了一个开发期诊断工具 agent-forge，浏览器打开 http://localhost:8787，就能看到主代理调度 5 个 Worker Agent（类型检查/架构/版本一致性/代码质量/安全扫描）实时扫描你的代码、进度条和日志一条条滚出来——不用再干等、不用再猜后台在干嘛。同时这一轮把你的前端 12 个页面 + 3 个动态页面 + 6 个核心接口都真机实测了一遍，确认全是好的（页面都正常渲染、接口都返回真实数据），还顺手揪出并修了一个隐藏问题：上一个版本号没同步齐（package.json 还停在 2.14.0，代码里已是 2.15.0），这次三个地方一起对齐到 2.16.0。

② 底层原理（大白话）
- (a) 零基础讲解：之前你派的「子代理」在本环境通道坏了（派出去要么空返回、要么连读文件工具都没被授予），所以你看不到任何 Agent 进度。这次我没跟那条坏通道死磕，而是另起了一套独立的小控制台——它不依赖那个坏通道，自己用 Node 起一个服务，前端页面通过「服务器主动推消息」（SSE）实时收到每个 Agent 的状态和日志，所以你能亲眼看到一堆 Agent 在干活。这轮实测则是主代理亲自当「测试员」，把每个页面和接口都点一遍、抓一遍，确认真实效果。
- (b) 代码在干啥：agent-forge/server.js 用 Node 内置 http 起服务，/api/run 触发后并行跑 agents.js 里 5 个 Worker（各自 spawn 子进程跑 tsc 或遍历源码统计），每个 Worker 通过 SSE 把进度和日志推到前端面板实时渲染；本轮另修 package.json 版本号（2.14.0→2.16.0）与 changelog-data.ts / CHANGELOG.md 对齐。本轮为 maxloop 深度体检 Round-26，子代理通道仍故障（自定义 agent 报 Tool Read not found），按「六之二」降级主代理亲验（见 PROCESS/meetings/round-26/chair-self-audit.md）。

---

## v2.15.0 确认路径一致性基线刷新对称修复（maxloop 深度体检 Round-25）

① 用户视角
- 一个你大概率从没注意、但确实存在的隐患被拆了：你用「自动确认」或「批量确认」（一次勾选好几章一起定稿）时，系统只去刷新「伏笔收束率」（哪条伏笔兑现了），却漏了「一致性事实基线」——就是用来查「前后人名/设定有没有打架」的那张底表。结果就是：手动点确认定稿的章，一致性面板立刻更新；批量/自动确认定稿的章，一致性面板会滞后、看不到最新内容。这次把两条路径补齐，和手动确认走一样的逻辑，批量/自动确认完，一致性面板也同步刷新。

② 底层原理（大白话）
- (a) 零基础讲解：软件里定稿后有好几种「后续动作」，像快递签收后要同时通知仓库和财务。手动确认那条线两个通知都发了；自动/批量确认那条线只发了其中一个（仓库），漏了财务（一致性），导致财务那边的账对不上。这次把漏掉的通知补上，三条线行为一致。
- (b) 代码在干啥：在 auto-confirm 与 batch-confirm 两个路由里，确认成功（确有章节被定稿）后，除了原有的触发伏笔检测，新增一行补触发一致性事实抽取（失败也不影响确认响应），与手动确认路由 PATCH 章节节点的写法完全一致。顺带修了一个测试隐患：自动确认路由单测之前没隔离这个新依赖，会偶发 500，现在加 mock 后稳定通过。本轮为 maxloop 深度体检 Round-25，子代理通道仍故障，按技能「六之二」降级主代理亲验（见 PROCESS/meetings/round-25/chair-self-audit.md）；tsc 0 错误，确认路由单测全绿。

---

## v2.14.0 确认栏类型逃逸收尾（maxloop 深度体检 Round-24）

① 用户视角
- 一个普通用户完全感知不到、但属于代码健康隐患的小毛病被修掉了：章节确认栏（你每章写完点「确认定稿」那个地方）里，有一段代码绕过类型检查去读「这篇稿子被审核过几次、最后一次填了没」的记录，原来用了 any 这种「不管什么类型都放行」的写法，等于把一道安全检查门拆了走后门。现在补回了正确的类型门（明确告诉编译器这条记录长什么样、最后一条的「填了没」用安全的可选链去读），编译器能替你盯着，以后不会再因为这种写法埋雷。你点确认定稿时看到的「已填/未填」文案，行为跟以前一模一样，只是底层更干净、更不容易出 bug。

② 底层原理（大白话）
- (a) 零基础讲解：代码里的「类型」像安检门——每个数据进出都要报清楚自己是什么（数字？文字？列表？）。any 等于「此门免检」，任何东西都能过，短期省事但长期会在你看不见的地方出事。这次把确认栏里两处「免检门」拆掉，换成「明确门」：先声明那条审核记录是「一组可能带/不带『填了没』标记的小条」，再用「可能为空就温柔跳过」的问号点读法去取最后一条的状态，编译器就能全程盯着，少一个隐患点。
- (b) 代码在干啥：确认栏组件里原本 `const logs: any[]`（声明成任意类型数组）和 `logs[...] as any`（强制转任意类型再取字段）两处，改成 `Array<{ fill?: string }>`（明确数组里每个元素是有个可选 fill 字段的对象）+ 可选链 `logs[...]?.fill`（如果最后一条存在就取 fill，不存在就安全返回空），最终用这个状态决定界面显示「已填/未填」。这次为 maxloop 深度体检 Round-24，本环境派出去的子代理（Agent 工具）通道返回空、文件也不落盘，已按技能里的「六之二」自动降级由主代理亲自读码诊断并完成修复（见 PROCESS/meetings/round-24/chair-self-audit.md）；tsc 0 错误，vitest 全量 80 文件 775/775 全绿。

---

## v2.13.0 体检链路无障碍与类型安全收尾（maxloop 深度体检 Round-23）

① 用户视角
- 三个不容易察觉但真实存在的毛病被修掉了：
  1. 只用键盘操作（或靠读屏软件）的人，以前在「全书体检」看板里没法选中某一行去查看它的命中明细——那个「点行看明细」只能用鼠标点。现在每行都支持键盘：Tab 键切到该行、按回车或空格就能打开那章的「写作体检」，而且能看到一圈清晰的焦点框，知道自己选到哪了。
  2. 单章「写作体检」弹窗里的「关闭」按钮，以前和系统的关闭机制是两条路，偶尔可能在关掉弹窗后留下一点「上次要打开哪章」的残留记录，导致下次点看板行跳转可能不灵。现在统一走同一条路，关得干净。
  3. 大纲树里分卷折叠的小箭头图标，代码里有一处多余的类型强制写法（历史残留），这次清掉了，代码更干净。

② 底层原理（大白话）
- (a) 零基础讲解：第一处像「把原本只能鼠标点的按钮，补上了键盘操作」——网页里能用键盘控制的元素，要给浏览器一个「我是按钮、可以被 Tab 选中、回车/空格等于点击」的声明，读屏软件也靠这个声明朗读。第二处像「两个关门的开关原本各管各的，偶尔会有一个忘关，这次让它们共用同一个开关」。第三处像「某根电线明明已经是标准规格，却额外套了个万能接头，这次把多余接头拆了」。
- (b) 代码在干啥：看板每行原本是个普通的 div（鼠标点它有反应，但键盘和读屏不认），我们给它加了 role=button（声明它是按钮）+ tabIndex=0（允许 Tab 选中）+ onKeyDown（回车/空格触发同一动作）+ focus-visible 焦点环样式 + aria-label（给读屏的说明文字）；弹窗底部关闭按钮从直接改状态改为调用统一的关闭函数（这个函数会顺带把受控触发的小纸条清空）；大纲树折叠箭头的 as any 强制类型删掉，改回类型安全写法。

---

## v2.12.0 全书体检看板行点击跳转单章体检（魔王循环第12轮）

① 用户视角
- 「全书体检」弹窗里的每一行章节现在都能点了。以前你在看板里看到某章有高危雷，得先关掉看板、去左边大纲找到那章、再点「写作体检」才能看明细；现在直接点那一行，看板自动收起、该章的「写作体检」弹窗直接弹出来，逐条命中明细和改稿建议就在眼前。从「发现危险章」到「看清怎么改」一步到位。

② 底层原理（大白话）
- (a) 零基础讲解：以前像「你在墙上贴的全班成绩表上，看到某同学主科崩了，却得跑到他课桌前翻他的卷子」；现在像「在成绩表那行直接点一下，他的卷子就弹出来了」。本质上是给看板和单章弹窗之间牵了根线——点看板某行，等于告诉系统「我要看这章的卷子」，系统就自动把那章的弹窗打开。
- (b) 代码在干啥：单章体检组件改成了「可被外面喊开」的模式（原本它只认自己那个章节按钮）；中间层（确认栏）记了一个「当前要点开哪章」的小纸条，看板行点击时把小纸条写上那章 id 并关掉看板，单章体检组件盯着这张小纸条，一旦 id 变了就自动去拉数据弹开；关掉弹窗时把小纸条擦掉，下次还能再点。三个组件靠这一个共享状态串成闭环，纯前端记个小纸条，没加任何新接口。

---

## v2.11.0 全书体检新增「高危/警示」分级列（魔王循环第11轮）

① 用户视角
- 「全书体检」弹窗的章节明细表里，多了一列「高危/警示」。以前每章只显示一个「安全分」数字，你看完只知道某章分低，却不知道是「必改的高危雷」还是「轻微的提示」；现在这列直接给两个数字（如 2/3 = 2个高危 + 3个警示），高危多的用红色加粗、只有警示的用黄色、一个都没有的显示灰色「—」，扫一眼整本书就知道哪几章真正踩了红线，优先去改。
- 配合 v2.10.0：在全书看板里锁定了高危章，再点开那章的「写作体检」就能看到逐条命中明细和改稿建议，从「定位」到「拆解」一条龙。

② 底层原理（大白话）
- (a) 零基础讲解：以前像「看一张全班成绩单，只知道谁总分低，却不知道偏哪科」；现在像「成绩单上多了一栏『主科错题数/副科错题数』」，一眼看出是谁主科崩了（高危必改）、谁只是副科小错（警示建议改）。后台扫描器本来就算出了每条命中是「高危」还是「警示」（贴了严重程度标签），只是之前只报总数。
- (b) 代码在干啥：后端全书体检接口在算每章时，顺手数了数命中里「高危(error)」和「警示(warning)」各有几条，把这两个数一起传给前端；前端表格加了一列，用「高危/警示」双数字显示，按红/黄/灰三色区分严重程度。扫描器一行没改，纯把已有的严重程度标签分组计数后透出来。

---

## v2.10.0 内容安全命中明细显式化（魔王循环第9轮）

① 用户视角
- 「写作体检」弹窗里，安全分下面多了一个可以点开收起的「命中明细」列表。以前它只告诉你「这章踩了 5 个雷」，现在点开就能看到每一个雷：属于哪一类（精确禁用词 / 句式 / 身体模板 / 模糊词 / AI高频词）、危险程度（红=高危、黄=警示、灰=提示）、具体出现在哪句话的上下文、以及建议怎么改。
- 比如命中「此外」这种 AI 腔词，列表里会标红「高危」并建议「删掉或用顺承句替代」；命中「瞳孔一缩」这种套路化身体描写，会标黄「警示」建议换成具体动作。改稿时直接照着改，不用猜雷在哪。

② 底层原理（大白话）
- (a) 零基础讲解：以前像「只给你一张写着『有5个雷』的纸条」，现在像「把每个雷拍了照片、标了位置、还写了拆弹说明，摊在你面前」。后台那套查违禁词的扫描器本来就算出了每一条命中的详细信息（在哪、是什么、多严重、怎么改），只是之前只往外报了个总数。这次我们把这堆明细也一起传给前端。
- (b) 代码在干啥：后端体检接口多返回了一个 matches 明细数组（每条含类别、严重度、命中内容上下文、修改建议，最多80条防过大）；前端面板把这些信息按危险程度排序，用红/黄/灰三色标签 + 一句上下文片段 + 一句建议，一条条列在可折叠区域里。扫描器本身一行没改，纯把已有的数据透出来展示。

---

## v2.9.0 质量分回写大纲常驻徽章（魔王循环第8轮）

① 用户视角
- 「全书体检」弹窗底部多了一个「保存质量分到大纲」按钮。点一下，左边大纲树的每一章名字后面就会常驻显示一个彩色分数小标签。
- 颜色含义：绿=写得很好（≥85）、主色=不错（≥70）、黄=勉强（≥60）、红=差（不到60）。以后不用每次点开弹窗，扫一眼大纲就知道哪章该返工。
- 点完保存，大纲和当前选中的章节会自动刷新，立刻看到新分数。

② 底层原理（大白话）
- (a) 零基础讲解：以前体检像「每次都要翻开体检报告单才能看分数」；现在像「把每章的分数贴到它名字旁边、贴在墙上常看」。我们给每章在数据库里本来就有个存「质量分」的小格子，只是之前一直空着。这次体检算好分数后填进去，大纲读出来就直接画个彩色标签贴章节名旁边。
- (b) 代码在干啥：按钮点下后，后端把全书每章的质量分数用数据库更新命令批量写进每个章节记录的 qualityScore 字段（哪一章写失败也不影响其他章，各自容错）；前端大纲组件读到这个字段就画彩色徽章；保存成功后自动重新拉一次项目数据，大纲和当前章立刻更新。

---

## v2.8.0 全书健康度体检看板（魔王循环第7轮）

① 用户视角
- 章节确认栏常驻多了一个「全书体检」入口。点一下，弹窗一次性把整本书每一章的「安全分 / 质量分 / 评级 / 字数 / 状态」列成表，写得差或踩红线的章节整行标红。
- 弹窗顶部四张汇总卡片：已体检多少章、平均质量分、平均安全分、需要返工的章数。一眼定位全书要返工的章节。

② 底层原理（大白话）
- (a) 零基础讲解：单章体检是「翻一页看一页」，全书体检是「把整本书所有页的分数自动汇总成一张总表」。底层复用现成的两个本地计算器——一个查违禁词（安全），一个打写作质量分——把书里每一章的正文各跑一遍，不花钱请 AI。
- (b) 代码在干啥：新增一个只读后端接口，按项目把所有带正文的章节捞出来，逐个调用那两个纯函数算出分数，前端独立组件把结果画成表格和汇总卡片；纯本地计算、秒出，不消耗任何 LLM。

---

## v2.7.0 写作安全/质量「体检」面板（魔王循环第6轮）

① 用户视角
- 章节确认栏常驻多了一个「写作体检」入口。点一下，弹窗给你这一章的「内容安全分 / 模糊词密度 / 各类禁用词命中数」以及「六维写作质量分 + 总分 + A/B/C/D 评级」。
- 双达标就提示「可放心定稿」；不达标就提示该清洗什么（安全红线 / 质量没到 C 级）。定稿前多一道可见的把关。

② 底层原理（大白话）
- (a) 零基础讲解：像写完作业自己先用红笔自查一遍——有没有写不该写的词、句子是不是太水。这两个自查都是现成的本地规则计算，不联网、不请 AI，点一下立刻出结果。
- (b) 代码在干啥：新增一个后端接口按章节取正文，跑违禁词扫描和写作质量评分两个纯函数；前端弹窗展示结果。纯前端弹窗、零额外 LLM 开销。

---

## v2.52.0 写作速率三连击（P0 优化落地 + 真机复测）

① 用户视角
- 写一章明显快了：同样硅基流动 DeepSeek-V4-Flash、同样「新城·龙陨之地」项目，单章耗时从实测 222 秒降到 91 秒（约 2.4 倍提速），进度条也不再卡在 99% 像死机，而是平滑走到 100% 然后告诉你「写好了」。
- 进度条变「按字数实时爬」：以前写正文时进度条长时间停在 0%、过了两三分钟才突然跳 33%，现在它随着字一个个冒出来平滑往前走，开头还有个 3% 的小信号证明它在干活、没卡住。
- 后台该干的活照样干：审校、摘要、伏笔、逻辑自查这些「写完之后的深加工」改到后台悄悄跑，你拿到正文的同时它们就在补，不挡你继续写下一章。

② 底层原理（大白话）
- (a) 零基础讲解：之前每次让 AI 写一章，都先把「整本书的角色卡 + 世界设定」原封不动塞给 AI（实测有 18 万字！），AI 光读这些就慢、还容易超它的上下文窗口被截断。现在改成「智能精简」——角色卡的关键信息全留，世界设定的长尾巴按预算裁剪，总量压到 1.2 万字（15 倍瘦身），AI 吃得动、写得快。
- (b) 代码在干啥：三处改动——①重写全局提示词拼接（标题去重 + 每字段截断 + 4 档递减预算，角色核心字段保真）；②批量写正文的进度回调改成「按已生成字数 / 目标字数」实时回写进度，前端直接读着平滑走；③给写正文核心加了个「写完即交付」开关：正文一落库就告诉你完成，后面的审校/摘要等 2 次 AI 调用改到后台异步跑（项目本就约定这些是「锦上添花、失败也不影响交付」）。配套 5 例单测锁死提示词压缩；双门禁 tsc 0 错 / vitest 108 文件 1059 全绿；已发 v2.52.0 并推 origin main。
