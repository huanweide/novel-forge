# novel-forge 更新报告（大白话版）

> 这份报告只讲两件事：你作为用户能感受到什么、背后那堆代码到底在干啥（用大白话）。不写技术对比、不写取舍、不写复现步骤。

## v2.8.0 现在能一键给「整本书」做体检了（UI/功能增强，魔王循环第7轮）

### 一、用户视角：你实际得到了什么

1. 有了「全书体检」这面照妖镜
   确认栏在「写作体检」（单章）旁边多了个「全书体检」按钮。点一下，它把你整本书所有写了正文的章节都过一遍内容安全和写作质量两关，弹出一个大表格——每章的安全分、质量分、A/B/C/D 档、字数、状态一目了然，写得差或踩线的章节整行标红；顶部还给你三张卡片：平均质量分、平均安全分、有多少章需要返工。你再也不用一章一章点，一眼就知道全书哪几章该先回去改。

2. 写长篇也有了「质量地图」
   以前只能单章看健不健康，现在有了全书视角，能横向比较哪章拖后腿、哪章最稳。比如发现第 3 章质量分只有 40、第 17 章踩了安全红线，直接定位去返工，不用凭记忆翻。纯本地秒出，不调 AI、不花 token。

### 二、底层原理（大白话讲清楚）

(a) 零基础讲解：我们到底加了啥、咋干的
- 这功能就是在上一轮「单章体检」的基础上，把那两个现成的纯函数盒子（查禁用词的、打质量分的）从「一次查一章」升级成「一次查整本书」。它们还是不调 AI、不碰数据库，喂文字进、出数字出；只是这次后端一口气把全书所有正文章节都取出来，挨个丢进两个盒子，再把每章的结果拼成一张大表。

(b) 代码在干啥（用你能懂的话讲，不复述术语）
- 后端新增一个接口：收到「项目 ID」后，去数据库把这本书所有带正文的章节一次性取出来（最多取 300 章，超长书分批），每一章分别丢进「禁用词盒子」和「质量盒子」，把每章的安全分、质量分、等级、字数算好，连同「全书平均质量、平均安全、需返工章数」一起打包成 JSON 交还给前端。
- 前端新建一个独立小看板：确认栏多一个「全书体检」按钮，点按调用上面那个接口，把返回的数据画成一张表——序号/标题/字数/安全分/质量分/评级/状态七列，写差或踩线的行整行染红；顶部四个汇总卡片。整个过程不碰 AI，所以秒出、不烧钱。

---

## v2.7.0 写作前能先「体检」一下你的章节了（UI/功能增强，魔王循环第6轮）

### 一、用户视角：你实际得到了什么

1. 定稿前多了一面「照妖镜」
   每章写完后，确认栏多了一个「写作体检」按钮。点一下，它会把你这章正文过两关：内容安全（有没有踩平台禁用词、模糊词密度高不高）和写作质量（废话多不多、视角跳不跳、句子是不是一个样等六个维度），然后弹一份报告——安全分多少、质量总分多少、A/B/C/D 哪个档、哪里踩线了，一目了然。双达标才提示「可放心定稿」，否则告诉你先洗哪些禁用项或润色哪里。

2. 不用再靠感觉猜「这章行不行」
   以前你写完一章，只能靠自己读或点「AI诊断」（那个要调大模型、慢且费 token）。现在「写作体检」纯本地秒出，不调 AI、不花 token，发布前随手点一下就知道这章健不健康，相当于给每章配了个随身的语文老师+审核员。

### 二、底层原理（大白话讲清楚）

(a) 零基础讲解：我们到底加了啥、咋干的
- 这功能就是把前面几轮已经做好、且被测试钉死的两个「纯函数盒子」串起来用：一个盒子专门查禁用词（前面第2轮装的内容安全扫描器），另一个盒子专门给写作质量打分（前面第5轮装的六维评分器）。它们都不调 AI、不碰数据库，喂文字进、出结果出。这轮只是新加了一个「按钮 + 弹窗」，把这两个盒子的结果拼成一个给人看的报告。

(b) 代码在干啥（用你能懂的话讲，不复述术语）
- 后端新增一个接口：收到「章节 ID」后，去数据库把这章正文取出来，分别丢进「禁用词盒子」和「质量盒子」，把两个盒子返回的数字（安全分、各类命中数、六维分、总分、等级）整理成一份 JSON 交还给前端。
- 前端新建一个独立小面板：确认栏里多一个「写作体检」按钮，点按调用上面那个接口，把返回的数字画成进度条和小卡片——安全分用红黄绿条、质量六维各一条进度条、总分和 A/B/C/D 徽章，最后根据「安全和质量是否都达标」给一句结论。整个过程不碰 AI，所以秒出、不烧钱。

---

## v2.6.0 给「你的文字写得咋样」的评分器加了自动质检（魔王循环第5轮）

### 一、用户视角：你实际得到了什么

1. 网站给你的写作打分更稳更准了
   网站有个"阅卷老师"会给你每章打分（废话多不多、是不是光说不做、视角跳不跳、句子是不是一个样、对话像不像人话、主语是不是老重复），综合出总分和 A/B/C/D 等级。这轮给这个阅卷老师写了 11 条自动质检，把"空章节怎么算、干净文字该多少分、塞满废话该扣到哪、视角乱跳怎么判"全锁死——以后升级，它不会因为改了一行代码就把"优等文"误判成不及格，也不会把废话率算错。

2. 评分逻辑复用且不重复劳动，结果更可信
   阅卷老师打分时会复用"禁用词扫描"的结果（前面第2轮装的那个扫描器），不再重复算一遍。这轮测死了这个复用：没传扫描结果就强制按"全是废话"算，证明它确实在用扫描结果而不是重新跑；视角一致性也测了——你老切主角它就扣分，没给角色名单就满分不冤枉。

### 二、底层原理（大白话讲清楚）

(a) 零基础讲解：我们到底改了啥、咋干的

- 这个"阅卷老师"是个纯函数：你把一章正文喂进去（可选带上你知道的角色名字），它按六个维度各打个 0~100 的分，再按权重（废话率两成、展示vs讲述两成、其余四个各一成半）乘起来四舍五入出总分，套 A≥85/B≥70/C≥60/D<60 出等级，总分≥60 算通过。整个过程不调 AI、不碰数据库、不依赖网页，是个"喂文字、出分数"的确定性盒子。
- 「自动质检」就是列一堆"喂它某段文字、它必须返回某总分/某等级/某维度分"的例子，把这套评分逻辑钉死。

(b) 代码在干啥（用你能懂的话讲，不复述术语）

- 11 条测试分别喂给阅卷老师各种文字：空章节→六维全满分、总分100、A级、通过；干净的动作描写→通过且≥85 A级；塞满禁用词→总分掉到100以下、不是A、废话率低于100；summary 写错时文案带"达标"字样。
- 还测了"复用"——不传禁用词扫描结果就强制废话率100（比真实扫描高），证明它在用扫描结果；以及"视角"——多段频繁换主角→视角一致性低于100，不给角色名单→视角满分；最后把评级锁死在 A/B/C/D 四档里、通过严格等于总分≥60。每条都断言"必须返回某结果"，跑一遍全过才算数。
- 纯逻辑、不调 AI 不碰数据库，跑得飞快随时能验证。

---

## v2.5.0 给「写作规则引擎」加了自动质检（魔王循环第4轮）

### 一、用户视角：你实际得到了什么

1. 你设的写作规则之间"打架"时，网站判得更准更稳了
   你可以在网站里给 AI 设各种写作规则（比如"这一章禁止用闪回""开头必须交待时间"）。有时候你设的两条规则会互相矛盾——一条说禁止、一条说必须。这轮给这个"规则裁判"写了 13 条自动质检，把"什么时候算冲突、该听谁的"全锁死：同类且意思相反才判冲突、不同类不误报、冲突时高优先级的规则压过低优先级，以后升级不会乱判、也不会把不冲突的误报成冲突。

2. 规则真正写进正文时，标记更清楚不会混淆
   规则引擎会把你的规则注入到给 AI 的写作指令里。这轮测死了注入行为：没有规则就原样返回；有冲突时，输的那条规则会被打上"已被更高优先级规则覆盖"的标记，你一眼能看出哪条被压住了；你原本写的作者指令和冲突裁决记录也会正确拼在一起，不会丢、不会乱。

### 二、底层原理（大白话讲清楚）

(a) 零基础讲解：我们到底改了啥、咋干的

- 这个"规则裁判"是个纯函数：输入你设的一堆规则（每条带分类、内容、优先级、生效范围），它干两件事——detectConflicts 负责找哪些规则互相矛盾（判冲突的标准是"同一分类 + 意思相反"，比如一条"禁止X"一条"必须X"；或者两条规则管的是同一个东西且内容不同），injectRules 负责把规则拼进给 AI 的指令文本，冲突的败方打标记。整个过程不碰数据库、不调 AI、不依赖网页，是个"喂进去、算出来"的确定性盒子。
- 「自动质检」就是列一堆"喂它某组规则、它必须返回某判定/某文本"的例子，把这套裁判逻辑钉死。

(b) 代码在干啥（用你能懂的话讲，不复述术语）

- 13 条测试分别喂给裁判各种规则组合：空规则→无冲突；一条"禁止闪回"一条"必须闪回"同分类相反→判定冲突且高优先级胜；两条都正向（都"必须"）→不冲突；不同分类的两条→不冲突；两条规则里引号同名的→判冲突。
- 还测了"裁决三阶段"：谁优先级数值大听谁的、一样大比谁更具体、再一样比谁先创建；以及"注入"——空规则原样返回、单条规则带名字、冲突败方标记"已被更高优先级规则覆盖"、你写的作者指令正确拼接、有冲突附裁决记录。每条都断言"必须返回某结果"，跑一遍全过才算数。
- 纯逻辑、不碰数据库不调 AI，跑得飞快随时能验证。

---

## v2.4.0 给「你说话网站就懂」的解析器加了自动质检（魔王循环第3轮）

### 一、用户视角：你实际得到了什么

1. 你用大白话指挥网站时，识别更稳了
   你在对话框里打字让 AI 干活（比如"查一下主角信息""继续写下一章""新建角色叫李雷"），网站有个"翻译官"把这些话变成具体功能调用。这轮给这个翻译官写了 16 条自动质检，把"什么话对应什么功能、该带什么参数"全锁死——以后升级，它不会把"写短的章节"误解成别的字数、也不会漏掉你说的功能。

2. 识别不出时更聪明地求助 AI
   遇到它真听不懂的话（比如闲聊"今天天气真好"），它会老老实实把这话交给背后的 AI 大模型去理解，而不是瞎猜。这轮把"什么时候该自己处理、什么时候该求助 AI"的边界也测死了，不会乱兜底也不会瞎硬撑。

### 二、底层原理（大白话讲清楚）

(a) 零基础讲解：我们到底改了啥、咋干的

- 这个"翻译官"是个**纯规则引擎**：它有一张"关键词→功能"的对照表（比如看到"查+角色"就调查角色功能，看到"继续写"就调续写功能），不调 AI、零消耗、秒回。一条消息可能触发好几个功能，它按"把握有多大"排个序、同一个功能只留最靠谱的那条。"自动质检"就是列一堆"喂它某句话、它必须返回某功能+某参数"的例子，把这套对照钉死。
- 「求助 AI 的边界」：如果一句话一个功能都没匹配上（或全是把握很小的），说明规则搞不定，就交给大模型兜底。"质检"把这条边界测明白：空话/闲聊→求助 AI；有明确功能→自己干。

(b) 代码在干啥（用你能懂的话讲，不复述术语）

- 16 条测试分别喂给翻译官各种话："查一下主角信息"（应认出查角色、把握0.85）、"新建角色叫李雷"（应提出名字"李雷"）、"写短的章节"（应带参数"1500字"）、"写长一点的正文"（应带"4000字"）、"写本章"（默认"2500字"）。
- 还测了"同一功能被多条规则命中只留一条""多个功能按把握大小排序""听不懂的闲聊返回空、交给 AI"。每条都断言"必须返回某结果"，跑一遍全过才算数。
- 纯逻辑、不碰数据库不调 AI，跑得飞快随时能验证。

---

## v2.3.0 给「内容安全扫描」加了自动质检（魔王循环第2轮）

### 一、用户视角：你实际得到了什么

1. 你写出来的正文被"AI腔体检"的判定更稳定了
   网站有个功能：你让 AI 写完一段，它会自动扫一遍，标出"像 AI 写的套路话"（比如"此外""综上所述""瞳孔一缩"这类固定句式、模糊词堆砌、AI 高频词）。这轮我们给这套扫描写了 19 条自动质检清单，把"什么算违规、怎么扣分、怎么提示"全锁死——以后网站升级，这套判定不会偷偷变松或变严、也不会漏判误判。

2. 更防黑客：有人想塞危险正则搞垮网站会被拦下
   扫描规则支持用"正则表达式"自定义。这轮专门测了一种最坏情况：有人写个会"算到死机"的恶意正则会怎样——结论是被系统当场拦下、弹个温和提示、网站照常转，绝不卡死。你的正文扫描不会因为别人的坏规则崩掉。

### 二、底层原理（大白话讲清楚）

(a) 零基础讲解：我们到底改了啥、咋干的

- 「内容安全扫描」像你交稿前的"AI腔体检"：它按五类毛病检查你的正文——固定的禁用词、套话句式、模板化身体描写、模糊词堆砌、AI 偏爱词——每中一项就扣分，扣到不及格就提醒你改。"自动质检"就是列一堆"喂它某段文字、它必须给出某结果"的例子，把这套体检标准钉死，防止以后改代码时把标准改歪。
- 「防死机正则」：正则表达式是个很灵活但也危险的工具，写得不好（比如"嵌套重复"）能让电脑一直算、把服务器拖垮。我们加了一道"安检门"：扫描前先判断这个正则会不会搞死机，会就直接拒绝、给提示、不执行。这轮测试就是确认这道门真管用。

(b) 代码在干啥（用你能懂的话讲，不复述术语）

- 19 条测试分别喂给扫描器各种文字：纯正常的（应满分通过）、含禁用词的（应扣分并标红）、模糊词堆很多的（应警告并附摘要）、空文本的（应直接通过不报错）。每条都断言"扫描器必须返回某结果"，跑一遍全过才算数。
- 防死机那条：测试故意传一个会拖垮电脑的恶意正则，断言扫描器"不崩溃、返回一个温和提示、不把你的正文判错"——证明安检门拦得住。
- 这些都是纯逻辑测试，不碰数据库、不调 AI、不依赖浏览器，所以跑得飞快、随时能验证。

---

## v2.2.0 上下文窗口「重新摘要」按钮 + 摘要确认（大修 #221 收官）

### 一、用户视角：你实际得到了什么

1. 写小说时的「上下文窗口」多了个「重新摘要」按钮
   你写小说时，右侧有个"上下文窗口"会显示喂给 AI 的记忆（其中"中期记忆"是你之前写过的章节被压成的小结）。现在这个"中期记忆"区多了个「重新摘要」按钮：你改完一章想让 AI 用最新正文重新总结，点一下就行。

2. 重新生成前先让你看、还能改，确认才保存
   点「重新摘要」后，AI 会基于当前章节正文重新生成一份摘要，但**不直接覆盖**你原来的——它会弹一个确认框，把新摘要显示出来给你看，你还能顺手改几个字；只有你点「确认保存」，新摘要才正式替换旧的。这样 AI 万一生成得不好，你也不会稀里糊涂丢掉原来的好摘要。

### 二、底层原理（大白话讲清楚）

(a) 零基础讲解：我们到底改了啥、咋干的

- 「中期记忆」就是 AI 写作时的"备课笔记"——把你写过的前面章节压成几百字的小结，AI 写新章节时瞄一眼就知道前面发生了啥，不用每次把全文都塞进上下文（那样会撑爆、还贵）。
- 「重新摘要」按钮：相当于让 AI 根据你这章**最新**的正文，重新写一份"备课笔记"。但我们不放心让 AI 直接覆盖旧的——所以先让它打草稿（不存盘），你过目、甚至改两笔，签了"确认"才正式替换。这像公司里"下属先出草稿、老板签字才生效"，避免 AI 抽风把好的笔记覆盖掉。
- 后端还有一道"防呆"：如果 AI 返回的是空话、套话模板（没实质内容），系统直接拒收，绝不写进数据库。

(b) 代码在干啥（用你能懂的话讲，不复述术语）

- 按钮点下去：前端调摘要接口，传「只要生成、别存库」的标记，拿到新摘要后弹个窗口显示，并给一个能改的文本框。
- 你点「确认保存」：前端把（可能改过的）摘要再发给接口，这次传「直接存库、别再跑 AI」的标记，数据库把旧摘要替换成新的。点「取消」就什么都不存。
- 后端接口里有一道检查：收到要存的摘要时，先判断是不是空模板/废话，是就拒收报错，拦住脏数据。

---

## v2.1.0 测试盲区收尾 + 仓库散报告清理（魔王循环第1轮）

### 一、用户视角：你实际得到了什么

1. 给「版本历史」功能加了自动检查
   你每次让 AI 覆盖改写一段正文前，网站会自动把「改写前的旧版本」存一份快照（方便以后回滚、对比不同版本）。这轮我们给这个「自动存快照」的小功能写了自动质检清单，锁死五种情况：空内容不瞎存、第一次存就是第 1 版、内容和上次一模一样就不重复存、内容变了就存成新的一版、就算数据库抽风存失败也绝不卡住你继续写正文。

2. 把仓库里一堆零散的旧报告删了、并成一份
   之前每轮改功能都会生成一份独立的工作报告，散落得到处都是。这轮把它们全删了，统一并进一份大白话的《更新报告.md》。你以后只看这一份，就能顺着版本号看懂网站每次改了啥；技术细节仍留在 CHANGELOG.md 备查。

### 二、底层原理（大白话讲清楚）

(a) 零基础讲解：我们到底改了啥、咋干的

- 「版本快照」就像你写论文时，每次大改前微信自动帮你存一份「修改前的版本」。这个小功能里有个「去重」逻辑：这次内容和上次一样就不重复存（省空间），不一样才存新的一版。「自动质检」就是列一堆「喂它某种情况、它必须给出某种结果」的例子，把这套行为钉死——以后谁改错，电脑立刻亮红灯。
- 删散报告：之前每轮优化都生成一份独立报告文件，散落各处。用户要「只看一份」，我们就把零散的删了，历史技术细节在 CHANGELOG.md 留着，大白话变化统一进《更新报告.md》。

(b) 代码在干啥（用你能懂的话讲，不复述术语）

- 给「存快照」小功能喂了 5 种典型情况（空内容、第一次存、内容相同、内容不同、数据库报错），检查它存的版本号、内容对不对，以及报错时绝不卡住你。例如「内容不同」时，它必须把新内容存成「最近版本号 + 1」。
- 删掉的那个错位测试：有个测试文件放错了文件夹（和另一个同名文件重复），我们删掉错位的那份，保留正确位置的那份，避免重复跑、结果混淆。

---

## v2.0.21 测试体系全面补强 + 修了一个显示乱码

### 一、用户视角：你实际得到了什么

这轮我们主要在「让网站更稳、不偷偷出 bug」和「修一个你看不懂的乱码」两件事上发力。

1. 修了一个界面乱码
   你写小说时，某处会显示「这段提示词 / 上下文占了多少额度（百分比）」。以前在一种特殊情况（额度总数还是 0）下，它会显示成一串「NaN%」的怪字符，你看不懂也不知道啥意思。现在这种情况会正常显示「0.0%」，不再出现乱码。

2. 给网站背后几十个小功能配了「自动质检」
   网站背后有一大堆负责算账、校验、清理的小工具（比如：检查你输入合不合法、扫描有没有违规词、判断角色笔记要不要过期清理、算字数额度）。以前这些小工具改了代码，只能靠人肉点一遍看有没有改坏。现在我们给它们每一个都写了「自动检查清单」：你改完代码，跑一条命令，电脑自动把几百个检查挨个验证，全过才算合格。以后你用的网站更不容易悄悄出 bug。

3. 测试结果
   自动检查从几百个涨到 719 个，全部通过。整体代码被自动检查覆盖的比例从 33% 提到 39%（意思是网站上超过三分之一的代码现在都有自动护栏盯着）。

### 二、底层原理（大白话讲清楚）

(a) 零基础讲解：我们到底改了啥、咋干的

- 「token」你就当它等于「字数额度」。AI 写小说时要算你这段提示词、上下文一共占了多少「字数额度」，好决定还能写多少。网站有个小功能负责把「用了 X、总共 Y」算成百分比显示给你。
- 那个乱码是怎么来的：当「总共 Y」还是 0（比如你还没设定额度上限）时，数学上「除以 0」是没意义的，电脑就算出一个叫 NaN 的怪东西（意思是「这不是一个数字」），显示成「NaN%」。我们加了一道很简单的判断：只要发现总数是 0，就直接给「0.0%」，根本不去算那个没意义的除法。
- 「自动质检」是啥：好比给每个背后小工具请了个质检员。你喂它一个输入，它检查输出对不对。比如喂「总数 0」，它就必须回答「0.0%」，答错就报警。我们给「角色去重、内容安全扫描、笔记过期清理、字数额度计算」等几十个小工具都配了这种质检员，列了一堆「输入某某、应该输出某某」的例子锁死行为。

(b) 代码在干啥（用你能懂的话讲，不复述术语）

- 修乱码那行：原来写的是「百分比 = (用了 ÷ 总数) × 100，再保留两位小数」。我们改成「如果总数大于 0，就正常算；否则直接给 0.0」。就加这一句判断，乱码没了。
- 那些测试清单：每个背后小工具配一个同名的「.test」文件，里面写了一堆「这种情况应该得到这个结果」的例子。电脑跑一遍，全对就亮绿灯。比如「笔记过期清理」这个小工具，我们测了：最重要的笔记永远留着；不重要的笔记放太久就自动删掉——这些规则都用例子定死，以后谁改错立刻红灯。

### 顺着写：本轮前面几轮测试补强（同一件事分阶段做）

- 阶段一（全面铺开）：先给「角色去重、文本解析、JSON 解析」这三类小工具写自动检查，并把「覆盖率统计」工具接上，看清哪些代码还没被检查到。
- 阶段二（输入与内容安全）：给「输入合法性校验」和「违规词扫描」写检查——确保你填的表单数据在进数据库前被拦住非法内容，以及扫出违规词时正确提示。
- 阶段三（写作辅助）：给「续写截断判断」「自动确认率计算」写检查，锁死这两个写作时的小逻辑不出错。
- 阶段四（探索与字数）：给「章节探索工具的关键词提取」和「字数额度计算 / 超长截断」写检查，确认提取关键词、算字数、超长截断都正确。
- 阶段五（收尾）：修掉上面说的乱码 bug，并给「笔记过期清理的判断逻辑」写检查。

---

> 完整的技术更新日志（含更早的 v2.0.17～v2.0.20 角色去重、日志治理等）见项目根目录 CHANGELOG.md。这份大白话报告只聚焦你用得到的变化。
