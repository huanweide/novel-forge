# 04 待迭代项清单（非阻塞）

## 一、目标
列出 novel-forge 已知非阻塞待迭代项，作为后续优化 backlog；随推进持续校准状态，避免未来会话误判。

## 二、待迭代项清单
1. ~~巨型组件拆分~~ ✅ 已完成（v0.44.3–v0.44.7：CharacterList / WorldPanel / PostGenPanel / AIChatBar 四个巨型组件全部拆完，父保留全部 state/handler 仅子树组件化，行为 100% 不变）。
2. **大列表虚拟化**：StorylineList、LoreTable 行较多时需虚拟化（react-window / @tanstack/virtual）。
   当前数据量小，普通 map 渲染无性能问题，属过度设计 → **暂缓**。
3. ~~dialog focus-trap~~ ✅ 已完成（v0.44.2：通用 useFocusTrap hook + 11 个裸弹窗统一挂载焦点陷阱）。
4. ~~adopt 失败重试~~ ✅ 已完成（v0.44.1：探讨模式采纳失败可点击重试，状态串统一）。
5. **dev hydration 根治**：偶发 hydration 警告。已排查——所有 `localStorage` 读取均已在 `useEffect` 内（app/page.tsx / explore/page.tsx / workspace 页），render 作用域无浏览器 API 直接访问；
   剩余偶发警告需复现抓栈定位（疑似 StrictMode 时序或第三方组件）→ **暂缓**。
6. ~~自动化填表端到端生成实测~~ ✅ 已做（v0.35.0 E2E 脚本验证通过；v0.44.5 项目级 LLM 覆盖端到端实测跑通）。

## 三、状态
【成功】清单已建立（2026-07-28）并持续校准。
当前活跃 backlog 仅余 2（大列表虚拟化·过度设计·暂缓）与 5（dev hydration·需复现·暂缓），均为低优先级非阻塞项。
酒馆理论迁移 7 项（v0.37–v0.44.8）已全部完成；v0.44.8 经 novel-forge-diagnostic 六维度全过（tsc 零错误 / Prisma 同步 / React 反模式 0 真实问题 / API 路由全 try-catch / 无死代码 / 服务 HTTP 200），项目处于稳定健康态。
