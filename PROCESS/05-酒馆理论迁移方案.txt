# 05 酒馆（SillyTavern）理论迁移方案

## 一、目标

全面学习用户提供的酒馆预设/正则/世界书/日志样本与理论说明，把其中对 Novel Forge 有用的方法论抽出来，形成可执行的产品改造项；并明确下一步先推进哪一项。

## 二、酒馆运行原理（学习结论）

1. **核心数据流**：`正文 → 填表总结 → 召回 → 正文`。写完一轮后由 LLM 把事实抽成结构化表格（SQL/JSON 行操作），下次生成时按关键词（绿灯）把相关表格行注入上下文。
2. **信息熵与格式**：酒馆实践认为 XML 标签适合做结构边界、YAML 适合中文说明、JSON 适合复杂结构化数据；最优是"提示词 + XML 标签 + YAML 内容"的自定义混合格式。
3. **上下文深度（Depth）**：世界书条目可配置插入深度（0-4）。0 级=用户指令下方（强效拦截），1 级=user 上方，2 级=更靠前，3 级=背景设定。越深越弱，但持久。
4. **正则后处理**：用正则清洗模型输出——删除思维链（`<thinking>`/`<think>`/`<analysis>`）、折叠远楼层消息、美化 HTML 展示。
5. **角色卡模板**：`<角色称呼>` + 特点/外貌/穿着/性格/背景/关系/发言参考，短设定触发长设定。
6. **表格七要素**：表格名称 / Note / Init / Insert / Update / Delete / DDL。
7. **记忆清除/注意力**：远楼层隐藏（不发送）、按关键词召回、摘要压缩老楼层。
8. **预设体系**：表格模板预设、剧情推进预设（召回规则）、API 参数预设、正则预设、世界书预设。

## 三、可迁移到 Novel Forge 的方法论

| 酒馆能力 | Novel Forge 现状 | 迁移方案 | 优先级 |
|---|---|---|---|
| XML+YAML+JSON 混合提示词 | 当前 mostly 纯文本/JSON | 升级 `assemblePrompt`：用 XML 标签包裹块（根 `<novel_forge_context>` + `<writing_task>`，子标签 system_instruction/global_setting/world_lore 等），中文说明保持自然语言 | 高（已完成 v0.40.0） |
| 世界书 depth 注入 | LoreTable/WorldBook 统一注入，无深度分层 | 给 LorebookEntry 加 `depth` 字段：0=强效注入正文前，1=用户指令上方，2=系统上下文，3=背景设定(默认) | 高（已完成 v0.38.0） |
| 正则后处理管线 | 生成后直接输出，无后处理 | 新增 `post-processing` 管线：可配置 regex 列表（删除思维链、折叠远楼层、美化输出），在 `write/route.ts` 生成后调用 | 中（已并 v0.37.0 完成） |
| 角色卡结构化模板 | CharacterCard 已有 appearance/personality/background/speechPatterns/relationships 齐全 | 侦察确认 relationships(四路齐全) 与 speechPatterns(dialogueStyle 内置) 早已落地，无需补字段；仅修注入渲染：称呼补别名+外貌补穿着+关系读 dynamic | 低（已完成 v0.41.0） |
| 远楼层摘要压缩 | `contextKeepChapters` 截断 + 单章多小节直接 truncate 尾部 | 单章内超出 token 窗口的老小节（远楼层）自动 LLM 摘要成 `<summary>` 块替代原文注入，而非硬截断丢失信息 | 中（v0.39.0 已实现"标记式折叠"·LLM 摘要列为可选增强） |
| 记忆清除/上下文溢出治理（**知识库补遗项**） | 仅 slidingWindow 截断，无"隐藏/折叠远楼层"显式机制 | 在 assemblePrompt 中区分"常驻/召回/折叠"三层：depth≤2 常驻、关键词召回、远楼层折叠标记；显式标记防止模型误读丢失 | 高（已 v0.39.0 完成） |
| 预设类型扩展 | Preset type 仅 {table-template, story-progression, style, worldview, character} | 新增 `regex`、`lorebook`、`api-config` 类型，支持用户上传酒馆正则/世界书/API参数预设 | 高（已完成 v0.37.0） |
| 数据隔离标记 | 多项目通过 DB 隔离，上下文无显式标记 | 可选：在全局提示词中加 `[ACU-{projectId}]` 标记，强化项目边界 | 低 |

## 四、下一步执行计划（结合 IMA 知识库复核后重排）

**已完成**：v0.37.0 Preset 类型扩展、v0.38.0 世界书 depth 注入、v0.39.0 记忆清除/上下文溢出治理（标记式折叠）、v0.40.0 XML 标签分层包裹提示词结构升级。

**新排定推进序（按 ROI 与依赖关系）**：

1. **【已完成 v0.41.0】角色卡结构化模板收尾**：侦察确认 relationships 与 speechPatterns 早已齐全（无需补字段），仅修注入渲染三处——称呼补别名、外貌补穿着、关系读 dynamic（原误读空 notes）；tsc + next build 全通过。
2. **【可选·增强】远楼层 LLM 摘要（非阻塞）**：v0.39.0 已实现"标记式折叠"（明确边界、消除静默丢弃与误读隐患），酒馆式"LLM 摘要替代原文"需额外 LLM 调用、增加延迟与复杂度，列为可选增强，不阻塞主线。

**原则**：凡涉及 assemblePrompt 结构的（格式升级）集中处理，避免重复返工；regex 后处理（已落地）随 v0.40.0 格式升级已回归确认——作用于模型输出，与 prompt 格式无关，无需改动。

## 五、关联项

- **上游**：用户上传的酒馆 JSON（正则/世界书/API 参数）。
- **中游**：Preset 模型、创意工坊 UI、`/api/presets` 路由。
- **下游**：
  - `regex` 类型 → 新建 `postProcessingRules`（项目级）→ `write/route.ts` 生成后调用正则管线。
  - `lorebook` 类型 → LoreTable/WorldBook → `context-loader.ts` 召回注入。
  - `api-config` 类型 → AppSettings / 项目级覆盖 → 生成路由读取模型参数。
- **并行相关**：05 方法论中"正则后处理管线"和"世界书 depth 注入"可独立做，但建议等 preset 类型扩展落地后再做，避免数据入口缺失。

## 六、状态

【成功】Preset 类型扩展已实现并推送 v0.37.0（commit c7193ee）：
- schema 新增 Project.postProcessingRules；Preset.type 支持 regex / lorebook / api_config
- /api/presets/[id]/apply 新增三类应用逻辑
- 创意工坊 UI 新增三类 TAB/标签/上传占位
- write 路由接入 src/core/post-process/regex.ts 正则后处理管线
- 内置示范预设：通用·删除思维链、示范·世界书条目、示范·创意奔放 API 参数
- tsc + next build 全通过

后续候选（由 04-待迭代项承接）：XML+YAML+JSON 提示词格式升级、远楼层摘要压缩、角色卡结构化模板。

【成功】世界书 depth 注入已实现并推送 v0.38.0：
- schema 新增 LorebookEntry.depth（默认3）；类型层 LorebookEntry/PromptContext.forcedLore/LorebookData 同步
- 编排层 buildPromptContext 拆分 forced(depth≤2)/triggerable(depth≥3)，关键词匹配仅作用于 triggerable
- 组装引擎 assemblePrompt：depth2→系统上下文区、depth1→指令上方、depth0→正文前(指令下方)；loreSection 按 depth 升序
- 宝宝流 recall 排除 depth≤2；lorebook 新建/编辑 API 与 preset apply 写入 depth；世界书 UI 新增深度选择器+徽标
- 创意工坊示范预设「核心设定」改 depth=2 演示强制常驻注入
- tsc + 生产 next build 全通过

**2026-07-28 18:22 IMA 知识库「Tri的知识库」复核**：读取知识库目录（2 文件，核心为《酒馆零基础到精通完全解析.md》约1.5万字，另含无关 ima 使用指南）。该解析文档即 v0.37 迁移方案源文档，内容已纳入本方案。
- 对齐现状：7 迁移项已完成 2（Preset 类型扩展 v0.37.0 / 世界书 depth 注入 v0.38.0）；正则后处理已并入 v0.37.0；角色卡结构化基础字段（appearance/personality/background/speechPatterns）已具备，仅需补 relationships+speechReference，降为低优先级。
- **新增第 5 迁移项「记忆清除/上下文溢出治理（高）」**：知识库第七章"记忆机制"此前未单列，现补入，作为 depth 注入后的自然延伸主推项（v0.39.0）。
- 重排推进序：v0.39.0 记忆清除/上下文溢出治理(+远楼层摘要压缩) → v0.40.0 XML+YAML+JSON 提示词格式升级 → 角色卡结构化收尾(低)。

【成功】角色卡结构化模板收尾已实现并推送 v0.41.0：
- 侦察结论：relationships 字段（schema/types/UI/注入四路齐全）与 speechPatterns（dialogueStyle 内置子字段）此前已落地，本次零新增字段、零 schema 变更
- 注入渲染修复三处（orchestrator.ts 调度卡构建）：
  - 称呼完整化：卡头追加别名（aliases）
  - 穿着注入：外貌块补 attire（穿着）——UI 字段早已存在却一直未注入的遗漏修复
  - 关系备注修复：注入读 UI 实际保存的 r.dynamic（原误读恒为空的 r.notes，关系动态永远丢失）
- 未触碰表单重建点，静态自检 + 生产 next build 零回归
- 待迭代：可选·远楼层 LLM 摘要增强仍为非阻塞候选（见 04）
