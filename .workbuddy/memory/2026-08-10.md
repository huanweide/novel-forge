# 2026-08-10 工作记录

## novel-forge v1.8.6 真后台 AI 生成（#174）
- 主 Agent 亲自主刀（非马斯克自治），把 v1.8.4 预埋的 `GenerationTask` 模型从「待启用」接成真后台：新增 `src/core/storyline/generate.ts`（纯生成函数）+ `execute-task.ts`（后台执行器，状态机 running→done/failed）+ `generation-tasks` API（POST 创建 fire-and-forget / GET [id] 轮询）；前端 `StorylineWorkbench.tsx` 的「AI 生成」改为轮询模式并实时显示「生成中… X%」。
- 验证：tsc 0 错；vitest 43 文件 368/368 全绿（新增 3 项执行器单测）；端到端实跑星辰项目创建任务→服务端约 30s 跑通真实 LLM→done+4 条故事线建议、七要素齐全（本地 LLM 这次真调通，非 mock）。
- 升版 v1.8.6：同步 `changelog-data.ts` 三处 + `CHANGELOG.md` 头条（CRLF 兼容 Python 脚本 `tmp_fix_v186.py`）。
- 费曼报告：`PROCESS/WORK_REPORT-bgllm-v1.8.6-2026-08-10.md`。
- 取舍：真后台用「进程内 fire-and-forget」而非消息队列（dev/自托管稳定，serverless 不保证）；不自动落库（保留中间态编辑）；未做「重开自动恢复任务」为已知简化。

## novel-forge v1.8.8 双 AI 生成路径统一为真后台异步
- 马斯克视角自治 Agent（general-purpose-2）后台执行，主 Agent trust-but-verify 核实：把 v1.8.7 已知分叉（StorylineList 左栏同步 `/api/storylines/generate` ↔ StorylineWorkbench 异步 `/api/generation-tasks`）统一为唯一真后台异步链路——抽 `startPolling(taskId)` useCallback + 新增 `initialTaskId` prop 与挂载即轮询 useEffect；左栏改 POST `/api/generation-tasks` 拿 taskId 传工作台自动轮询。功能完全等价（列表生成→中间态编辑→采用并落库），同步阻塞路径彻底移除。
- 全站自查：删 CharacterList.tsx 三处 SSE 残留 console.log（git diff 3 行删除、Grep 0 匹配证实）；as any / SSE 重复解析列为诚实桥接后续项，未盲删。
- 双门禁主 Agent 亲跑：tsc 0 错；vitest 43 文件 368/368 全绿（与基线一致）。未碰 schema、未删功能、未 commit/push。
- 升版 v1.8.8：同步 `changelog-data.ts`（LATEST_VERSION/CHANGELOG_BRIEF/VERSIONS 首条）+ `CHANGELOG.md` 头条。
- 费曼报告：`PROCESS/WORK_REPORT-unify-dual-gen-path-v1.8.8-2026-08-10.md`。
- 清理：删除仓库根历史遗留临时脚本（_debug_*.cjs/_fix_*.py/tmp_*.cjs/shot.cjs），保留 shot2.cjs（截图工具）与 shots/（v1.8.7 截图证据）。

## novel-forge v1.8.9 马斯克检验后细节收口
- 主 Agent 亲自无头截图 + 亲自读图观察细节（13 页核心 UI + 交互态），据此收口真实摩擦：
  - `shot2.cjs` 动态读取 `LATEST_VERSION` 并预置 localStorage，默认关闭 onboarding / 首页更新公告（`novel-forge-last-version`）/ 快捷键速查（`nf-shortcuts-seen`）三类自动弹窗，保证截图验收稳定。
  - `ShortcutProvider.tsx` 移除首次进入工作台自动弹速查；`openHelp()` 与设置页入口保留。
  - `LeftPanel.tsx` 把「故事线」从「更多」收起菜单移出，与「大纲 / 角色 / 世界」并列左栏顶部，核心创作路径直接可见。
  - 修正 v1.8.8 日期为 2026-08-10；升版 v1.8.9，同步 `changelog-data.ts` 三处 + `CHANGELOG.md` 头条。
- 验证：tsc 0 错；vitest 43 文件 368/368 全绿；v2 截图确认首页/工作台无弹窗、故事线标签直接可见。
- 费曼报告：`PROCESS/WORK_REPORT-musk-inspection-v1.8.9-2026-08-10.md`。
- 取舍：只收敛首屏打扰与导航层级，不碰 LLM 生成逻辑；保留快捷键帮助的主动入口。
