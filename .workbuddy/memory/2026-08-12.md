
## 本地 novel-forge 项目版本记录核查（2026-08-12 21:20）
- 用户纠正：之前担心的"文空间被删"指**本地项目**而非 Neon 数据库。
- 本地 7 个副本目录全部存在，未被删。
- 版本记录(git)完整：
  - 主力工作区 2026-07-25-14-19-44/novel-forge：518 提交，分支 main，最新 v2.0.9（2026-08-12 19:15），远程 huanweide/novel-forge。即用户说的"2.0 版本"实际已迭代到 v2.0.9。
  - Projects/novel-forge：180 提交，v0.26.0（2026-07-26），master 分支。
  - 其他 WB 副本：v0.44.1（1 提交 shallow）、v0.26.0（14 提交 clone）、novelforge（xiaoxiao3315 基准，7 提交 v0.22.0）。
- 结论：本地 git 历史从未丢过"文空间"数据；小说记忆/数据存在 Neon，《新城·龙陨之地》完好，无需重建记忆。
- 本地 git 删除历史仅含代码文件（UI 组件 / apk 构建产物），无小说数据删除记录。
- 对齐：前序数据库软删 39 个垃圾项目（仅留《新城》）是按用户经 AskUserQuestion 确认的方案执行；本地项目本身完好无损。

## 掌握 novel-forge 项目全貌（2026-08-12 21:34）
- 用户目标："找到 2.0.1 版本源代码，让我知道项目全部情况"。已定位并通读核心架构。
- v2.0.1 = 提交 49635e0 (fix(digest): 摘要大修缮+...? v2.0.1)。仓库仅 v0.x 有 tag，v2.0.x 仅靠 commit message 标记、无 tag。
- 源码位置：主力工作区 2026-07-25-14-19-44/novel-forge（518 提交，最新 v2.0.9）。
- 技术栈：Next.js16 + React19 + TS + Tailwind4 + Prisma7 + Postgres(Neon) + Zustand + three.js(Nova 视觉) + vitest。dev/start 端口 3001。
- 核心数据模型(文空间底层)：Project(空间:globalPrompt/timelineDigest/storylineDigest/软删deletedAt/confirmedAt) + CharacterCard(+Revision回滚) + LorebookEntry(世界书) + LoreTable(宝宝流结构化表) + StoryNode(章节树+StoryNodeRevision版本快照) + StoryBranch + ChapterSummary(中期记忆) + ConsistencyFact/Conflict(一致性) + PendingCommitment/PendingItem(伏笔) + StoryBeat/Storyline + GameSession/GameState(游戏模式) + Rule(特异性冲突裁决) + StyleCard(文风量化) + GlobalPromptRevision(prompt当代码) + AppSettings(LLM Key) + DissectionTask/ImportTask/FillTask + Preset(创意工坊) + LlmCallLog(成本) + ChatSession(会话记忆)。
- 目录架构：src/{app(路由+api), core(agents/babylore/consistency/dissect/distillation/game/llm/pipeline/post-process...), lib(api/entity*/memory*/prisma...), components, store(zustand), hooks, generated(prisma client)}。

## round-17 分类标签治理收尾 + 推送通道修复（2026-08-12 续）
- 真身路径澄清：WorkBuddy 工作区 `C:\c\Users\Administrator\WorkBuddy\2026-07-25-14-19-44\novel-forge` 是**残缺镜像**（无 package.json/prisma/src/lib，非 git 仓库，仅含 .workbuddy/PROCESS/scripts/src测试）。所有改动必须在真身 `C:\Users\Administrator\WorkBuddy\2026-07-25-14-19-44\novel-forge`（v2.0.9→v2.0.11，git main HEAD ce976dd）执行。摘要记忆偏差根因在此——残缺镜像会让读文件/读报告全失败。
- round-17 分类标签 P0 三件套（F-01/F-02/F-03）已落地并上线：
  - F-01 世界分类 4 套 taxonomy 不通约 → 收敛为单一权威源 `world-category-classifier.ts`（WorldCategory 15 类 + ALL_WORLD_CATEGORIES + WORLD_CATEGORY_LABELS/SECTIONS 类型强制全覆盖）。
  - F-02 LORE_COLORS 缺 4 色 → 类型升为 `Record<WorldCategory,string>`，补全 character_relationship/fate_system/physics/public_system 4 色；图例 WORLD_LEGEND_CATS 改 derive 自 ALL_WORLD_CATEGORIES。
  - F-03 ChapterEntitiesPanel 吞 7 类 → buildGroups 遍历 ALL_WORLD_CATEGORIES 动态生成 15 组，仅兜底异常 category。
  - 改动文件：entity-highlighter.ts / ChapterEntitiesPanel.tsx / route.ts / changelog-data.ts / CHANGELOG.md。
  - 门禁：tsc 0 错误、vitest 513/513 全绿。
- 推送通道修复（本次关键突破）：全局强制 `http.proxy=127.0.0.1:7897`，代理对 git HTTPS 大包上传（git-receive-pack POST）静默中断 → push 128；纯直连 443 超时（沙箱出网受限，curl 的 200 是读 HTTPS_PROXY 环境变量走代理的假象）。改用 **SSH over 代理 TCP 隧道**绕过：
  - `connect.exe`（Git for Windows 自带 `C:/PROGRA~1/Git/mingw64/bin/connect.exe`）走 7897 HTTP 代理建到 `ssh.github.com:443` 的 CONNECT 隧道，密钥 `id_ed25519_huanweide`。
  - 临时 `url."git@gh-proxy:".insteadOf="https://github.com/"` + `GIT_SSH_COMMAND="ssh -F <临时config>"`，不改任何持久配置；代理只转发裸 TCP、不解析 git pack，绕开大包截断。
  - 推送成功：`700dda6..ce976dd main -> main`（v2.0.10 + v2.0.11 上线）。临时 config 用完即删。
- round-18 待办（用户"分类标签等"延伸，下轮做）：F-04 角色 role 3 处硬编码错标、F-05 genre 3 套列表不同步、flow-lens（续写静默交付 F1/F2、服务端自调用 origin 硬编码 F4）、ui-lens 其余（RefineDiffModal 无 focus-trap、虚拟滚动未接 WorldEntryList/CharacterList）。
- round-18 已完成并上线（v2.0.12，commit b657752，2026-08-12）：F-04 角色 role 单源治理 + F-05 题材 genre 单源对齐。
  - F-04：character-parse.ts 的 CHARACTER_ROLE_OPTIONS 补 comic_relief（对齐 CharacterRole 8 类：protagonist/antagonist/supporting/mentor/love_interest/catalyst/comic_relief/background）并派生 CHARACTER_ROLE_LABEL: Record<CharacterRole,string> 单一中文映射；修复 DissectDimensions/ImportWizard/workshop 三处硬编码错标（love_interest/catalyst/background/comic_relief 不再被标为「配角」），workshop 角色下拉遍历全集 8 类；CharacterList 的 roleOrder/roleLabel 与 CharacterFilters 筛选 chip 改从权威源派生。
  - F-05：explore/types.ts 的 GENRE_OPTIONS 改为以首页 GENRE_TEMPLATES 的 name 为基准并集补充（玄幻/奇幻/末世/游戏/军事），消除与首页选题分叉（西幻 vs 奇幻、缺玄幻/末世/游戏/军事）；genre 仍自由 string[] 未强枚举，不破坏导入。
  - 门禁：tsc 0 错、vitest 513/513 全绿。推送复用 round-17 的 SSH over 代理隧道方案（connect.exe → ssh.github.com:443，临时 config 用完即删）。
  - 仍待办（后续子轮）：flow-lens（续写静默交付 F1/F2、服务端自调用 origin 硬编码 F4）、ui-lens 其余（RefineDiffModal focus-trap、虚拟滚动未接 WorldEntryList/CharacterList）。
- round-18 flow-lens 四项错误修复收口（2026-08-12 续，真身 v2.0.12 → v2.0.13，经 SSH over 代理隧道推送，临时 config 用完即删）：
  - F1 续写截断保护：确诊续写路由根本丢弃 `chunk.type==="done"` 分支，`finishReason="length"` 被忽略、残缺章当 completed 交付。抽 `classifyTruncation(finishReason, contentLength, targetWords)` 单一真相（write/continue 共用，60% 字数阈值区分「被截断但尚可」与「明显不足」）；续写补 done 分支捕获 finishReason + 后处理前插入 length 保护块（回退 node `status="drafting"` + 下发 `truncated:true` + 告警 + `nextAction` 引导点「继续生成」补全）。
  - F2 续写对齐确认门：删续写路由无条件 `safeFillAfterWriting({source:"continue"})` 自动填表，填表统一归 `applyConfirm` 确认门（与 write 路径一致），消除 autoConfirm 双触发导致 lorebook 被续写顺手填空；done 事件 `nextAction` 改「请确认后回填记忆库」。
  - F3 草稿标记竞态：write/continue 两处草稿保存由 fire-and-forget `.then(落库)` 改 `await prisma.storyNode.update` 同步落库，删 `saving` 重入锁变量，杜绝草稿标记与后处理落库竞态导致 `[PARTIAL_DRAFT]` 串入正文。
  - F4 origin 硬编码死链：`triggerForeshadowDetect` 由 HTTP 自回环（fetch origin/api/... 硬编码 localhost:3001 + sleep 重试）改进程内直调 `detectPayoffs(projectId)`（去重锁防并发雪崩保留）；`storylines/[id]/route.ts` 主线缝合怪由 fetch 自调 `/api/storylines/generate` 改进程内直调 `runStorylineGeneration({projectId, mode:"newMain"})`（generate 核心逻辑抽为可导出 `runStorylineGeneration(bodyJson)`）；`confirm-guard.test.ts` 断言由 fetchMock 改 `detectPayoffs` mock。
  - 门禁：tsc 0 错、vitest 60 文件 514/514 全绿（新增 `finish-reason.test.ts` 4 用例）。无 schema 迁移、无新依赖。
  - 改动文件：finish-reason.ts(新)/finish-reason.test.ts(新)/continue/route.ts/write-generation.ts/confirm-guard.ts/confirm-guard.test.ts/storylines/[id]/route.ts/storylines/generate/route.ts/changelog-data.ts/CHANGELOG.md。
  - 仍待办（后续子轮）：ui-lens 其余（RefineDiffModal 无 focus-trap、虚拟滚动未接 WorldEntryList/CharacterList）。

## round-19 写作右侧栏/大纲/角色去重 UI 深度修复（2026-08-12 续，v2.0.14 + v2.0.15）
- v2.0.14（commit d2cba8a，已 push）：右侧检测栏关闭后无法拉起 → RightPanel `minimized` 提升为父页面 props 常驻渲染，竖条随时拉回，与左栏互斥，transition-all 平滑过渡；角色栏筛选徽章 4 种激活色统一为 bg-[var(--nv-primary)] + 新建标签/打标移入 CharacterToolbar 复用 base 样式消除大小字体不一；大纲按钮改后台运行（进度胶囊 + 关窗继续 + 完成自动重开预览 + Dialog 内提示）。门禁 tsc 0 错、vitest 514/514 全绿。
- v2.0.15（commit 8f9a2e4，已 push）：去重漏检根因=规则组要求变体解析到集合内全名正主（脏卡无正主被跳过）+ LLM 因背景稀疏/同姓警示保守不归组 → 误报「全部干净」。修复：新增 coreTokenOf（去尊称/后缀/描述词提取稳定核心名）+ looseTokenGroups（核心名相同归组，覆盖韩先生/韩姓男子、迭戈三兄弟）+ mergeOverlappingGroups（LLM∪规则∪宽松三路并集、共享 id 归并）；置信度=有普通全名正主→high 自动合并，脏卡互相→low 进 pending 确认 UI（MergePendingPanel 确认/忽略/回滚）；前端弹窗显示 pending 提案并修正「全部干净」误判，来源标识加「宽松判定」；entity-auto-creator 自动发现新角色后 fire-and-forget 后台去重（动态 import 避免与 character-dedupe 循环依赖）。门禁 tsc 0 错、vitest 514/514 全绿（computeConfidence 8 用例覆盖林惊羽/林惊雨→low、韩立+韩先生→high 等边界）。
- 推送复用 SSH over 代理隧道（connect.exe → ssh.github.com:443，临时 ssh config 写完即删）；远程已恢复 https://github.com/huanweide/novel-forge.git。
- 仍待办（用户截图提及，本轮未做）：CharacterRow 待审对勾 hover 才显示（设计如此，非 bug，可保持）；ui-lens 其余（RefineDiffModal focus-trap、虚拟滚动）。
- round-20 ui-lens 收尾（v2.0.16，commit 1485ba8，已 push）：RefineDiffModal 接入已有 useFocusTrap hook（面板 ref + tabIndex=-1，Esc 关闭 / Tab 循环 / 关闭后焦点交还），修复键盘焦点逃逸 a11y 缺陷；WorldEntryCard/CharacterRow 包 React.memo，CharacterList 把 toggleSelect/handleConfirm/onDelete/onConfirm/onTagClick 改 useCallback 稳定化（toggleSelect 改函数式更新），让 memo 真正生效（搜索输入、去重结果弹窗等父级 state 变化时未变卡片跳过重渲染）；虚拟滚动经评估暂缓（单项目角色/世界设定条目通常几十~几百条，普通 map 渲染足够，盲目引入 windowing 库属过度优化且增新依赖，留待数千+条目真实大数据量场景再接）。门禁 tsc 0 错、vitest 514/514 全绿。推送复用 SSH over 代理隧道（connect.exe 在 mingw64/bin，经 7897 → ssh.github.com:443，临时 config 用完即删）；远程已恢复 https。
