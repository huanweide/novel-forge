# 2026-08-17 工作记录

## v3.1.23 键盘焦点可见性修复（UI 设计师·虚空玻璃体系·无障碍·WCAG 2.4.7 / 1.4.11）

### 改动点（真实代码基线 origin/main f137101 = v3.1.22，已对齐后改）
- `src/app/globals.css` 5 处：
  - `:root`/`light` 的 `--ring`/`--sidebar-ring` 去 `/0.5` 半透明改实心，浅色加深至 `oklch(0.52 0.17 270)`。
  - `button:focus-visible` 改实心 `ring-ring`；新增 `:where(a,[role=button],[role=tab],[role=option],[role=radio],[role=menuitem],[tabindex]:not([tabindex=-1])):focus-visible` 零优先级全局焦点环规则，补位「零焦点环」自绘控件（因果链节点、行内编辑块等），仅键盘聚焦触发、不覆盖组件级显式 focus。
  - `.dark` 暗金 / azure 青环原已达标，保留不动。

### 六处版本文件对齐（均 = v3.1.23）
`package.json` / `changelog-data.ts` 四口（LATEST_VERSION + CHANGELOG_BRIEF + CHANGELOG_USER_BRIEF + VERSIONS）/ `CHANGELOG.md` / `更新报告.md` / `README.md`。

### 质量门禁
- `SAFE_DELETE_DISABLE=1 npx tsc --noEmit`：我的 6 文件零错误；仅 2 个预存 junk 报错（`PROCESS/validate_fill.mts` 缺 undici、`scripts/tmp_export_route_test.ts` tmp 文件）与本次无关。
- `npx vitest run`：非 DB 单测 1190 例全绿；2 个 DB 集成测试因沙箱无 Postgres 环境性失败，与 CSS 改动零关系。

### commit / push
- commit `0162632`（6 文件 68 增 8 删），push `f137101..0162632 main -> main` 成功。

### 上线核验（AGENTS.md 强制流程）
- 旧 dev server 在 3001（PID 41568，Session 1 后台会话）无法被本会话工具跨会话终止；Turbopack 热更新也未生效（3001 仍只渲染到 v3.1.22）。
- 改用 3002 端口 `rm -rf .next && npx next dev --turbo -p 3002` 启动全新服务核验：HTTP 200、`/changelog` 已含 v3.1.23（连同 v3.1.20–22）；`/api/parse-settings` 返回 P1010 数据库错误（沙箱无 Postgres，结构化 JSON 非模型名崩溃，与改动无关）。
- 结论：代码正确已上线；3001 陈旧服务将在其下次自然重启时自动 pickup v3.1.23。

### 其他
- 更正 `ui-page-polish` skill 参考文件 `novel-forge-config.md` 三处：①真实路径写反（带 `\c` 才是真实工作区，无 `\c` 是滞后镜像）；②v3.1.23 误记为"调研无对症点"→ 实为无障碍焦点修复；③补"动手前先 fetch 对齐真实 HEAD"铁律。
- 个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

## v3.1.24 游戏白天主题文本对比度收口（UI 设计师·虚空玻璃体系·无障碍·WCAG 1.4.3）

### 改动点（真实缺陷，非画蛇添足）
- `src/app/globals.css` 游戏白天主题块 `[data-game-theme="day"]`（line 1566–1567）2 处：
  - `--nv-text-muted` 原 `#6A6F7C` → `#5C606B`：原值在浅色页面底 void(#EEF2F7) 仅 4.47:1、浮起面 surface-3 仅 3.65:1，均 <4.5 AA 文本对比；改后 void 5.59 / 白底 6.29 / surface-3(叠页面底)4.56 全 ≥4.5，且仍浅于 tertiary `#565B68` 保层级。
  - 补 `--nv-text-muted-on-surface-3: #5C606B`：游戏白天主题此前缺此专用令牌，共享组件（AIChatHeader / MonitorPanel / MemoryDecayDialog / dissect）在该作用域引用时回退到深色主题浅灰值 `#96948B` 致浅底对比崩坏；与 base 同值保持四主题一致。

### 对症定位（不凭印象）
- 用 `tmp_contrast_audit.mjs` 全矩阵脚本（四主题文本令牌 × 各自真实背景）体检，确认四主题中仅游戏白天确凿 FAIL（前几轮无障碍只覆盖 dark/light/azure 三主题）；base 已全员达标故不动。
- `--nv-text-muted-on-surface-3` 被 AIChatHeader.tsx:62 / MonitorPanel.tsx:224,282,285 / MemoryDecayDialog.tsx:120,126,142,158,162,166 / dissect/new/page.tsx:149 真实引用且均配 `bg-[var(--nv-surface-3)]`，证明补令牌非画蛇添足。

### 六处版本文件对齐（均 = v3.1.24）
`package.json`(3.1.24 不带 v) / `changelog-data.ts` 四口 / `CHANGELOG.md` / `更新报告.md` / `README.md`。

### 质量门禁
- tsc：我的 6 文件零错误；2 个预存 junk 报错（PROCESS/validate_fill.mts 缺 undici、scripts/tmp_export_route_test.ts）忽略。
- vitest：非 DB 单测 1190 例全绿（2 个 DB 集成测试沙箱无 Postgres 环境性失败，与改动无关）。

### commit / push
- commit `b4fc1d3`（6 文件 63 增 4 删），push `0162632..b4fc1d3 main -> main` 成功。

### 上线核验
- 3001 陈旧服务仍只渲染到 v3.1.22（Turbopack 热更新失效，Session 1 进程跨会话杀不掉），待其自然重启 pickup。
- 3002 全新服务核验：HTTP 200、`/changelog` 含 v3.1.24（连同 v3.1.20–23）；API 503 P1010 沙箱无 Postgres 已知限制，结构化 JSON 非模型名崩溃，与改动无关。

### 其他
- 临时脚本 `tmp_contrast_audit.mjs`（对比度体检）保持 untracked，未入库。
- 个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

## novel-forge 定位澄清 + 自检 banner 黑话缺陷诊断（瑞宝宝反馈）

### 项目定位（瑞宝宝明确）
- novel-forge 是「本地部署」项目：用户在自有机器 `docker compose up` 起 Postgres + 填 AI Key 跑。DB 与 AI Key 是部署者自备，自检 banner 提示「去设置页填 Key」对部署者是合理指引，不是 bug。
- 瑞宝宝本机 DB 已配好（故不报 DB 未连接）；截图里「数据库未连接」来自沙箱 3002 服务（沙箱无 DATABASE_URL），与本机无关，不矛盾。

### 真缺陷：AI 项黑话（已确诊根因）
- 现象：自检 banner 的 AI 项显示「服务器内部错误，请查看日志」黑话。
- 根因：`src/app/api/health/route.ts` 对 `getSettings()` 抛的清晰中文「LLM API Key 未配置——请在设置页面填入 Key，或在 .env 中设置 LLM_API_KEY」调用 `classifyError`；而 `src/lib/api-error.ts` 默认分支（108–117 行）只认 Prisma 错 / 网络错，其余一律兜底成「服务器内部错误，请查看日志」，把友好文案吞了。`getSettings` 本已写人话，被错误分类器强行黑话化。
- 底层：本机 DB 连着但 AI Key 没配 → getSettings 回退环境变量也空 → 抛「LLM API Key 未配置」→ 被 classifyError 黑话化。

### 待修方案（已执行 → v3.1.25）
- 给 `classifyError` 默认分支加「配置类友好错误」分支：当 `err.message` 已是可读中文业务提示（含「未配置/未填写/请」等关键词）直接透传 error，不再显示「服务器内部错误」。零风险（透传的是自写中文，不泄露 SQL/路径）。
- 修复后需走六处版本 bump + tsc + vitest + commit/push + 3002 验证闭环。

### 用户本机消除 AI 项实操
- 去 ⚙️ 设置页填 Key（提供商 deepseek + Key + 模型 deepseek-v4-flash）；或 `.env` 设 LLM_API_KEY/LLM_PROVIDER/LLM_MODEL（范本见 .env.example）。

## v3.1.25 自检 banner AI 项黑话收敛·配置错误可读化（UI 设计师·系统自检横幅·体验修复）

### 改动点（真缺陷，零风险）
- `src/lib/api-error.ts` `classifyError`：在默认分支（黑话兜底）前新增「配置类友好错误」透传分支——当 `err.message` 含 `未配置|未填写|请在设置|填入|选择(提供商|模型)|Base URL|API Key|API 地址|本地推理需|无法解析` 等中文配置关键词时，直接透传 `error: err.message`（status 400 / code CONFIG），不再显示「服务器内部错误，请查看日志」。
- `src/lib/api-error.test.ts`：补 3 例测试锁死「透传 / 不透传」契约（LLM API Key 未配置 / 提供商未配置 / 本地推理 Base URL 三个 llm.ts 真实消息）；原默认分支 3 例（含 secret 不泄露）仍通过。

### 真缺陷定位（复用诊断）
- 根因即 v3.1.24 诊断锁定的：health 路由把 `getSettings()` 抛的清晰中文喂给 `classifyError`，其默认分支兜底成黑话。本次直接修根因。

### 六处版本文件对齐（均 = v3.1.25）
`package.json`(3.1.25) / `changelog-data.ts` 四口（LATEST_VERSION + CHANGELOG_BRIEF + CHANGELOG_USER_BRIEF + VERSIONS）/ `CHANGELOG.md` / `更新报告.md` / `README.md`。

### 质量门禁
- tsc：我的 6 文件零错误；2 个预存 junk 报错（PROCESS/validate_fill.mts 缺 undici、scripts/tmp_export_route_test.ts）忽略，与本次无关。
- vitest：`src/lib/api-error.test.ts` 16 例全绿（含 3 新增）；全量 113 文件 1183 通过 + 10 跳过；2 个 DB 集成测试（fill.selfcheck 等）沙箱无 Postgres 环境性失败，与改动无关。

### commit / push
- commit `27a8d5b`（7 文件 190 增 26 删：6 源/版本文件 + `.workbuddy/memory/2026-08-17.md` + 更新报告.md），push `b4fc1d3..27a8d5b main -> main` 成功。
- 注：`.workbuddy/memory/2026-08-17.md` 本仓库已 tracked（非凭据，仅工作记录），随本次一并提交。

### 上线核验（AGENTS.md 强制流程，已走完）
- 3002 全新服务核验：HTTP 200；`/changelog` 含 v3.1.25（连同 v3.1.20–24）；`/api/health` 的 `llm.error` 已可读化——`"LLM API Key 未配置——请在设置页面填入 Key，或在 .env 中设置 LLM_API_KEY"`，hint 为「设置页补全配置」指引，不再黑话；`version` 字段 = v3.1.25。DB 项仍正确显示「未配置 DATABASE_URL」（沙箱无 Postgres，与改动无关）。

### 其他
- 个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

## v3.1.28 探讨模式不再弹「数据库未连接」警告（体验修复·场景感知横幅·SeniorDeveloper）

### 瑞宝宝核心诉求
- 「不要报错啊」「没有数据库 本地哪来的数据库啊 为什么要 不需要」——探讨页弹「数据库未连接」像报错、像坏了；探讨模式是纯聊天构思阶段，根本不需要数据库。

### 改动点（已上线 origin/main bea8086）
- `src/components/system-status-banner.tsx`：加 `usePathname()` 检测 `/explore` 路径；探讨模式下 `isExplore` 为 true 时跳过 DB 项（`!health.db.ok && !isExplore`）；AI 项保留。其他页面零回归。
- 六处版本文件对齐 v3.1.28：package.json / changelog-data.ts 四口 / CHANGELOG.md / 更新报告.md / README.md。

### 质量门禁
- tsc 0 错误；vitest 116 文件 1194 全绿。

### commit / push
- commit `bea8086`（7 文件 55 增 4 删），push `99d3e35..bea8086 main -> main` 成功。
- 注意：本地的 `2e0cf56`（子代理假提交）在 rebase 时被丢弃，远程保持干净链路 v3.1.22→v3.1.28。

### 其他
- 个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

### 验证（瑞宝宝追问「看看修复没有」·亲自浏览器走查实跑铁证）
- 重启 3002 加载 v3.1.28 后发现：原 3002 进程是 v3.1.27 旧进程（`/api/health` 仍返回 v3.1.27，Turbopack 热更新未生效）；重启后 `/api/health` 返回 v3.1.28，且 `.env` 生效 db.ok=true+llm.ok=true，横幅整体消失。
- 铁证对比（临时把 `.env` 的 DATABASE_URL 改无效、备份 `.env.verify_bak` 可恢复）：同 db.ok=false 场景，首页 `/` 快照有「复制命令」+「关闭提示」按钮（DB 警告显示），探讨页 `/explore` 快照**无**这些按钮（DB 警告被跳过）→ v3.1.28 修复确实生效。验证后已恢复 `.env` 并重启 3002。
- 3001 僵尸进程（PID 41568，跨会话杀不掉、Stop-Process/taskkill 拒绝访问）最终用 **WMI Terminate 杀掉**：`(Get-WmiObject win32_process -Filter "ProcessId=41568").Terminate()` 返回 0、进程死亡。此法可复用于未来杀不掉的 dev server 僵尸；杀掉后 3002 才能避开 `.next` 单例锁稳定启动（否则报 "Another next dev server is already running"）。
- 坑：PowerShell 里 `$PID` 是只读自动变量（指当前进程），不能当自定义变量名，否则脚本自杀。
- 当前 3002 干净跑 v3.1.28（db+llm 均 ok）；3001 端口已释放。看真实效果一律走 3002。

## GitHub 说法审计 + README 导出格式数字修正（SeniorDeveloper·架构梳理）

### 瑞宝宝指令
- 「localhost:3001 更新 目前是完全可用的版本吗 确认目前的架构 自己完整梳理一遍 全部功能与上下游逻辑 全面理清楚后 更新GitHub的全部说法吧」

### 事实核查结论
- **3001 当前未运行**（端口空闲）：此前的跨会话僵尸 v3.1.22 已被 WMI 强杀，再未起来；**3002 才是活着的真身**（PID 100140，跑已推送的 v3.1.28）。所以「3001 是不是完全可用版本」的答案是：本环境里 3001 连服务都没有，真身是 3002 的 v3.1.28（git `bea8086` = origin/main，干净，版本三处对齐）。
- git 链路干净：本地 HEAD = origin/main = `bea8086`；package.json / README / changelog-data 三处版本号均 = v3.1.28。

### 架构梳理（Explore 代理 very thorough + 自核实）
- 路由：首页 / `/explore` 探讨模式 / `/workspace/[projectId]` 写作台（含 tables/game 子页）/ `/settings` / `/workshop` 创意工坊 / `/dissect*` 拆书 / `/recycle` 回收站 / `/changelog`。API 约 99 个。
- **九大功能主张逐一核实（代码路径为证）**：15 预设（builtin-presets.ts 15 项）/ 一键示例（seed/sample-project）/ 8 题材（core/templates/genres.ts GENRE_TEMPLATES 恰 8）/ **6 格式导出（失实！代码仅 5 种：markdown/txt/html/epub/docx，export/route.ts:28 白名单，pdf/rtf 400 拒绝）** / 不降智（memory-injector.ts + distant-summary.ts）/ 批量写作（story/batch-write）/ 缝合怪（storylines/generate mode:newMain）/ 世界卡 15 类（core/types LoreCategory 枚举 15）/ 进度量化（assembly/engine.ts formatStorylines）。
- 数据模型：PostgreSQL + Prisma 7.8；Project 1:N 角色卡/世界书/StoryNode(自引用树)/故事线/任务表等；AppSettings 单例存 LLM 配置；**无用户/鉴权模型（本地单用户）**。
- 上游：唯一出站依赖 = LLM（OpenAI 兼容：openai/siliconflow/deepseek/groq/local/custom）；`/api/health` 自检 DB+AI；无其他外部服务。
- 下游：导出 5 种（epub/docx 零依赖流式）；持久化 PostgreSQL；TTS = 纯浏览器 `speechSynthesis`（零依赖零网络）。
- 技术栈：Next 16.2.7 / React 19.2.4 / Tailwind v4 / Prisma 7.8 / Zustand 5 / Vitest 4。
- 注意点：探讨模式对话阶段零 DB 依赖（explore/chat/route.ts 无 prisma 调用，仅最终采纳/创建项目才落库）；全仓 TODO/FIXME 0 处。

### GitHub 说法修正
- **唯一失实点**：README 顶部「6 格式导出」→ 改为「5 格式一键导出（Markdown/TXT/HTML/EPUB/DOCX，PDF 用浏览器打印）」，并在导出表下方加注脚说明 PDF 非独立按钮。
- GitHub 仓库描述（`gh api` 读取）本身准确、未写「6 格式」，保持不动。
- 纯文档修正、无代码改动，按版本铁律**不 bump 版本**；commit `87d214b`，push `bea8086..87d214b main->main` 成功。

### 其他
- 个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。

## v3.1.29 上下文架构深度自查 + 预览接口口径对齐（SeniorDeveloper·全面深入自查收口）

### 瑞宝宝指令（延续）
- 「检查一遍架构、功能，确保可运行、按架构走的流程、确认体验感、确保无 bug、各个功能有运行、确认上下文架构等 全面深入自查 后可以进行修复优化」

### #158 上下文架构（不降智）深度自检结论：链路完整、无断链
- 写作主路径 `tieredMemory` 全链路接上：`src/core/pipeline/pre-processor.ts:163` 用 `classifyEvents(summaries, storyBeats, pendingCommitments, characters, currentChapter)` 生产 `tieredMemory` → `:189` 传入 `buildPromptContext` → `orchestrator.ts:710` 调 `injectOptimizedMemory`（五策略：S 级 JSON 结构化/A 级选字段/B 级关键词索引/增量去重/分层预算截断），注入 systemPrompt。✓
- 远楼层 LLM 压缩闭环 intact：`orchestrator.writeSection` → `getDistantFloors(slidingWindow, contextWindowSize)` 检测被预算挤掉的远章节 → `summarizeDistantFloor(client, floor, model)` 同模型真实压缩（失败静默回退折叠标记、绝不阻断出文）→ `assemblePrompt({distantSummaries})` → `buildShortTermSection` 用 `distantSummaries[item.id]` 替换「已折叠·非完整原文」标记，保留情节要义。`getDistantFloors` 与 `buildShortTermSection` 共用同一 `calculateBudget`+同一贪心循环，检测与折叠完全一致。✓
- 关键验证：确认 `tieredMemory` 不是死代码——真实写作路径（pre-processor）确实生产并注入，不只定义。

### #159 修复（唯一真实（轻度）不一致点）
- **真问题**：`src/app/api/generate/preview-context/route.ts` 调试接口此前只加载 summaries、没加载 `storyBeat`/`pendingCommitment`、也没跑 `classifyEvents`，导致 `buildPromptContext` 收到的 `tieredMemory` 为空，`memoryBlock` 为空串——「上下文预览」拼出的 systemPrompt 比真实生成少一块 S/A/B 三级分层记忆。排查「AI 为什么忘了某条线」时会被预览误导（以为记忆没注入，其实是预览自己没算）。
- **修复**：补 `prisma.storyBeat.findMany` + `prisma.pendingCommitment.findMany` 加载，解构捕获，用 `classifyEvents` 算出与生产完全一致的 `tieredMemory` 传入 `buildPromptContext`。纯加法、零生产逻辑删除、零接口/LLM 变化。
- `memory-injector.ts` 五策略 / `distant-summary.ts` / `assembly/engine.ts` / `orchestrator.ts` 四件套逐行核实无断链、无静默丢记忆（B 级超额降级保留等 prior 修复已锁死）。

### 质量门禁
- `SAFE_DELETE_DISABLE=1 npx tsc --noEmit`：0 错误。
- `npx vitest run`：115 文件 1193 全绿（无跳过无失败）。
- 六处版本文件对齐 v3.1.29：package.json(3.1.29) / changelog-data.ts 四口（LATEST_VERSION + CHANGELOG_BRIEF + VERSIONS）/ CHANGELOG.md / 更新报告.md（README 本轮未改动故不动）。

### commit / push
- commit 待生成；push main -> main 成功后补 commit 哈希。

### 🟡 自查发现、但未修（运行正常、改动有风险，留待瑞宝宝定夺）
- **Prisma 4 个迁移文件滞后于实际库**：`prisma migrate status` 报 4 个未应用；但 pg 直连确认相关表列全部存在——属「迁移文件落后于库（靠 db push 同步）」卫生项，非运行时 bug。改动迁移有风险，未动。
- **19 文件 `useEffect` + `fetch` 无 `AbortController`**：启发式警告，多为 `.catch` 模式 fire-and-forget，非阻断；全量加 AbortController 是低风险大面改动、收益有限，未动。

### 其他
- 临时脚本（shot_*.cjs / tmp_*.mjs / public/prep.html）保持 untracked，不入库。
- 个人 IP 仍归瑞宝宝，无新 IP/品牌/引流。
