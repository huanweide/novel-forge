# 2026-08-11 工作日志

## v1.8.25 — 自动情节化（Task #265 落地）~19:36
- 路线图 #2 自动情节化：抽取关键事件一键归纳进故事线。
- 三处改动：①`src/core/pipeline/plot-event.ts` 纯函数 `computePlotEventAdoptions` + 7 单测；②`apply-extraction/route.ts` 新增 plotEvents 落库分支（找活跃主线→查现有事件/最大 position→纯函数算清单→逐条 create，kind=EVENT、role=null、sourceRefs 记来源章节）；③`PostGenPanel` 新增常显「情节」Tab（`PlotTab.tsx`）。
- 零 schema 变更（复用 StorylineEvent 现有字段）。
- 双门禁：tsc 0 错 + vitest 50 文件 437/437 全绿。
- 真实 星辰 库后端集成核验（建 3 事件 position 1/2/3、sourceRefs 含章节；二次去重仍 3 条；已清理）+ 无头冒烟零控制台报错。
- 已知未做：live 生成会话端到端点击（情节 Tab 勾选→保存）未走通——服务端 LLM 调用 Playwright 拦不到，核心落库已被真实库集成测试覆盖，留上线人工点验。
- 版本化：changelog-data.ts→v1.8.25、CHANGELOG.md 顶部加 v1.8.25；费曼报告 PROCESS/WORK_REPORT-auto-plot-v1825-2026-08-11.md。
- 收口：重建缺失的 MEMORY.md / 当日日志；git commit + push origin/main（连同 prior 遗漏的 v1.8.15–1.8.24 系列 WORK_REPORT 一并补登，临时脚本保持 untracked）。

## 待排期（v1.9 路线图）
- #3 角色对话/附身、#4 文风定制 Tab、#5 内容安全审核。
- 「推进/试探墙」「投票」缺设计规格未落地。
- Task #253 aixiaoshuojia.cn 调研仍阻塞（需登录凭据/cookie）。
