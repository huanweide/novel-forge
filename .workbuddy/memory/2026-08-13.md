# 2026-08-13 工作日志

## round-21 v2.0.17 检验并优化（用户指令「继续 检验并且优化」）

### 检验基线
- `SAFE_DELETE_DISABLE=1 npx tsc --noEmit` 0 错误；`npx vitest run` 60 文件 514/514 全绿（含未跟踪的 prompt-eval.test.ts 5 例、rollback/route.test.ts 6 例均被收集通过）。

### 静态巡检发现
- 高频噪声日志（后台任务每触发都刷屏）：
  - `src/core/sync-global-prompt.ts`：每次角色/世界/风格变化打印成功 console.log。
  - `src/core/babylore/fill.ts`：每次 LLM 调用（含 3 次重试）打印 attempt/http/raw_len/finish 调试日志。
  - `src/core/babylore/loop.ts`：每章填表打印结果汇总 console.log（信息已 SSE 到前端）。
- 游离 git 外的守护型代码（技术债）：
  - `src/core/prompt-eval.ts` + `prompt-eval.test.ts`：#320「prompt 当代码」评测集（固定 fixture + evaluatePromptVersions 要素守护纯函数），类型/测试健康、功能完整。
  - `src/app/api/projects/[id]/prompt-revisions/rollback/route.ts` + `route.test.ts`：#319 prompt 版本回滚 API，mock 测试覆盖正常+4 类边界，功能完整；前端暂无调用入口（纯后端闭环）。

### 优化（v2.0.17）
- 日志治理：删 sync 成功 log、删 fill LLM 调试 log（失败降级 console.warn）、删 loop 填表汇总 log；保留 console.error 与一次性导入/回写流程日志（dissect/engine.ts、pipeline/plan-chapter.ts 因低频且对排查有用而保留）。
- 守护代码入库：prompt-eval.ts(+test) 与 rollback/route.ts(+test) 正式 git add 纳入版本控制。
- 同步 `src/lib/changelog-data.ts` / `CHANGELOG.md` 至 v2.0.17。

### 验证与上线
- 门禁：tsc 0 错、vitest 514/514 全绿。
- 提交 `53e2838`（9 文件 +438/-7），经 SSH-over-代理(7897)隧道推送 `2250377..53e2838 main -> main` 成功；remote 恢复 https，临时 ssh conf 用完即删。

### 仍待办 / 下一轮可切入
- `any` 类型债务（`tool-registry.ts`/`explore/page.tsx`/`store/index.ts`/`assembly/*`）收紧——高风险改动面大，需谨慎单点推进。
- 回滚 API 前端 UI 入口（rollback 暂无前端调用）。
- 虚拟滚动（数千+条目真实大数据量场景再接）。

## round-22 v2.0.18 推送收尾（2026-08-13 续，部署阻塞解除）

### 阻塞背景
- v2.0.18（commit `5878d13`，10 文件 +326/-49）代码/单测/门禁/版本号全部就绪并已本地 commit，但 round-22 当时 `git push ghssh main`（SSH-over-代理 7897 → ssh.github.com:443 隧道）失败：`Connection closed by UNKNOWN port 65535`。
- 当时诊断：curl 探测所有候选代理端口（7890-7899/1080/8080）的 HTTP 与 SOCKS5 模式到 `https://ssh.github.com:443` 全 `http_code=000`；无 proxy 环境变量 → 判定代理环境变化、无法通达 GitHub，v2.0.18 滞留本地。

### 解除（本轮）
- **诊断纠偏**：用 bash `/dev/tcp` 重扫，发现 7897 其实 LISTEN（之前 PowerShell `Test-NetConnection ... 2>$null` 因 `$null` 在 bash 下非语法被误判 DOWN）；且**直连 `ssh.github.com:443` 与 `github.com:22` 端口均 OK** → 根本无需代理隧道，可直接 SSH 直连。
- **改用 SSH 直连推送**：`git remote add ghssh git@github.com:huanweide/novel-forge.git`，`GIT_SSH_COMMAND="ssh -i $HOME/.ssh/id_ed25519_huanweide -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o ConnectTimeout=20" git push ghssh main` → 成功 `251fb17..5878d13 main -> main`（exit 0）。
- 推送后 `git remote remove ghssh`，origin 保持 `https://github.com/huanweide/novel-forge.git`。

### 方法论更新（重要，未来复用）
- **HTTPS 通道 TLS 直连失败**：`git fetch origin` 报 `TLS connect error: unexpected eof while reading`；但 **SSH 22 端口直连正常**。说明当前环境 GitHub 仅放行 22/443 的 SSH 握手，HTTPS 直连被中间设备截断。
- **未来推送优先用直连 SSH**（不再依赖代理隧道）：`GIT_SSH_COMMAND` 包装 `ssh -i ~/.ssh/id_ed25519_huanweide` 直连 `git@github.com:huanweide/novel-forge.git`（临时 ghssh remote 或改 origin 为 ssh 均可），push 完恢复 https origin。代理隧道 7897 现在不稳定，不可作为默认路径。
- 密钥 `id_ed25519_huanweide` 认证正常（`Hi huanweide! You've successfully authenticated`）。

### 线上验证待办（任务 #32 线上部分）
- 代码层：别名合并/两变体互并/含·不合并/大纲判断已被 15 例单测覆盖（vitest 529/529 全绿），逻辑就绪。
- **线上脏卡需手动触发清理**：新城项目线上 DB 仍存有 round-22 之前的脏卡（韩姓男子/韩先生/迭戈先生/迭戈/迭戈·美第奇 🆕 自动发现）。自动发现实时合并只对新发现的角色生效，已存在的脏卡需用户**在线上手动点一次「自动去重合并」**来合并（韩姓男子+韩先生→合并，迭戈+迭戈先生→合并；迭戈·美第奇含·保持独立马甲）。本轮未自动清理（避免无确认改动线上数据）。

## round-22 续 v2.0.19 存量角色自动检测提示（2026-08-13，已 push commit 818dab6）

### 改动
- `src/core/character-dedupe.ts`：`dedupeCharacters(projectId, { detectOnly })` 新增开关——detectOnly 时只分组（mergedGroups/pendingGroups）**不写库、不 applyMerge**；顺带把内部 `interface CharLite` 补 `export`（修复 round-22 测试文件对未导出类型的导入，该 TS 错误被 vitest 的 esbuild 不查类型掩盖）。
- `src/app/api/characters/dedupe/route.ts`：透传 `{ projectId, detectOnly }`。
- `src/components/workspace/CharacterList.tsx`：`useEffect([projectId])` + `useRef` 记已检测项目，加载/切换项目时 fire-and-forget 调 detectOnly；结果存 `dedupeHint`；角色栏顶部非阻塞 banner 提示「发现 N 个可能为同一人的角色，点击一键清理」，点击即调现有 `handleDedupe()` 全量合并。
- `changelog-data.ts` + `CHANGELOG.md` 升 v2.0.19 同步。

### 门禁与上线
- tsc 0 错；vitest 62 文件 529/529 全绿。直连 SSH 推送 `0f53f04..818dab6 main -> main`，origin 保持 https。

### 设计取舍（重要）
- **只提示不自动合并**：detectOnly 不写库，保留用户对低置信组的确认权（避免加载即误并线上数据）。自动发现实时合并仍只对新角色生效，本功能补上存量脏卡场景。
- **验证局限**：本地无法直连线上 Neon DB（.env DATABASE_URL 为占位模板 + 白名单），未做线上真实验证；逻辑由 round-22 单测覆盖，detectOnly 零数据风险。

## round-22 续 v2.0.20 高置信组加载时静默自动合并（2026-08-13，已 push）

### 改动
- `src/core/character-dedupe.ts`：`dedupeCharacters` 的 detectOnly 分支取消 high 的"跳过合并"——置信度 high 时无论 detectOnly 与否都 `applyMerge` + 写 `applied` 快照（可回滚）；low 仍只 pending 提示、不写库不合并。
- `src/components/workspace/CharacterList.tsx`：detectOnly `useEffect` 在 merged>0 时调 `onExpanded()` 静默刷新列表，仅 pending>0 显示 banner（文案仅强调低置信待确认；若 merged>0 附「另有 M 组高置信重复已自动合并」）；banner 触发 `handleDedupeFromHint` 跑现有全量合并（low 进待确认面板）。
- `changelog-data.ts` + `CHANGELOG.md` 升 v2.0.20 同步。

### 门禁与上线
- tsc 0 错；vitest 62 文件 529/529 全绿。直连 SSH 推送（ghssh 临时 remote，用完即删），origin 保持 https。

### 设计取舍
- high 合并仍写 applied 快照保留回滚权；low 坚决不自动合并（含·马甲/歧义组安全优先）。detectOnly 合并由 route 全局 try-catch 兜底。
- **验证局限**：detectOnly+high 自动合并路径未连真实 DB 端到端实测（本地 .env 占位 + 白名单），正确性靠 v2.0.15/18 既有 applyMerge + 单测覆盖 + 本次最小改动（仅去一个跳过判断）保证；待线上验证（任务 #32 线上部分：新城项目打开角色栏应见高置信脏卡静默合并、低置信弹提示）。

## 全面优化测试体系（2026-08-13 续，已 push commit 9a2818e）

### 诊断
- 引入 `@vitest/coverage-v8`，配置 `vitest.config` 覆盖率（provider v8，聚焦 src/core + src/lib 纯逻辑层）。
- 全量覆盖率 33.76% stmts / 34.73% lines；111 个被统计文件中 79 个 <80%，pipeline/agents/babylore/dissect 等大模块与 json-parser/character-parse/rate-limit 等工具为 0% 盲区。

### 补盲区（3 文件 31 例，全部纯逻辑、无 DB 依赖、零脆弱）
- `src/core/character-dedupe.flow.test.ts`（5 例）：补 dedupeCharacters 主流程集成测试，锁死 v2.0.20 detectOnly+high 静默自动合并 + applied 快照路径（此前纯函数测试不覆盖）。
- `src/lib/json-parser.test.ts`（15 例）：覆盖 BOM/代码块/尾逗号/控制字符/未转义引号/粘连对象/未闭括号/全失败抛错 + safeParseAIJson。
- `src/lib/character-parse.test.ts`（11 例）：覆盖 fromText/toText 互逆、时间线解析、CHARACTER_ROLE_LABEL 权威映射。

### 关键发现（已锁死为回归护栏）
- `parseAIJson` 能修单一类型未闭括号（缺 `}` / 缺 `]`），但**对象内嵌未闭数组且对象本身也未闭**（`{"a":[1,2}`）超出修复能力、抛错——测试显式断言该边界，未来增强 parser 时需同步更新预期。

### 去脆弱 / 提速
- 提速：基线 3.87s/529 已快；唯一慢点 `babylore/fill.selfcheck`（434ms）为真实本地 PG 集成测试（本地 127.0.0.1:5432 novelforge 库可达且已 seed），属必要保留，非脆弱。
- 去脆弱：确认 vitest 文件级隔离下 env/vi.mock 不串扰；新测试纯逻辑无模块级状态泄漏；fill.selfcheck 环境依赖已在报告标注（无本地库环境会红，属自检性质）。
- 配置迁移：`vitest.config.ts` → `vitest.config.mjs`（ESM 原生），消除 `ESM syntax in CJS` 构建警告；新增 `npm run test:coverage` 脚本固化覆盖率命令。

### 门禁与上线
- `SAFE_DELETE_DISABLE=1 npx tsc --noEmit` 0 错；`npx vitest run` 65 文件 560/560 全绿（基线 529 + 31 新增）；ESM 警告消失。
- 直连 SSH 推送 `7fd6864..9a2818e main -> main`，origin 保持 https，临时 ghssh remote 用完即删。
- 仅提交本轮相关 7 文件（配置 + 3 测试 + package*.json）；历史遗留 tmp_*.cjs / _shots / PROCESS 报告等未跟踪文件刻意未混入。

### 后续可切入（覆盖盲区盘点）
- 0% 高价值纯逻辑后续可补：rate-limit（需 fake timers + 独立 key 避免模块单例泄漏）、validators、entity-detector、entity-auto-creator、confirm-guard、sync-global-prompt、storyline/execute-task。
- 大模块（pipeline/agents/babylore/dissect/distillation）需 mock LLM+DB，成本高，建议按业务风险单点推进，不为刷覆盖而补。
- 前端组件（components/app）需 jsdom + RTL，本轮未覆盖，另行规划测试环境。

## 诊断记录真伪核查（2026-08-13 续，只读核查）

### 背景
用户粘贴一段"三轮循环诊断 / N 轮自查"记录，要求去真身逐条确认真伪。核查对象为真实 2.0.9 目录（非旧基线 novel-forge-ours v0.26）。`C:\c\Users\...\novel-forge` 是 Windows 复制产生的残缺镜像，须走真身 `C:\Users\...\novel-forge`。

### 逐条结论
- 真实成立：124 route.ts、98 err.message、2 dangerouslySetInnerHTML(layout.tsx)、0 loading.tsx、无 middleware.ts、api-error.ts 默认不泄露内部错误、rate-limit 默认关闭(ENABLE_RATE_LIMIT≠"true" 放行)、Project 无 userId/ownerId(单机本地设计)、core/llm/client.ts 门面成熟、零用户鉴权(命中的 3 文件是 gameSession 游戏会话非用户鉴权)、zod 库全仓 import 计数=0(项目用手写 validators.ts)。
- 数字需校准：`.catch(() => {})` 空体精确 47(带参变体 8、无空格变体 4)，总 .catch 176——诊断称"161"偏高；AbortController 33 行/17 文件、全仓 fetch( 220——诊断"54 仅 9 用"口径不同；"106/124 错误收敛"未独立复现。
- 总体：该诊断基本基于真实 2.0.9，非瞎说八道；核心定性(单机本地应用、无鉴权是设计预期、短板是输入校验缺失+客户端竞态)成立。

### 路径陷阱（重要，未来复用）
- 真身 = `C:\Users\Administrator\WorkBuddy\2026-07-25-14-19-44\novel-forge`（Bash 用 `/c/Users/...`）。
- `C:\c\Users\...\novel-forge` 是残缺镜像（Windows 复制多出的盘符前缀）。Write 工具字面 `C:\c\...` 会写进残缺镜像而非真身，git 跟踪不到；Read 工具只认 `C:\Users\...`（无 c 前缀）。
- 本轮初误用 `C:\c\` 前缀 Write 两份文件到残缺镜像，已校正：内容重写到真身（DIAGNOSIS_VERIFY_2026-08-13.md + 本段 memory），并清理残缺镜像误建副本。后续所有 Read/Write 须用 `C:\Users\...` 无 c 前缀。

## 继续优化测试（2026-08-13 续，检验更新 + 补核心模块单测）

### 检验本地项目更新
- SSH 直连 `git fetch` 比较：本地 HEAD = 远程 HEAD = `9a2818e`，落后 0、领先 0 → 本地与远程完全同步，无远程更新需拉取。
- 工作区仅有历史遗留未跟踪文件（PROCESS 报告 / tmp 脚本 / 截图）与 memory 改动，不影响继续优化。

### 补核心纯逻辑单测（2 文件 23 例，全绿）
- `src/lib/api-error.test.ts`（14 例）：锁死 classifyError 全分支——6 个 Prisma 已知码中文映射(P1001/P1002/P1000/P2021/P2024/P2002)、SCHEMA_MISMATCH、通用 Prisma、NETWORK(502)、**默认分支不泄露内部 err.message**（传入含 SECRET 的 Error，断言返回"服务器内部错误，请查看日志"且不透传）；jsonError 返回标准化 NextResponse 且不泄露 body。
- `src/lib/rate-limit.test.ts`（9 例）：锁死"默认关闭——ENABLE_RATE_LIMIT≠'true' 即永远放行"(undefined/false/0 三态)；createRateLimiter 滑动窗口核心（窗口内放行、超量拒绝+retryAfter、窗口过期重置、不同 key 互不干扰，fake timers 隔离时间）；clientIp 取 x-forwarded-for 首段、rateLimitResponse 429 中文。

### 门禁与覆盖率
- tsc 0 错；vitest 67 文件 583/583 全绿（基线 560 + 23）。
- 覆盖率：api-error.ts 100% stmts/lines（branch 91.3%）、rate-limit.ts 70.96% stmts（便捷函数启用分支+60s sweep 未覆）；All files 36.53% stmts（上一轮 33.76% → +2.77pp）。
- 两个测试纯逻辑、无 DB、无网络、零脆弱（rate-limit 用 fake timers + 唯一 key 防模块单例泄漏）。

### 路径教训（已固化到上一诊断段）
- 真身 = `C:\Users\...\novel-forge`；`C:\c\...\novel-forge` 是残缺镜像，Write 字面 `C:\c\` 会写错位置。本轮所有 Read/Write 已走 `C:\Users\...` 无 c 前缀。
