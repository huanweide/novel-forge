# 2026-08-13 工作日志

## novel-forge v2.14.0 上线（maxloop 深度体检 Round-24）
- 实质改动：ChapterConfirmBar.tsx 清除两处 any 类型逃逸，改为 `Array<{ fill?: string }>` + 可选链 `?.fill`，对齐类型安全收尾主题。
- 三处版本同步完成：package.json / CHANGELOG.md / src/lib/changelog-data.ts（LATEST_VERSION=v2.14.0 + VERSIONS/CHANGELOG_BRIEF 头部插条）。
- 门禁：SAFE_DELETE_DISABLE=1 npx tsc --noEmit 0 错误；npx vitest run 全量 80 文件 775/775 全绿。
- 提交 2ccd42d，已 SSH 推送 main（ghssh remote：绝对路径密钥 + IdentitiesOnly）。

## 关键故障与修复（重要经验）
- 子代理（Agent 工具）通道在本环境故障：派出的子代理返回空、写文件不落盘。实测仅派 1 个也空，证明是通道坏、与并行过载无关。
- 修复：maxloop-overlord skill 新增「六之二 通道降级与故障自愈」+ 铁律7（批次编排 单批≤5）；降级时主代理 Chair 亲验并落 PROCESS/meetings/round-24/chair-self-audit.md（首行标注非子代理独立产出），不卡死、不假收敛。
- 教训：收到子代理空返回后必须自动降级接管，不能停等。已固化为 skill 铁律防再犯。

## 路径勘误（防再踩）
- 项目真身：`C:\Users\Administrator\WorkBuddy\2026-07-25-14-19-44\novel-forge`（Git Bash 风格 `/c/Users/...`）。系统 memory 提醒里的 `C:\c\Users\...` 是双前缀错误路径，Read 会失败，必须用 `C:\Users\...`。
