# 2026-08-15 工作留痕

## v2.30.0 角色筛选冗余清理（瑞宝宝 UI 收口）
- 移除 CharacterFilters 角色栏筛选栏「已分类/未分类」两个派生芯片（key=has-tags/no-tags）
- 同步清 filterCharacters 纯函数死分支（has-tags/no-tags 不可达）+ statHasTags/statNoTags 统计 + 4 个单测（character-filter.test.ts 3 例 + CharacterFilters.test.tsx 相关）
- tsc 0 错；vitest 92 文件 859 全绿（删 4 例，864→859）；push 683f60f..c53495b

## 架构审计（故事线/伏笔/大纲/因果链，主对话亲做，未派子代理）
- **故事线 token**：generate.ts 重量级 LLM——maxTokens 8192 + SYSTEM「铁律」prompt 近 2000 字 + 注入最多 30 角色(各100字)/20 世界观(各200字)。但**按需触发**（用户点「生成故事线」才跑 /src/core/storyline/generate.ts:148 completeText），非每次写作自动，不持续费 token。
- **伏笔 token**：detectPayoffs 收束率检测＝零 token 纯字符串命中（中文短语种子扫章节摘要/正文）；enrichForeshadow＝轻量 LLM 单次 ≤120 字 fire-and-forget（src/core/foreshadowing.ts:77）。
- **核心重叠**：故事线 type="thread"（线索/伏笔，generate.ts 铁律段明确定义为"悬而未解的谜团/物证/暗线"）与独立 `PendingCommitment` 伏笔系统追踪同一类"悬疑种子"——两套都在管伏笔/线索。主线/支线七要素规划 vs 大纲（章节级 storyNode）是不同粒度，不重叠。因果链＝analyze-relationships/sync-relations/character-dedupe（角色关系），与故事线不同维度。
- **死代码确认**：autoCreateEntities（src/lib/entity-auto-creator.ts:381，至文件末尾）无调用方、测试未引用；其内部调用的 normalizeDiscoveryName/isSimilarName/resolveDiscoveryMergeTarget/resolveEntityCategory 均为已导出保留函数——可安全删除，列为待清理项。

## 待办优化优先级（用户问"能不能一波优化"）
- 低风险已做：标签死代码清理（v2.30.0）
- 低风险可做：删 autoCreateEntities 死代码（重写 entity-auto-creator.ts 末尾段）
- 中风险提案：故事线 thread 与伏笔 PendingCommitment 合并（消除伏笔/线索双轨追踪，需数据迁移，待用户确认）
- 高风险：整体数据模型重构（暂缓）
- 注：路径双生陷阱仍存——Grep 工具默认搜带 c:\c\ 副本，Edit/Write 用不带 c 层绝对路径落真项目；Bash grep 限定 src 最稳。

## v2.31.0 autoCreateEntities 死代码删除（架构瘦身，用户"继续"执行）
- 删除 src/lib/entity-auto-creator.ts 的 autoCreateEntities 函数（原 381→566 行，零调用方、测试未引用，确属死代码）
- 连带删除其独有 AutoCreateResult 接口 + TYPE_LABELS 常量 + 4 个因此变未用的 import（prisma/Prisma/DetectedEntity/isCompleteEntityName）
- 保留仍被 entity-sync 使用的纯函数与确定性分类器（resolveEntityCategory/isSimilarName/normalizeDiscoveryName/resolveDiscoveryMergeTarget/shouldAutoCreateCharacterCard 等），建卡清洗逻辑不受影响
- 文件净减 ~195 行；tsc 0 错；vitest 92 文件 859 全绿（零回归）；push c53495b..05d3f36
- 删前用 grep/find 双重核准：全 src 无 import/调用，仅 changelog 历史文本提及；删除后用 node 脚本按「// ─── 主函数」唯一锚点截断文件尾，避免行号偏移

## v2.32.0 导出转换函数 proseToHtml 补全地基测试（马斯克 CEO 循环运营 hourly 轮次）

### 本轮动作（延续「找真实测试覆盖缺口」主线，低风险纯测试补全）
- 真实基线：自动化记忆滞后写 v2.14.0/v2.30.0，以代码为准 HEAD=v2.31.0（05d3f36，用户主对话「删 autoCreateEntities 死代码」已推送）。工作树仅 .workbuddy memory 改动 + untracked 临时目录，无未收口源码（src/app/api/storylines/generate/* 为用户并行主对话实时编辑、未提交，不触碰）。
- 候选核验：导出链路（export/route.ts + epub.ts 流式 buildEpubStream/buildHtmlDocStream）质量高——格式白名单/空内容拦截/选章级联/Readable.from 背压流式分块俱全，无崩溃 bug；导入 commit 路由防御充分；类型逃逸 1620 处绝大多数为 LLM/Prisma JSON 桥接（马斯克此前暂缓高危项），纯前端继续清理边际收益低；v2.30 验收干净无遗留。
- 修复：导出三大格式（HTML/EPUB/DOCX）共用的散文→HTML 核心转换 proseToHtml（src/core/epub.ts:28，长期零单测）补全 11 例自动化测试（src/core/proseToHtml.test.ts），锁死：空内容→空串、单行段落包 <p>、空行分段、**粗体**→<strong>、*斜体*→<em>、粗斜体共存、---→<hr/>、>引用块→<blockquote>、HTML 特殊字符转义(<script>&"'→&lt;script&gt;&amp;&quot;&#39;)、段落内换行→<br/>、多行引用块无残留空段落。node 实锤行为健康、无丢内容或崩溃级缺陷。纯测试补全、零生产代码改动、零运行时影响。
- 双门禁：tsc 0 错；vitest 全量 93 文件 870/870 全绿（较 v2.31.0 基线 +1 文件 +11 例）。注：首次全量遇 storyline/generate.test.ts「type=thread 解析」用例偶发失败，单独重跑 3 次全绿、二次全量重跑均 870 全绿；git blame 显示 generate.ts:185 为「Not Committed Yet 11:32」即用户并行主对话实时编辑未提交改动（强制 thread→side 属伏笔归伏笔面板设计），非本轮引入、属测试间状态污染/用户并行编辑时序，未触碰该用户领地文件。
- 四文件同步 v2.32.0（package.json/changelog-data.ts LATEST_VERSION+CHANGELOG_BRIEF头条+VERSIONS[0]/CHANGELOG.md/更新报告.md）；commit + SSH 推送 ghssh main；dev server /changelog HTTP 200 含 v2.32.0。

### 关键复盘
- 子代理通道故障持续：review-worker/deepseek-v4-pro 本环境仍返回空/无模型，按用户 2026-08-14 纠偏「不派子代理、主对话亲验」，本轮主代理代行马斯克 CEO 拍板并如实标注，不回头问用户；IP 铁律仍归瑞宝宝，无新 IP/品牌/引流。
- 不假收敛：导出/导入链路已高质量、类型逃逸仅剩高危 LLM 桥接（马斯克暂缓）、历史轮缺口全闭环，本轮只补「被三大导出复用却零单测」的真实防护缺口，属 v2.18/v2.22 同性质低风险测试流程优化，不制造虚假改动。
