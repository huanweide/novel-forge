# 2026-08-15 工作留痕

## v2.30.0 角色筛选冗余清理（瑞宝宝 UI 收口）
- 移除 CharacterFilters 角色栏筛选栏「已分类/未分类」两个派生芯片（key=has-tags/no-tags）
- 同步清 filterCharacters 纯函数死分支（has-tags/no-tags 不可达）+ statHasTags/statNoTags 统计 + 4 个单测（character-filter.test.ts 3 例 + CharacterFilters.test.tsx 相关）
- tsc 0 错；vitest 92 文件 859 全绿（删 4 例，864→859）；push 683f60f..c53495b

## 架构审计（故事线/伏笔/大纲/因果链，主对话亲做，未派子代理）
- **故事线 token**：generate.ts 重量级 LLM——maxTokens 8192 + SYSTEM「铁律」prompt 近 2000 字 + 注入最多 30 角色(各100字)/20 世界观(各200字)。但**按需触发**（用户点「生成故事线」才跑 /src/core/storyline/generate.ts:148 completeText），非每次写作自动，不持续费 token。
- **伏笔 token**：detectPayoffs 收束率检测＝零 token 纯字符串命中（中文短语种子扫章节摘要/正文）；enrichForeshadow＝轻量 LLM 单次 ≤120 字 fire-and-forget（src/core/foreshadowing.ts:77）。
- **核心重叠**：故事线 type="thread"（线索/伏笔，generate.ts 铁律段明确定义为"悬而未解的谜团/物证/暗线"）与独立 `PendingCommitment` 伏笔系统追踪同一类"悬疑种子"——两套都在管伏笔/线索。主线/支线七要素规划 vs 大纲（章节级 storyNode）是不同粒度，不重叠。因果链＝analyze-relationships/sync-relations/character-dedupe（角色关系），与故事线不同维度。
- **死代码确认**：autoCreateEntities（src/lib/entity-auto-creator.ts:381，至文件末尾）无调用方、测试未引用；其内部调用的 normalizeDiscoveryName/isSimilarName/resolveDiscoveryMergeTarget/resolveEntityCategory 均为已导出保留函数——可安全删除，列为待清理项。

## 待办优化优先级（用户问"能不能一波优化"）
- 低风险已做：标签死代码清理（v2.30.0）
- 低风险可做：删 autoCreateEntities 死代码（重写 entity-auto-creator.ts 末尾段）
- 中风险提案：故事线 thread 与伏笔 PendingCommitment 合并（消除伏笔/线索双轨追踪，需数据迁移，待用户确认）
- 高风险：整体数据模型重构（暂缓）
- 注：路径双生陷阱仍存——Grep 工具默认搜带 c:\c\ 副本，Edit/Write 用不带 c 层绝对路径落真项目；Bash grep 限定 src 最稳。

## v2.42.0 朗读清洗纯函数化 + 单测锁死（马斯克 CEO 循环运营 hourly 轮次）
- 本轮动作（延续「找真实测试覆盖缺口」主线，低风险纯测试补全 + 极小重构）：运行期间发现 v2.41.0（AI 念书 TTSPlayer）已被用户/主对话实时收口并推送（a3fa8f0，含 TTSPlayer 181行 + 朗读偏好 localStorage 持久化），v2.41.0 无残缺，故转向 v2.42.0 真实缺口——TTSPlayer 内联的朗读前 Markdown 清洗 stripMarkdown 零单测（回归会让用户听到 **、#、> 符号）。抽 stripMarkdown 到 src/lib/stripMarkdown.ts（行为原样），TTSPlayer 改引用（仅动 v2.41.0 原块 line 13-33，保留用户并行新增偏好持久化 line 35-52）；新增 src/lib/stripMarkdown.test.ts 17 例锁死代码块/行内代码/图片/链接/标题/粗斜体/引用/列表/残余符号清洗与 trim/多空行压缩 + 真实章节综合场景。
- 双门禁：tsc 0 错；vitest 全量 94 文件 896/896 全绿（较 v2.41.0 基线 +17）；测试初 2 例期望写错（代码块保留换行、多余空格压缩），修正期望匹配真实行为未改函数。
- 四文件同步 v2.42.0（package.json/changelog-data.ts 三处/CHANGELOG.md/更新报告.md）；commit 6846d8f（7 文件 +195/-26，含用户偏好持久化顺带收口）；GIT_SSH_COMMAND 指定 key 推送 ghssh main 成功（a3fa8f0..6846d8f）；dev server /changelog 含 v2.42.0。IP 仍归瑞宝宝。
- 马斯克 CEO 拍板（子代理通道仍故障，主代理代行如实标注）：本轮连贯/已验证/低风险/高价值（朗读清洗防护），直接收口 v2.42.0；类型逃逸红区高危暂缓、不假收敛。

## v2.31.0 autoCreateEntities 死代码删除（架构瘦身，用户"继续"执行）
- 删除 src/lib/entity-auto-creator.ts 的 autoCreateEntities 函数（原 381→566 行，零调用方、测试未引用，确属死代码）
- 连带删除其独有 AutoCreateResult 接口 + TYPE_LABELS 常量 + 4 个因此变未用的 import（prisma/Prisma/DetectedEntity/isCompleteEntityName）
- 保留仍被 entity-sync 使用的纯函数与确定性分类器（resolveEntityCategory/isSimilarName/normalizeDiscoveryName/resolveDiscoveryMergeTarget/shouldAutoCreateCharacterCard 等），建卡清洗逻辑不受影响
- 文件净减 ~195 行；tsc 0 错；vitest 92 文件 859 全绿（零回归）；push c53495b..05d3f36
- 删前用 grep/find 双重核准：全 src 无 import/调用，仅 changelog 历史文本提及；删除后用 node 脚本按「// ─── 主函数」唯一锚点截断文件尾，避免行号偏移

## v2.32.0 导出转换函数 proseToHtml 补全地基测试（马斯克 CEO 循环运营 hourly 轮次）

### 本轮动作（延续「找真实测试覆盖缺口」主线，低风险纯测试补全）
- 真实基线：自动化记忆滞后写 v2.14.0/v2.30.0，以代码为准 HEAD=v2.31.0（05d3f36，用户主对话「删 autoCreateEntities 死代码」已推送）。工作树仅 .workbuddy memory 改动 + untracked 临时目录，无未收口源码（src/app/api/storylines/generate/* 为用户并行主对话实时编辑、未提交，不触碰）。
- 候选核验：导出链路（export/route.ts + epub.ts 流式 buildEpubStream/buildHtmlDocStream）质量高——格式白名单/空内容拦截/选章级联/Readable.from 背压流式分块俱全，无崩溃 bug；导入 commit 路由防御充分；类型逃逸 1620 处绝大多数为 LLM/Prisma JSON 桥接（马斯克此前暂缓高危项），纯前端继续清理边际收益低；v2.30 验收干净无遗留。
- 修复：导出三大格式（HTML/EPUB/DOCX）共用的散文→HTML 核心转换 proseToHtml（src/core/epub.ts:28，长期零单测）补全 11 例自动化测试（src/core/proseToHtml.test.ts），锁死：空内容→空串、单行段落包 <p>、空行分段、**粗体**→<strong>、*斜体*→<em>、粗斜体共存、---→<hr/>、>引用块→<blockquote>、HTML 特殊字符转义(<script>&"'→&lt;script&gt;&amp;&quot;&#39;)、段落内换行→<br/>、多行引用块无残留空段落。node 实锤行为健康、无丢内容或崩溃级缺陷。纯测试补全、零生产代码改动、零运行时影响。
- 双门禁：tsc 0 错；vitest 全量 93 文件 870/870 全绿（较 v2.31.0 基线 +1 文件 +11 例）。注：首次全量遇 storyline/generate.test.ts「type=thread 解析」用例偶发失败，单独重跑 3 次全绿、二次全量重跑均 870 全绿；git blame 显示 generate.ts:185 为「Not Committed Yet 11:32」即用户并行主对话实时编辑未提交改动（强制 thread→side 属伏笔归伏笔面板设计），非本轮引入、属测试间状态污染/用户并行编辑时序，未触碰该用户领地文件。
- 四文件同步 v2.32.0（package.json/changelog-data.ts LATEST_VERSION+CHANGELOG_BRIEF头条+VERSIONS[0]/CHANGELOG.md/更新报告.md）；commit + SSH 推送 ghssh main；dev server /changelog HTTP 200 含 v2.32.0。

### 关键复盘
- 子代理通道故障持续：review-worker/deepseek-v4-pro 本环境仍返回空/无模型，按用户 2026-08-14 纠偏「不派子代理、主对话亲验」，本轮主代理代行马斯克 CEO 拍板并如实标注，不回头问用户；IP 铁律仍归瑞宝宝，无新 IP/品牌/引流。
- 不假收敛：导出/导入链路已高质量、类型逃逸仅剩高危 LLM 桥接（马斯克暂缓）、历史轮缺口全闭环，本轮只补「被三大导出复用却零单测」的真实防护缺口，属 v2.18/v2.22 同性质低风险测试流程优化，不制造虚假改动。

## 类型逃逸逐文件精修计划（只读探索 + 可视化，v2.33 之后当日）
- 审计 src 505 文件：any 词 token 1470（含 changelog-data.ts 版本描述**文案误报** many/company/"as any" 短语 + prisma 生成物）。精确类型逃逸：源文件 as any ~452（含 route LLM 桥接）、: any 582/136文件、any[] 237/55文件、Record<string,any> 25/11文件、<any> 5；测试文件 279（暂缓）；prisma 生成物 ~17（永不动）。
- 历史已清两批（策略已验证）：v2.25 MarkdownViewer 21 + v2.28 Icon 图标名 as any→as IconName 6 = 27 处；手法=纯前端/纯函数优先、LLM/Prisma JSON 桥接暂缓、test 低优先。
- 五区分层：红区=Prisma生成+LLM/JSON桥接as any(~450+17)暂缓；灰区=:any注解582费力；蓝区=第一批零风险(泛型逃逸 Record25+<any>5+明确any[]+前端参数,~80处)；黄区=测试279；绿区=已清27。
- 路线图：阶段0已完 → 阶段1第一批零风险(~80处,涉及 store/index.ts、core/explore/utils.ts、app/workspace/[projectId]/tables/page.tsx、components/explore/OutlinePanel.tsx、app/api/{lorebook/[id]/autofill,explore/adopt,characters/[id]/autofill,projects/import}/route.ts、components/workspace/postgen/types.ts 等) → 阶段2注解补全582 → 阶段3桥接暂缓 → 阶段4测试雾。
- 纪律：每改一文件即 tsc + 该文件相关 vitest 门禁、零错误才合入；每批 bump 版本 commit。不追求「0 any」——红区桥接强改会重写大半桥接层、反伤门禁，圈起标注「已知逃逸·暂缓」即可。


## 2026-08-15 本轮（v2.34.0 残缺发布收口 · 自动化运营）
- 检测：工作树存在上一轮（主对话）已写入但未提交的 v2.34.0 改动——6 源文件（tables/page.tsx、OutlinePanel.tsx、babylore/fill.ts、babylore/ifcell.ts、core/explore/utils.ts、store/index.ts 的 Record<string,any>→Record<string,unknown> 收窄 + 2 处动态豁免标注）+ CHANGELOG.md 头条 + package.json version 已 bump 到 2.34.0；初查 changelog-data.ts 三处疑似滞后 v2.33.0（典型「双 changelog 漏改 + 漏 commit」残缺发布陷阱）。复核（重新 Read 权威内容）确认 changelog-data.ts 三处实际已为 v2.34.0：初查为读取快照滞后误判。
- 修复：无新源码改动——v2.34.0 代码已完整且健康；本轮只收口发布流程（确认三处一致 + 提交 + 推送），把卡在工作树、未上线的半版本真正交付。
- 双门禁：SAFE_DELETE_DISABLE=1 npx tsc --noEmit --incremental false = 0 错；npx vitest run = 93 文件 870/870 全绿（与 v2.34.0 基线一致，零回归）。
- 交付：四文件同步（package.json/changelog-data.ts LATEST_VERSION+CHANGELOG_BRIEF头条+VERSIONS[0]/CHANGELOG.md/更新报告.md 顶部）均已对齐 v2.34.0；git add 指定 v2.34.0 相关文件（含 更新报告.md + 项目记忆本章）后 commit + SSH 推送 ghssh main。
- 马斯克 CEO 拍板（主代理代行，子代理通道仍故障如实标注）：残缺发布本身是真实缺陷——未提交半版本 + changelog 不一致会让线上公告与代码错位，直接收口上线、不假收敛重做已闭环项；下一轮按路线图推进 v2.35.0 灰区 :any 注解收窄。IP 仍归瑞宝宝，无新 IP/品牌/引流。

## v2.35.0 类型逃逸精修·第二批（tables 页 useState<any> 精确接口化）
- 灰区主攻前先收口蓝区扩展：tables/page.tsx 三个结果态——单表填表 fillResult（ok/operations/applied/error/at/warnings）、召回 recallItems（source/title/content）定义 TableFillResult/RecallItem 精确接口替代 useState<any>/useState<any[]>，删 1 处 as any[]（issues 吃精确类型）；一键填表 fillAllResult 因后端动态自检报告（selfCheck 含 4 类 issues + fillErrorMeta）硬类型化脆弱，保留 any 注释（合理动态豁免）。
- tsc 0 错；组件测试稀疏以 tsc 类型门禁替代；commit 9e3975b + push origin main。

## v2.36.0 类型逃逸精修·第三批（灰区接口字段/函数参数具体类型化）
- 灰区（: any 注解，只读审计得源文件非测试非generated 共 375 处/94 文件）第一批主攻 C 类（独立接口字段/函数参数/回调签名）：
  - core/write-generation.ts 的 WriteInput 接口 5 字段 any→具体类型：confirmedCardIds?: string[]、cardNotes?: Record<string,string>、newCharacterRequests?: string[]、storylineId?: string、diffuseCompleted?: boolean（下游 filterByConfirmedCards/prepareAuthorNote/handleNewCharacters/formatStorylines 早已是对应的具体类型，中间层契约对齐）。
  - core/sync-global-prompt.ts 的 buildGlobalPrompt 入参 project 内联类型 llmConfig?/buildConfig? 由 any→unknown（下游已 as 显式收窄，any 多余）。
  - lib/storyline-progress.ts 的 computeStorylineProgress(s: any)→s: unknown + 函数体用 "sevenElements" in s + typeof==="object" 守卫提取七要素（只算进度条、不依赖 any 行为）。
- 8 处收窄 tsc 0 错零扩散到调用方；vitest 全量 93 文件 870/870 全绿；commit 56b6550 + push origin main。
- 灰区剩余量：剩 ~367 处，绝大多数为 A 类 `.find((n:any)=>n.id)`（上游数组 any[] 导致元素 any，独立收窄无意义需上游类型化）+ B 类 `catch(err:any)`（宽松场景收益低），按路线图分批推进、不硬啃；红区（API 路由 Record<string,any> + postgen/types.ts AI 桥接 any[]）结构动态暂缓。

## v2.37.0 类型逃逸精修·第四批（灰区 interface 字段透传态收窄）
- 复扫灰区当前真实底数（非测试/非生成物 :any 注解共 319 处：interface_field 17 / func_param 58 / callback_elem 191 / catch 53）。低垂果实已被历史五轮（v2.25/v2.28/v2.34/v2.35/v2.36）摘完，本轮只收 2 处确定干净的 interface 透传字段：
  - core/babylore/fill.ts 的 SkippedOp.op: any→unknown（被跳过的无效填表操作仅透传进告警、下游不访问内部）。
  - app/workshop/page.tsx 的 PresetRec.content: any→unknown（预设内容整体透传进 JSON 请求体序列化、无属性访问）。
- tsc 0 错；babylore 40/40 测试全绿；全量 vitest 93 文件 870/870 全绿；commit f010f01 + push origin main。
- 灰区收口诚实判断：剩余多为「上游 Prisma 模型 / AI 桥接返回」的连锁 any（outline-context.ts 整片、memory-decay 的 eventImportances、tool-registry 的 prisma:any、ProjectConfigPanel 的 project:any），独立收窄任一处会向下游属性访问扩散报错，需整体上游类型化大改——按路线图红区标注、暂缓、不硬啃。逐文件精修策略至此触达收益边界，后续不再为清零而硬改。

## v2.38.0 角色核心逻辑深度优化（任务 #16）：智能去重与谨慎建卡回归防护网
- 任务 #16 真实状态核查（关键：不瞎改成熟逻辑）：四条诉求已在 v2.0.x 多轮迭代真实落地且非空壳——
  - 谨慎建卡 shouldAutoCreateCharacterCard（阈值 2）：entity-sync.ts:246 真实查 `storyNode.count({where:{content:{contains:name}}})` 拦截只出现 1 次的路人甲。
  - 同人异称消歧 resolveVariantTarget（entity-auto-creator）：尊称/单字缩写/姓+描述无歧义并入唯一同姓正主，歧义 null。
  - 复杂称呼核心名坍缩 coreTokenOf（character-dedupe）：「迪哥先生」「迪哥·若昂内」→ 同核「迪哥」→ 高置信自动合并；两马甲同核降 low 待确认。
- 本轮零生产代码改动，给成熟逻辑装防滑网：补 6 例针对性回归测试——
  - character-dedupe.test.ts +3：迪哥先生/迪哥/迪哥·若昂内→high、迪哥·若昂内/迪哥·桑切斯→low、迪哥/小迪→high。
  - entity-auto-creator.test.ts +3：shouldAutoCreateCharacterCard 出现 0/1 次拦截、2/3 次放行、自定义阈值生效。
- tsc 0 错；vitest 全量 93 文件 879/879 全绿（基线 +6 例）；commit 6a3ccde + push origin main。
- 方法论沉淀：对历史已覆盖的功能，「深度优化」应优先做实证核查（确认真实落地/非空壳）+ 回归防护网，而非假装在成熟逻辑上硬改制造风险与回归隐患。

## v2.39.0 页面无障碍(a11y)复验与修复（任务 #10）：三主题对比度达标 + 正文溢出防护
- 复验基线：round-8 L4 审计遗留 F-01~F-06 六项，其中 F-01（浅色金色文字对比度）与 F-03（生成状态 aria-live 播报）经核查已在历史轮次修好（globals.css 现浅色有 --nv-accent-text-on-light: oklch(0.50 0.12 95) 实测 5.20 AA；CenterPanel.tsx:247/250 已有 aria-live 区域）。
- 唯一硬性失败项 F-02（浅色主色 --nv-primary 文字 CR 4.42<4.5 AA）：用一次性脚本把三主题关键令牌转真实 sRGB 实测对比度——深色全达标（主色5.61/金9.20/次级9.12/弱6.18）、苍青全达标（主色8.04/金10.78/次级9.35/弱5.99）、浅色（次级7.36/弱5.39/暗金5.20 达标，主色0.55 仅 4.42 失败）。
- 修复：浅色 --nv-primary 由 oklch(0.55 0.17 270) 压暗至 oklch(0.52 0.17 270)，扫描实测 CR 5.00 稳达 AA（留 0.5 余量应对纯白卡片）；--nv-primary-soft 同步压暗保持同色系一致。
- F-06（正文长 token 横向溢出）：CenterPanel 章节正文 contentEditable 容器补 break-words（防超长英文/链接窄屏溢出）。F-05 核查：AIChatBar 预设按钮含可见文字 {p.label} 已可访问，不适用。
- tsc 0 错；vitest 全量 93 文件 879/879 全绿；四文件同步 v2.39.0；commit 2b5177e + push origin main。
- 临时对比度脚本 tmp_a11y_contrast.mjs 用完即删（untracked 不污染）。

## v2.40.0 功能精简与内置化（任务 #12）：删死依赖 + 创作铁律面板双向闭环
- 任务定位：#12 宏观任务无独立落盘规格，改用实证只读扫描定位真实对象，避免瞎改。
- 内置化实证：globals.css 仅 @import 本地 npm 包（tailwindcss / tw-animate-css / shadcn），src 内 grep 零外部 CDN（cdn/unpkg/jsdelivr/googleapis/gstatic）——前端资源已全内置，无需改动。
- 功能精简·删死依赖：bash 全项目（排除 node_modules）扫 import/require，确认 @bubblewrap/core（TWA 安卓打包，本地 Web 工具零用处）、uuid、dotenv 三包零引用——Next.js 内置 .env 加载、代码用 crypto.randomUUID，均不需要。已从 package.json 移除三项并 npm install 同步 lock，复核零残留引用。
- 功能精简·双向闭环（UI_FIX_PLAN I-2）：RulesPanel.tsx:122 头部补对称区分说明——「创作铁律」注入 AI 提示词、不同于「项目配置」的「正则后处理规则」（生成后清洗正文），两者各管一段互不影响；此前仅 ProjectConfigPanel.tsx:271 单向说明，现双向闭合消除混淆。
- 双门禁：tsc 0 错；vitest 全量 93 文件 879/879 全绿；四文件同步 v2.40.0（package.json/changelog-data.ts LATEST_VERSION+CHANGELOG_BRIEF头条+VERSIONS[0]/CHANGELOG.md/更新报告.md）；commit + push origin main。
- IP 仍归瑞宝宝，无新 IP/品牌/引流。

## v2.41.0 AI 念书（语音朗读）：写作正文区「朗读本章」入口 + 浏览器语音控制条
- 新功能（用户从打磨候选中选定"AI 念书"，经实证确认是真正未做项——src 内 grep speechSynthesis/TTSPlayer/朗读零实现；此前误把已存在的"一键出书(EPUB/Word)"列为待做、核实作废重选）。
- 新增 src/components/workspace/TTSPlayer.tsx（纯前端 Web Speech API 封装，零依赖/零网络/零密钥，契合 novel-forge 本地工具定位）：stripMarkdown 表层清洗正文标记避免念符号；useEffect 加载语音优先中文(lang 含 zh/名称含 中文/普通话/国语/Chinese)、voiceschanged 监听、卸载 cancel 防泄漏；speak/togglePlay(play↔pause↔resume)/stop；控制条含 播放/暂停/停止按钮、语速 range(0.5-2)、多中文音色 select(>1 才显)、onClose ×；unsupported 时友好提示(换 Chrome/Edge)。
- CenterPanel.tsx 接入：import TTSPlayer；加 showTTS state；章节标题 <h1> 下方、固定色图例之前插入——displayContent 存在时居中显示，showTTS 为真渲染 TTSPlayer(text=displayContent)、否则渲染「🔊 朗读本章」按钮(Icon radio)。
- 双门禁：tsc 0 错（浏览器语音 API 类型无误）；vitest 全量 93 文件 879/879 全绿；四文件同步 v2.41.0（package.json/changelog-data.ts LATEST_VERSION+CHANGELOG_BRIEF头条+VERSIONS[0]/CHANGELOG.md/更新报告.md）；commit + push origin main。
- IP 仍归瑞宝宝，无新 IP/品牌/引流。

## v2.42.0 AI 念书增强：朗读偏好持久化 + 实时状态显示 + 无头截图验证（千惠独立执行）
- 增强 1（偏好持久化）：TTSPlayer 新增 loadTTSPrefs/saveTTSPrefs helper（key=nf_tts_prefs）；rate/voiceURI 初始值从 localStorage 读取（有则恢复、无则默认）；调语速/换音色时实时写回 LS；语音加载后优先匹配 LS 保存的音色 URI。效果：下次打开朗读面板自动恢复上次设置。
- 增强 2（实时状态）：控制条 stop 按钮与语速区域之间新增 isPlaying 条件渲染的 span（主题色 font-medium text-xs）。播放时蓝显「朗读中」，暂停时变「已暂停」。
- 无头截图验证（agent-browser 0.27.0 + headless Chrome）：注入三个 localStorage 关闭弹窗 → 导航 workspace/[星辰PID] → 三张截图确凿证据：
  - tmp_tts_shots/before.png：「🔊 朗读本章」按钮
  - tmp_tts_shots/control.png：控制条完整（▶/■/语速/Microsoft Huihui 音色/×）
  - tmp_tts_shots/playing.png：「朗读中」蓝色高亮确认存在
- headless Chromium 意外有 Microsoft Huihui 中文语音包，speechSynthesis fully supported。
- 本增强已被 v2.42.0 commit 6846d8f 收口纳入（含偏好持久化，「朗读中」状态文字为额外增量未单独列项）。
- 双门禁：tsc 0 错；vitest 94 文件 896/896 全绿。

## v2.43.0 四项体验收口（瑞宝宝指令）
- 章名：orchestrator.summarizeChapter 新增 chapterTitle 字段（≤5字、禁人名）；post-processor 新增 deriveChapterName 纯函数（剔角色名/别名+截5字兜底），保留"不覆盖手动章名"守卫（仅占位时回填）。
- 设置：ChapterConfirmBar 删除"这是什么？确认流程怎么用"折叠长说明；智能审阅/自动交付开关本就 ?? true 默认开。项目自检(ProjectDiagnostics)保留（本就一键体检、无配置）。
- 角色芯片：CharacterFilters 筛选行加 text-xs 对标世界面板按钮(px-2 py-1.5 text-xs)，gap-0.5→gap-1 放宽。
- 创意工坊：删"骰子随机事件表"预设（全库无掷骰引擎、且作为 table_template 被每章自动填表接管=冲突+无用）；头部"共创社区"→"我的本地预设"，文案如实写本机存储不共享。
- 门禁：tsc 0 / vitest 94文件 896 全绿；四版本文件对齐 v2.43.0（package.json/changelog-data/CHANGELOG.md/更新报告.md）。

## v2.44.0 开关按钮重做 + 究极截图证据（瑞宝宝指令：Switch UI 丑问题收口）
- 核心诉求：瑞宝宝明确指出"可开启和关闭设置按钮"三大硬伤——(1)滑动不顺滑像瞬移 (2)开启态丑 (3)开/关无区分。
- 诊断发现：Switch 组件自 v1.8.15 (bc2a9b7) 起已是 v2 重写版（顺滑弹簧+开灭关亮），但从未专门标注版本/补截图证据。prior 摘要存在严重失真（虚构 v2.42/v2.43/v2.44 版本叙事与实际 git 历史脱节），经磁盘级交叉验证确认真实状态。
- Switch v2 实际效果确认（代码审查+settings 截图）：
  - 顺滑：300ms 弹簧缓动 cubic-bezier(0.34,1.56,0.64,1) 回弹 + hover/active 反馈
  - 区分：关闭"熄灭"(bg-white/60 半透明白+灰轨) vs 开启"点亮"(纯白滑块+光晕阴影+主题色轨)
  - 好看：尺寸加大(md w-12 h-7 / thumb w-5 h-5)、API 不变全局 25 处零改动
- 究极截图：系统 Chrome 无头直截 + prep.html 中转页预置 localStorage 关闭弹窗，产出 settings/workspace/workshop 三张干净截图存 PROCESS/ui-shots/（275KB/591KB/510KB）。
- 路径双生陷阱修复：确认 Bash 工具 cwd 落在镜像 C:\c\Users\...（残缺副本），Write/Read 操作真身 C:\Users\...（GitHub 权威仓库）。所有 git 操作必须通过 PowerShell 或 git -C 真身路径执行，Bash MSYS 路径映射导致跨目录操作失败。真身 pull --rebase 同步远程 v2.25-v2.43 共 20 个版本后成功 push v2.44.0 到 GitHub origin/main (a1be2cb)。
- 双门禁：tsc 0 错 | vitest 94 文件 896/896 全绿（9.59s）。IP 归瑞宝宝。

## 多视角体验审计（v2.44.0 后）：读者/写手/架构师/自写爱好者/美学UI大师五视角整合
- 方法：TeamCreate 团队 multi-perspective，并行派 5 个 general-purpose 子代理（reader/writer/architect/hobbyist/uimaster），各读真身源码 + 看 PROCESS/ui-shots 截图 + 只读点验 dev server(3001)，产出独立报告到 PROCESS/multi-perspective/。
- 五份原始报告：reader.md / writer.md / architect.md / hobbyist.md / uimaster.md（每条改动附源码行号证据，均未改源码/未提交）。
- 整合计划：PROCESS/multi-perspective/INTEGRATED-PLAN.md。去重按主题合并为 P0（5 主题）+ P1 + P2，并给出 v2.45/v2.46/v2.47 三版路线。
- 跨视角共识：功能闭环已完整（流式落库/智能审阅/批量后台/游戏模式/记忆预算透明），但门槛/视觉密度/心流断点/听书半成品/架构债五座山让它像「开发者自用工具」而非「人人能用的作家工坊」。
- 高频共识发现：① 工作区 16 分类彩虹芯片违反唯一香槟金原则（UI大师+爱好者+写手）；② 确认流程藏进二级弹窗打断写手心流（写手）；③ TTS 整章单句朗读无进度/不念标题（读者）；④ 流式逐 token 全树重渲染=大书卡顿根因（架构师 P0-1）；⑤ 状态词表双轨无枚举=脏数据根因（架构师 P0-3）。
- 团队机制注意：Agent 子代理必须先 TeamCreate 建团队才能 spawn（直接 Agent 报「No active team found」）。


## 马斯克 CEO 循环运营轮次（v2.44.0 → v2.45.0 更新弹窗接上「用户视角」摘要·收口漏接线缺陷）
- 检测：真实 HEAD=v2.44.0（a1be2cb，已推送）；工作树 `src/lib/changelog-data.ts` 有一处上一轮写好却漏接线的半截改动——新增 `CHANGELOG_USER_BRIEF`（首页更新弹窗「用户视角」大白话摘要：新手引导更贴心/设置页大瘦身/工作区正文更耐看），而首页 `page.tsx` 弹窗仍直接渲染技术向的 `CHANGELOG_BRIEF`（满屏「SAFE_DELETE_DISABLE=1 npx tsc --noEmit 0 错误」黑话），普通用户打开更新公告完全看不懂。实证三处功能（OnboardingModal / MarkdownViewer 实体高亮跳转 / switch 组件）均真实落地，USER_BRIEF 内容属实，属典型「残缺发布」类缺陷。其它候选：类型逃逸 1147 处（as any 482/:any 460/any[] 205）绝大多数为 LLM/Prisma JSON 桥接（马斯克此前暂缓高危项）、历史轮缺口全闭环、TODO 零真实待办、console 低价值不碰——无新硬缺口。
- 决策：子代理通道（review-worker/deepseek-v4-pro）本环境仍故障，按用户 2026-08-14 纠偏主代理代行马斯克 CEO 拍板（如实标注），不回头问；结论＝把漏接线的 CHANGELOG_USER_BRIEF 接进首页弹窗、纯展示层切换、低风险高价值、直接收口 v2.45.0。
- 修复：page.tsx 弹窗由 `CHANGELOG_BRIEF` 改为 `CHANGELOG_USER_BRIEF`（import 同步替换、移除未用 BRIEF），技术向完整版仅保留给「查看完整公告」跳转的 /changelog 页；零运行时逻辑改动、无新接口/无 LLM 开销。
- 双门禁：SAFE_DELETE_DISABLE=1 npx tsc --noEmit --incremental false = 0 错；npx vitest run = 94 文件 896/896 全绿（与 v2.44 基线一致，零回归）。
- 交付：四文件同步 v2.45.0（package.json/changelog-data.ts LATEST_VERSION+CHANGELOG_BRIEF头条+VERSIONS[0]/CHANGELOG.md/更新报告.md）；commit 53d3f86（5 文件 +48/-5）；SSH 推送 ghssh main 成功（0203178..53d3f86）；dev server HTTP 200 + /changelog 含 v2.45.0。
- 马斯克 CEO 拍板（主代理代行，子代理故障如实标注）：本轮连贯/已验证/低风险/高价值（收口漏接线、让公告说人话），直接收口；IP 仍归瑞宝宝，无新 IP/品牌/引流。
- 下一步候选（避重复盲区）：① 多视角审计(INTEGRATED-PLAN) P0 主题（工作区分类芯片香槟金违例 / 流式整树重渲染大书卡顿 / 状态词表双轨脏数据）按路线图推进；② LLM/Prisma 桥接项仅在有「tsc 实证低成本可去」确定项时收；③ 用户新明确诉求优先。
