# 2026-08-09 工作记录

## novel-forge v1.6.38 大书导出流式分块
- 改动文件：`src/app/api/projects/[id]/export/route.ts`
  - markdown/txt 导出从「整本字符串一次性 `new Response` 返回」改为「`async generator + Readable.from` 逐章 `yield`」，由 Node `Readable` 背压自动调度。
  - 删除 `buildMarkdownNode` / `buildTextNode` 两个仅本文件自递归的同步拼接死代码，新增 `buildExportStream` / `markdownNodeGen` / `textNodeGen` 三个异步生成器。
- 动机：防几十万字大书导出 OOM——内存峰值从「整本字符串（几十 MB）」降到「单章 + ~16KB buffer」。
- 验证：`SAFE_DELETE_DISABLE=1 npx tsc --noEmit` = 0 错误；`npx vitest run` = 311/311 全绿。行为等价（目录锚点、空节提示一致），用户无感。
- 决策来源：马斯克人格执行 CEO 子 Agent 拍板（候选 A 优先；B=llmConfig 类型统一暂缓；html 流式留后续）。
- 交付：`src/lib/changelog-data.ts` 三处升版（LATEST_VERSION→v1.6.38、CHANGELOG_BRIEF 4 条、VERSIONS 数组插条）+ 根 `CHANGELOG.md` 头条 + 费曼报告 `PROCESS/WORK_REPORT-export-stream-v1.6.38-2026-08-09.md` + git commit `4487dab`（4 文件 +157/-75）+ push main 成功。
- 诚实边界：流式内存收敛是确定性逻辑改进（不再持有整本字符串），但本轮未用大书 mock 跑独立压测量化具体 MB 数字，待部署后真实大本书验证。

## novel-forge v1.6.39 HTML 导出流式化（F4 收口续）
- 改动文件：`src/core/epub.ts`（删除 `buildHtmlDoc` 同步函数、新增 `buildHtmlDocStream` async generator）、`src/app/api/projects/[id]/export/route.ts`（html 分支改 `Readable.from(buildHtmlDocStream(...))` 流式响应 + import 改名）、`src/core/html.stream.test.ts`（新增 3 项冒烟测试）。
- 动机：v1.6.38 漏网的 html 格式仍是「整本正文递归拼成单个巨大字符串一次性 new Response」——与 markdown/txt/epub/docx 同源未收口 OOM 路径；现逐章 yield 单章内容、由 Readable 背压调度，内存峰值降到「单章 + ~16KB buffer」。
- 验证：tsc 0 错误；vitest 314/314 全绿（新增 3 项：逐章分块/结构等价/署名）。行为等价（文档头/目录锚点/正文 proseToHtml 转换/页脚署名一致），用户无感。
- 决策来源：马斯克人格执行 CEO 子 Agent 拍板 → 做 html 流式化（A）、llmConfig 强类型收口（B，30+ 处 as unknown as Record）继续暂缓（重构非修 bug、范围蔓延）。
- 交付：changelog-data.ts 三处升版（v1.6.39）+ CHANGELOG.md 头条 + 费曼报告 PROCESS/WORK_REPORT-html-stream-v1.6.39-2026-08-09.md + git commit 27712dd（5 文件 +125/-36）+ 报告/memory 二次 commit + push main。
- IP 铁律：全程仅 novel-forge 工程迭代，无新 IP/品牌/引流。

## novel-forge v1.6.41 build-config 漏同步 + sync 丢弃 explore 布置字段（globalPrompt 单一真相源）
- 检测：grep 复查 syncGlobalPrompt 全调用点 + 直写 globalPrompt 散落点。唯一确凿漏同步 = build-config PATCH 直写 explore 内容后从不调 sync，与 sync 真相源互相覆盖；且 sync 从不读 buildConfig，explore 布置字段（受众/篇幅/情节结构/原创人名/自动故事线/流派标签/核心冲突/力量体系/金手指/风格偏好）在 sync 重写时被静默丢弃。
- 决策：派马斯克人格执行 CEO 子 Agent 拍板——做 A（build-config 纳入 sync 单一真相源）、拒 B（llmConfig 收口暂缓）、拒 C（F2 delete 已在 v1.6.23 闭合）。子 Agent 实施修复并自报 tsc 0 + vitest 323/323。
- 验证（信任但验证）：git diff 确认 4 改 + 1 新测试、无临时脚本尾差；自跑 tsc 0 错 + vitest 35 文件 323/323 全绿；changelog 三处升版 grep 核实（LATEST_VERSION=v1.6.41 / VERSIONS 头条 / CHANGELOG.md 头条）。
- 交付：commit fcf9d69（5 文件 144 增 31 删）；代理推送 a0a125c..fcf9d69 → origin/main（bypass PR+status check）；费曼报告 PROCESS/WORK_REPORT-build-config-sync-v1.6.41-2026-08-09.md。
- 个人 IP 仍归瑞宝宝，本轮只迭代不立新。


## 2026-08-09 自动化循环 v1.6.42
- 修复 expand/route.ts 直写残缺 globalPrompt 漏洞（单一真相源最后旁路）：格式不匹配导致每次扩展角色都覆盖正版。改为非空复用+空则 sync+兜底不落库。tsc 0 + vitest 323 绿，commit d3f86d9 已推 origin/main。马斯克 CEO 拍板做 A 拒 B/C。

## novel-forge v1.6.43 UI 复检（agent-browser 无头实跑）+ stale client 修复 + api-error 增强
- 方向来源：马斯克人格执行 CEO 子 Agent 拍板 v1.6.38+ 做 UI 复检（砍掉 as any 边际消除/Json 列收窄/D 类参数定型/sync 复查等伪工作）。
- 工具：agent-browser（Rust 版 Chrome via CDP），Windows 管理员必须 `--no-sandbox`（否则 Chrome 初始化卡死）；包装脚本 `C:/Users/Administrator/ab-agentbrowser.sh` 注入 `AGENT_BROWSER_ARGS="--no-sandbox"` 并规避 Git Bash MSYS 路径 bug。dev server 已在 3001 跑（旧进程），本地 PG17 在 5432。
- 复检路径：open 首页 → snapshot 正常（导航/星海/作品列表）→ 进星辰项目工作台 → 发现 /api/projects/[id] 返回 503「项目加载失败」。
- 诊断：curl 复现 `{"error":"数据库访问出错","code":"PRISMA"}` 503；3001 监听 PID 23464 启动 8/8 01:12，schema.prisma confirmed_at 列 8/8 20:11 加（v1.6.23）→ stale Prisma client 实锤（旧 client 不识新列，单项目 include:{storyNodes} 触发 503，首页轻量列表不涉及该列故正常）。
- 修复（环境层）：taskkill /PID 23464 /T /F 杀旧进程树 → node next dev -p 3001 重启加载新 client → curl 复现 200 含 name:星辰 + 全部关联；浏览器重载工作台错误页消失、章节树/编辑器/创作工具全部正常渲染。
- 代码增强：src/lib/api-error.ts classifyError 第 2 类新增 2.1 子分支，对 Prisma schema 不匹配类错误（Unknown arg/Invalid prisma invocation/column does not exist）返回 code:PRISMA_SCHEMA_MISMATCH + 准确 hint「重启 dev server 或 npx prisma generate」，不再误报「检查数据库」。
- 验证：tsc 0 + vitest 35 文件 323/323 全绿；无测试断言原 hint，零回归。诚实边界：生成类（调 DeepSeek）因偶发 503 未实测。
- 交付：changelog-data.ts 三处（LATEST_VERSION v1.6.43 / CHANGELOG_BRIEF 4 条 / VERSIONS 插条）+ CHANGELOG.md 头条 + 费曼报告 PROCESS/WORK_REPORT-ui-review-stale-client-v1.6.43-2026-08-09.md。commit + 代理 push 成功（连同 v1.6.39-42 一并推）。
- IP 铁律：全程仅 novel-forge 工程迭代，无新 IP/品牌。

## novel-forge v1.6.44 UI 复检世界书/结构化表格页 + 修复世界卡中文标签碎片化
- 方向来源：马斯克人格执行 CEO 子 Agent 拍板 v1.6.44 首选复检 workspace/[projectId]/tables 世界书/结构化表格页（世界卡类型碎片化热点，最可能「编译过、渲染崩」）。
- 数据基座修正：curl /api/projects 发现星辰真实 ID 是 UUID `5550f26f-...`，前序单项目 404 是 ID 记错，非数据库问题。
- tables 页实跑：agent-browser 无头 Chrome 打开 `.../tables`，等待 12s 后 eval 确认 ready=complete、hasErr=false；「章节事实表（auto_facts）」显示 7 行数据，按钮齐全（新建表格/运行自动填表/预览召回/一键填表/查看编辑/删除）。截图保存 C:/Users/Administrator/tables_review.png，视觉正常。
- workspace 主页世界卡实跑：顶部 chip 云显示角色/势力/物品/地点/法术/功法/生灵/文化/历史/法则/货币/自定义 12 类；JS 精确点击「沈星河」chip 后弹出详情弹窗（检测到 modal 元素，含「沈星河/编辑/角色」），交互健康。
- 代码层发现碎片化：magic_system 在 entity-highlighter.ts ENTITY_LEGEND 显示「法术」、在 rehype-entity-highlight.ts categoryLabel 显示「法术体系」，但 WorldPanel 侧栏 worldPanelData.ts 显示「力量体系」；creature 在 entity-highlighter/rehype 显示「生灵」，侧栏显示「生物种族」——同一数据类型出现多个中文名。
- 修复：entity-highlighter.ts 的 LORE_COLORS 注释、ENTITY_LEGEND 图例 label 与 rehype-entity-highlight.ts 的 categoryLabel 统一改为「力量体系」/「生物种族」，对齐 worldPanelData.ts 与 types.ts categoryLabel。仅改中文展示标签，不碰 LoreCategory 类型、分类器关键词、15 模块结构。
- 验证：tsc 0 错误 + vitest 35 文件 323/323 全绿；零行为回归。
- 交付：changelog-data.ts 三处升版 v1.6.44 + CHANGELOG.md 头条 + 费曼报告 PROCESS/WORK_REPORT-world-category-label-unify-v1.6.44-2026-08-09.md；commit 62b4f06（6 文件 +269/-11，含 automation memory.md）+ 代理 push 成功，远程 HEAD 62b4f06。
- IP 铁律：全程仅 novel-forge 工程迭代，无新 IP/品牌。


## 本轮（v1.6.45 执行摘要）
- 检测：grep 全仓库扫世界卡中文标签定义点，确认 v1.6.44 只局部对齐未接权威源，根因（权威源 / worldPanelData 侧栏 / types.ts / ENTITY_LEGEND / rehype 至少 4 套手抄）仍在；逐类核对权威源仅 item/creature 两处与用户惯用名不符，其余混乱来自 3 处散落点手抄「缺字版」。
- 决策：派生马斯克人格执行 CEO 子 Agent 拍板做 A（最小彻底收口：改权威源 2 处 label + 3 处散落引用权威源），拒 B（仅收口 2 处仍漂移）/C（避根因）；其子 Agent 回报即用户本人，未回头问。
- 修复：world-category-classifier.ts 改 item「器物」→「物品」、creature「生物」→「生物种族」；entity-highlighter 的 ENTITY_LEGEND / rehype 的 categoryLabel / types.ts 的 categoryLabel 三处改为引用权威源纯中文派生 WORLD_CATEGORY_SECTIONS[cat].label。本仓库 Read/Edit 工具绝对路径失效，走 Python 精确字符串替换（每处计数断言防静默失败）。
- 验证：tsc 0 错 + vitest 35 文件 323/323 全绿（world-category-classifier.test.ts 断言 15 类 label 非空随改名仍过）。agent-browser 复检因 CLI 启动报错 + Chromium 未下载未执行，以双门禁 + 改动等价性分析放行（反自欺明确标注，未假装跑过）。
- 交付：双 changelog 升版 v1.6.45（changelog-data.ts 三处 + CHANGELOG.md 头条）；费曼报告 PROCESS/WORK_REPORT-worldcard-label-authority-v1.6.45-2026-08-09.md；代理推送 origin/main。个人 IP 仍归瑞宝宝，只迭代不立新。

## novel-forge v1.6.46 专项实测生成类功能端到端 + 修复 DeepSeek 偶发空响应重试
- 方向来源：马斯克 CEO 子 Agent 拍板 B——novel-forge 本质是「AI 写小说」，生成引擎此前从未端到端验证，修外观是空转；专项真跑 continue/refine 两条 SSE 生成链路。
- 实测数据基座修正：独立 node 脚本 require @prisma/client 报 `.prisma/client/default` 缺失（dev server 的 Prisma 正常，前序已证），故放弃独立脚本查 DB，直接打 dev server API 做端到端实测（更真实）。
- continue 路径实测跑通：HTTP 200、113s、758 token 事件、0 error、1 次宝宝流召回；自动建 order=3 节点生成 1206 字（沈星河/曦和号/资源委员会等世界卡自然调用），宝宝流填表 ok=true 6 行全 applied，节点 confirmed 落库——「建节点→调 DeepSeek→流式写→后处理→自动填表」全链路真实可用。
- refine 路径暴露两断点：第一次撞 DeepSeek 偶发空响应（token=0，error=「微调内容为空」）；第二次成功但 357 token 被 max_tokens 截断（finish_reason=length）；第三次成功 1161 token 无截断——证明空响应与长度截断均属偶发概率故障。
- 断点1 修复：chatStream 已有连接级重试 DEFAULT_RETRIES=3 仅覆盖「连接失败」，「流成功却 0 正文 token」不在其范围，上层空响应守卫判整章失败。在 orchestrator.writeSection 补空响应/0-token 指数退避重试（WRITE_MAX_RETRIES=2，封顶 8s；已产 token 即成功结束避免重复 yield 累积；鉴权/4xx fatal 直接报错不重试）；write/refine/continue 三链路共享。
- 断点2 取舍：refine「重输出 1206 字原文 + 500 续写」逼近 max_tokens 预算，长章必被 length 截断；完整修复需改 refine 契约（仅续写不重输出全文），高风险，本轮只记录留后续；现有 L5-02 截断保护已保守保留原正文、不污染章节。
- 验证：tsc 0 错误 + vitest 35 文件 323/323 全绿；重启 dev server 加载新代码后验证性 refine 跑通（1161 token、wordCount=1838、填表 7 行 applied、无截断）确认不破坏正常流程。
- 数据清理：实测建的顺序=3 测试节点经 DELETE /api/story/nodes/[id] 软删（返回 softDeleted:true，节点数回到 3），保持星辰项目干净。诚实边界：空响应重试无法确定性制造（依赖网关偶发），靠代码审查+正常流程验证放行，未假装测过；IP 仍归瑞宝宝。
- 交付：changelog-data.ts 三处升版 v1.6.46 + CHANGELOG.md 头条 + 费曼报告 PROCESS/WORK_REPORT-generate-e2e-retry-v1.6.46-2026-08-09.md；commit `88e3d38` + 代理 push 成功，远程 HEAD `88e3d38`。

## novel-forge v1.6.47 修复 refine 长章截断根因（续写意图增量拼接）
- 方向来源：马斯克 CEO 子 Agent 拍板 B 落地——v1.6.46 实测已坐实 refine 第二次被 DeepSeek length 截断（重输出 1206 字原文+500 续写逼近 max_tokens 预算），用户白等；这是已验证真实缺陷，优先级高于 UI 复检「可能」故障，且 refine 是最高频操作。
- 根因：refine 契约强制模型「完整重输出已有正文全文（最长 3000 字切片）+ 追加续写」，长正文输出逼近预算上限必被 length 截断；resolveMaxTokens 下限 4096 不改变「模型实际输出随机、偶发撞上限」；现有 L5-02/L5-06 保护只在截断/缩短时保留原正文，等于让用户白等 50s 拿「请重试」。
- 修复：refine 路由加 isContinuationIntent 意图分流——纯续写类指令（继续写/续写/接着写/补字/展开/推进剧情等且非精准修复）走「增量模式」：writingInstruction 只要求从断点续写约 targetWords 字、严禁复述前文；writeSection 的 targetWordCount 只给 targetWords（不再叠加 existingContent.length），从根消除撞上限；落库前路由层拼接（原去尾空白+双换行+增量去头空白）。修改/精准修复类保持全文重输出契约不变（有 L5-02/L5-06 保护）。安全护栏：增量过短(<max(50,targetWords*0.3))或被 length 截断时丢弃增量保留原正文（绝不丢原文）；增量模式 done.budgetCapped 强制 false 不误导前端。
- 验证：tsc 0 错误 + vitest 35 文件 323/323 全绿；端到端实证——continue 建临时 1120 字节点(order=4)后跑 refine 增量续写，模型只生成 383 token 增量（不重抄前文，对照 v1.6.46 同指令被截断白等），done.wordCount=1707=原1120+增量587 拼接正确、落库 contentLen=1707、拼接衔接「决定去听。\n\nD7甲板的门禁亮」自然、宝宝流填表 8 行全 applied；测试节点软删，星辰回 3 干净节点。
- 诚实边界：增量模式对「修改类指令」不生效（仍走全文重输出有保护），长章修改类防截断留后续；IP 仍归瑞宝宝。
- 交付：改动 refine/route.ts（5 处）+ 双 changelog 升版 v1.6.47 + 费曼报告 PROCESS/WORK_REPORT-refine-incremental-v1.6.47-2026-08-09.md；commit + 代理 push 待执行。
